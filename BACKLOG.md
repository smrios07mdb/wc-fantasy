# BACKLOG.md — Pending features & fixes

Living work queue for XI — The Starting Eleven. Maintained alongside the brain files
(`PROJECT.md` / `DECISIONS.md` / `ARCHITECTURE.md` / `SCORING.md`); re-upload to Project
knowledge when it changes. **Guiding constraint: boring and reliable over clever.**

_Last updated: 2026-07-01._

---

## Legend

**Status:** `TODO` · `DIAGNOSING` · `IN PROGRESS` · `BLOCKED` · `VERIFY` (believed done — confirm in DB) · `DONE`
**Value:** Critical (correctness in a live competitive league) · High · Med · Low
**Effort:** S · M · L
**Risk class** (governs merge authority):
- `contained` — Code-autonomous on green gates (read/UI/loader, no schema/purity/auth change)
- `review` — Chat clearance before merge (resolver / purity / migration-adjacent / auth-or-reveal gated)
- `migration` — schema/RLS change; Sergio is merge authority

Gate: `pnpm -w typecheck && pnpm lint && pnpm format:check && pnpm test` (+ `pnpm --filter @app/web build` for web/CSS threads).

---

## Gating prefix — integrity & security (precede feature work)

These come before the T-items. A wrong standing or an open RLS hole outranks any feature. The first three closed as of 2026-06-21; **SEC-P3 (below) was a latent finding — the commish decision landed 2026-06-21 (T12 thread): the gap-window XI reveal is ACCEPTED (lineups public by design, ARCHITECTURE §5), so SEC-P3 is closed won't-fix.**

| ID | Item | Value | Status | Notes |
|----|------|-------|--------|-------|
| SEC-P0 | `faab_bid_select_settled` RLS — add `TO authenticated` (audit F-P2-01) | Critical | `DONE` (2026-06-21) | Scoped to authenticated league members (`8d0c036`). anon=0 verified live 2026-06-21. |
| SEC-P1 | `pool_pick` Realtime publication — auth-or-clock gate on SELECT (audit F-P2-02) | High (latent) | `DONE` (2026-06-21) | Clock-gated SELECT (`4cb29e5`, migration `20260621120000_fix_pool_pick_realtime_rls`), deployed live. `pg_policies` USING = league-member AND (own-pick OR `pool_pick_match_kicked_off(match_id)`); SECURITY DEFINER helper exists (`prosecdef=t`). Role-switched integration suite green. |
| DATA-VAR | VAR remediation — stale `stat_player_match` conceded rows re-scored | Critical | `DONE` (2026-06-19) | Whole VAR theme (incl. Algeria–Argentina / Martínez remediation) closed; commish confirmed 2026-06-21. **Do not reopen.** |
| SEC-P3 | Vs-the-Field shows the next wave's XIs during the inter-matchday GAP (pre-existing) | Low–Med (latent) | `DONE` (resolved 2026-06-21 — accepted) | Surfaced by the T11 review. `loadVsField` pins the current wave via `selectCurrentPeriod` (predicate `now < lastKickoff + MATCH_DURATION_MS`, true for a future wave). Once a wave's last match ends, the next (not-yet-kicked-off) wave becomes the default view, so every manager's lineup_slot XI for it is rendered before kickoff. **Vsfield has no per-fixture reveal gate at all (it shows all XIs by design — ARCHITECTURE §5); the `readVisiblePicks` kickoff gate is the *pool* surface, not vsfield.** PRE-EXISTING — the default selection is byte-identical pre/post-T11, and T11's selectable set (started-only) does NOT widen it. Decision for commish: is the gap-window XI reveal acceptable (lineups public by design) or should vsfield gain a per-fixture reveal gate (mirror pool's clock gate)? **RESOLVED 2026-06-21 (commish decision, recorded in the T12 thread): the gap-window XI reveal is ACCEPTED — upcoming lineups are public by design (ARCHITECTURE §5), so vsfield will NOT gain a per-fixture reveal gate. Won't-fix / accepted.** |
| SEC-P3b | Set-lineup validator has no period-DONE gate (defense-in-depth) | Low | `TODO` (flagged 2026-06-21) | Surfaced by the `fix/t11-corrections` review. `validateLineup` / the POST `/api/lineup` controller enforce the lock-on-play latch + forfeit play-state rules, but nothing rejects a write to a fully-COMPLETED period outright. A crafted POST could rearrange never-appeared / zero-scoring (never-locked) players within a done period. **UI-unreachable** (the client gates every mutation off `readOnly`) and **zero standings impact** (those slots scored 0 and stay 0; played slots are already latch-protected). Defense-in-depth only: add a `periodIsDone`-style server gate that rejects any lineup write to a completed period. Effort S, risk review (touches the shared write path → hold). |
| CONTRACT-P2 | `league.status` direct-read on the FAAB/waiver path → derive phase from `playoff_entry` existence (the data-existence contract the dashboard/playoffs loaders honor) | High (latent — live FAAB/waiver phase) | `DONE` (merged `9c753c3`, 2026-06-26) | `fix/league-status-phase-contract`. The two read sites (`loadWaivers.ts` + `@app/faab/prisma` `loadReleaseContext`) now key phase on `loadPlayoffPhaseActive` (= `playoffEntry.count({where:{leagueId}}) > 0`), the **atomic twin** of `status='playoff'` (both written in one `applyTransition` `$transaction`). Behavior-identical on the live path; `selectTournamentPhase` deliberately NOT the signal (regresses the R32 pre-kickoff trim window + post-Final). Source-only — no schema/migration/RLS/DB-write; `packages/scoring`+`packages/recompute` untouched/unread. TDD red→green: `playoffPhase.test.ts` (unit) + gated-PG `release.integration.test.ts`/`loadWaivers.integration.test.ts` + `loadWaivers.contract.test.ts` (source-shape). Full gate green (2622 tests + web build + gated PG T2/T4). **Shared-contract → Sergio is merge authority.** See DECISIONS → "FAAB/waiver phase derives from `playoff_entry` existence". |
| FAAB-FA-P2 | `/waivers` FA panel won't surface for R16/QF/SF/Final once their batch has cleared (pre-kickoff) | Med (latent — live FA path) | `RESOLVED` (not-a-bug, 2026-06-28) | **RESOLVED — not a bug (2026-06-28).** Premise was a STALE docstring (`periodOrder.ts:67-69` / `loadWaivers.ts:142`) predating `feat/period-status-lifecycle` (2026-06-17). Verified read-only against the lifecycle wiring: the hourly `wc-fantasy-period-close` cron is the SOLE writer of `status='open'` and AUTO-opens each knockout round (R16→QF→SF→Final) when the prior round's fixtures all reach `completed`, ~1 day before that round's FA window — so the FA panel mounts for every knockout round exactly as for R32 (the `open` fast-path of `selectCurrentPeriod` selects the round regardless of `batchClearedAt`). **No code change.** **Residual (real but narrow): the period-close cron is a single point of failure** — a stall (e.g. this week's MD3 freeze miss) or a postponed/abandoned prior-round fixture (anomaly blocks close) leaves the next round `pending` and the FA panel hidden → see `docs/RUNBOOK.md` → "Knockout transitions — pre-flight". Docstrings corrected (DECISIONS Prompt-54 bullet, ARCHITECTURE §15, `periodOrder.ts` comment). ~~Original premise (false):~~ Surfaced by the `feat/faab-fa-cap-parity` thread. `loadWaivers.ts:142` pins the current period via `selectCurrentPeriod(periodRows, (p) => p.batchClearedAt === null)` — a period whose blind-bid batch has ALREADY cleared (the free-agency gap: post-clear, pre-first-kickoff) fails that predicate, so `currentPeriodRow` skips it and the **FA panel never mounts** for that round. R32 works only because its period is already `status='open'` at the R32 transition; the later knockout periods (R16/QF/SF/Final) won't surface the FA panel unless each is flipped to `status='open'` (or the predicate widened to "cleared-but-pre-first-kickoff") at its transition. Scope: loader period-selection only — the cap-parity fix (this thread) is orthogonal and already merged-pending. Effort S, risk `review` (touches the live FA loader's period gate → hold). **Do NOT conflate with the cap-parity fix.** |
| CONTRACT-P3 | Migrate the FAAB ENFORCEMENT cap (+ playoff-participant phase) off `league.status` onto `playoff_entry` existence | Med | `DONE` (merged `87cc048`, 2026-06-27) | `fix/league-status-enforcement-cap` (Option B). All four enforcement loads (`loadManagerBidContext` / `loadManagerFaContext` / `loadBatchContext` / `listOverCapPlayoffSurvivors` in `@app/faab/prisma`) now derive BOTH the cap AND the participant signal from `rosterCapForPlayoffPhase(loadPlayoffPhaseActive(...))`; `league.status` is fully removed from `@app/faab` and `rosterCapForLeagueStatus` is **DELETED** from `@app/shared`. Provable no-op on reachable spend behavior (status⟺entry-existence; the `complete` arm is unreachable today → robustness, not change). **No `switch`/`never` guard added** — a repo-wide census found NO exhaustive `switch` over `LeagueStatus` (the deleted helper was a ternary; every remaining site is `===`/`!==` equality), so a guard would be ceremony, not safety. Cap read stays pre-commit (never inside the `commitBatch`/`claimFreeAgent` `$transaction`). **Survivor (by design, NOT a gap):** the worker batch CADENCE selector (`apps/worker/src/faab/prismaStore.ts:23`, `status: { in: ["group","playoff"] }`) still reads `league.status` — a separate axis (which leagues to batch), NOT the cap. TDD red→green: `enforcementCap.contract.test.ts` + gated-PG `enforcementCap.integration.test.ts` (its own `FAAB_CAP_PG_TEST_URL` var). Full gate green (typecheck/lint/format/**2626 passed**/`@app/web` build; gated PG enforcementCap 12 / release 6 / faabSettledRls 3). **Shared-contract on the live FAAB spend path → Sergio is merge authority.** See DECISIONS → P2/P3 block. |
| P1a | Period status-advance is a single-writer SPOF — the hourly `wc-fantasy-period-close` cron is the SOLE writer of `period.status`, so a stall silently skips a knockout round's `pending → open` (FA-window) mount. **Verified hard-miss:** on recovery the now-completed round is swept `pending → closed` directly (`toClose` matches `status != "closed"`), never `open` → the round's FA window is lost permanently | High (latent — live FA-window mount; cron already stalled once, MD3 freeze miss 2026-06-28) | `DONE` (implemented 2026-06-29 — `feat/tick-status-advance`, MERGED `4c53d0a` 2026-06-29) | **Dual-writer redundancy** (design locked in Chat). The resident 60s worker tick now co-hosts status-advance as a SECOND writer: `dispatchPeriodStatusAdvance` (`apps/worker/src/period/dispatch.ts` + `store.ts`/`prismaStore.ts`/`memoryStore.ts`, wired LAST in `scheduler.ts`'s tick) re-runs the **UNCHANGED** pure `selectPeriodStatusTransitions` and applies the **SAME** guarded `updateMany` (close `status != "closed"`, open `status = "pending"`) as the cron. Equivalent idempotent claims (the `dispatchFaabBatches` `batch_cleared_at`-latch precedent) → cron+tick near-simultaneous = clean no-op for the loser; steady state skips the transaction. **Additive — `periodClose.ts` + `periodStatus.ts` byte-UNCHANGED.** Freeze stays cron-only (its plain `update` relies on the `frozenAt:null` query filter, not a WHERE-guard → not dual-write-safe). **Non-fix (by design):** the anomaly hold (a stuck-`open` N−1 blocks N's open) lives in the pure selector, caller-independent → NOT bypassed. TDD red→green: `apps/worker/src/period/dispatch.test.ts` (applies once + idempotent across consecutive ticks + steady-state no-op). Full DoD gate green (typecheck / lint / format / **2723 passed**, 48 gated-PG skipped). **Worker hot-path + shared-resolver call site → Sergio is merge authority (deploys in a match-free window).** See DECISIONS → "P1a: dual-writer status-advance" + ARCHITECTURE §22. |
| P2 | FAAB-batch / open readiness guard — a knockout period can flip `pending → open` (and its FAAB batch clear) BEFORE the group→playoff `commish:transition` seeds its field. `toOpen` (`periodStatus.ts:94`) has no field-readiness guard: it opens the earliest non-closed `pending` period purely on canonical label order. R32 is uniquely dangerous (the transition that seeds `playoff_entry` is a manual CLI); R16/QF/SF/Final re-incur it pre-`commish:advance` cut | Low–Med (latent — bounded by transition-vs-batch timing) | `TODO` (deferred 2026-06-29; read-only discovery this thread) | Discovery (read-only) found the open-before-seed is largely **cosmetic at the write layer** — neither write path gates on `period.status='open'`. FAAB gates on `playoff_entry` existence/`alive` (`loadIsPlayoffParticipant`): while unseeded it falls back to GROUP rules (everyone participates) and the D4 resolver voids non-participants once seeded; lineup gates on `status != "closed"` + `closesAt` + per-slot locks + `period.kind` mode, with NO participant gate (an eliminated manager can set a knockout lineup, but it is scoring/advancement-neutral). Real exposure = the narrow window where a period is open but the transition hasn't seeded the field before that period's FAAB batch clears. **Candidate fix:** add a field-readiness guard to `toOpen` (don't open a knockout period until its fixtures exist / for R32 until `playoff_entry` exists). Touches the shared pure `selectPeriodStatusTransitions` → `review` / Sergio. **REJECTED this thread: a DB heartbeat / last-run column for cron-stall detection** — migration-class and made unnecessary by P1a's dual-writer redundancy. |
| A-lite | Cron-resilience **DETECTION** — external **liveness** dead-man's-switch (catches **P1a**: a stalled/crashed cron) + **anomaly attention** ping (catches **P1b**: a HEALTHY cron blocked by a postponed/abandoned fixture). Complements the P1a dual-writer (PREVENTION) with VISIBILITY: the dual-writer self-heals status-open but never *tells* anyone the cron died (and freeze stays cron-only), and it CANNOT see P1b — the cron runs + reports `done` every hour while an anomaly blocks the next open | Med (operational visibility — cron already stalled once, MD3 freeze miss 2026-06-28) | `DONE` (implemented 2026-06-29 — `feat/period-close-heartbeat`, MERGED `438ae91` 2026-06-29) | Two env-gated, fire-and-forget HTTP signals (`apps/worker/src/jobs/heartbeat.ts`): **liveness** = `ping(PERIOD_CLOSE_HEARTBEAT_URL)` after `job.periodClose.done` (+ `…/fail` on the crash path, Healthchecks.io convention) → monitor alerts on ABSENCE; **attention** = `ping(PERIOD_CLOSE_ATTENTION_URL, {anomalies, leagueId})` only when a run's `anomalies > 0` (same count the `done` log carries) → monitor alerts on PRESENCE. **PURELY observational (the load-bearing invariant):** `ping` swallows every failure (throw / ~5 s `AbortController` timeout / DNS / non-2xx) and never rethrows; `safePing` adds a second isolation layer; an UNSET URL is a silent no-op → the job is byte-identical in local/test/pre-monitor envs. Pings sit AFTER the `done`/`error` logs → `job.periodClose.done`/`.error` + the 0/1 exit codes byte-UNCHANGED. **Operator setup (Sergio, Render dashboard — NOT done by Code):** set `PERIOD_CLOSE_HEARTBEAT_URL` + `PERIOD_CLOSE_ATTENTION_URL` on the cron service (`render.yaml` declares both `sync:false`); liveness = a Healthchecks.io-style check (~hourly period + ~15 min grace, alert on missed ping; `/fail` = immediate crash alert); attention = a check/webhook alerting on receipt (Slack/email/SMS) with dedup so a multi-day postponed match doesn't spam. If a native Render "notify on cron failure" toggle is used for liveness, leave `PERIOD_CLOSE_HEARTBEAT_URL` unset (code no-ops it); the attention half is content-based → needed regardless. TDD red→green: `apps/worker/src/jobs/heartbeat.test.ts` (the never-throw property IS the test; +17 → **2740 passed**). Worker-only — no schema/RLS/web/CSS/migration. **Touches the cron deploy surface (render.yaml → cron redeploys on merge) + inert until env+monitor set → Sergio is merge authority (coordination hold, not risk-class).** The DB-heartbeat column stays REJECTED (P2, migration-class) — this is the no-migration form of the same detection. See DECISIONS → "A-lite: cron-resilience DETECTION" + ARCHITECTURE §22 + RUNBOOK → "Knockout transitions — pre-flight". |
| SEC-P4 | Quiniela `POST /api/pool/pick` accepts a pick on an undecided knockout match (no server-side teams-resolved gate) | Low (latent) | `DONE` (implemented 2026-06-28 — `feat/sec-p4-undecided-pick-guard`, MERGED `184391a` 2026-06-29) | Surfaced during the knockout-Picks work (post-`b41d1be`). The Picks UI hid pick buttons on undecided matches (a side is a feed placeholder, name `/^Team \d+$/`), but `validatePickSubmission` / the `POST /api/pool/pick` handler did NOT reject a pick on such a fixture — a crafted POST could store a pick on a TBD knockout match before its teams are known. **UI-unreachable** (client renders no button) and was moot until the knockout sides resolve (~Jul 15). **Fixed:** `validatePickSubmission` now rejects a pick on an undecided knockout fixture (placeholder name **or** null team FK) with a typed `pick-on-undecided-match` error → HTTP 409, keyed on the `isPlaceholderTeamName` predicate **lifted into `@app/pool`** so the server gate and the UI `isKnockoutFixturePickable` share ONE source (no drift; `getMatchFacts` now selects `homeTeam.name`/`awayTeam.name`). Knockout-only (group + unseeded `periodKind` null no-op); 3rd-place play-off covered via `resolvePoolPeriod`. Scoring/reveal/schema byte-untouched. TDD RED→GREEN; full DoD gate green (2720 tests / 48 gated-skipped, +23) + web build; adversarially reviewed (4-lens). Docs-adjacent, no migration → Chat-cleared; MERGED to main `184391a` (2026-06-29, `--ff-only`) on a green gate. See DECISIONS → 2026-06-28 (SEC-P4) + PROJECT.md + ARCHITECTURE.md §6. |
| FAAB-COPY-P1 | `/playoffs` `ReinforceModule` + `PLAYOFF_EXPLAINER` still render "FAAB reset to $100" / "a clean $100 at the group→playoff transition" — contradicts the corrected rule (ONE $100 for the whole tournament, group + playoffs, NEVER reset; group spend carries forward) | Med (user-visible, live, wrong — but display-only, no engine effect) | `DONE` `4e98f1d` (2026-07-01) | **DONE (`4e98f1d`, 2026-07-01):** rewrote every user-facing FAAB string to the carry-forward rule (ONE $100 for the whole tournament, group spend carries into the playoffs, NEVER reset) — `/playoffs` `ReinforceModule` (meter caption relabelled to the tournament budget, `po-reset-tag` pill → "Carries over · no reset", the "clean $100" sentence) + `PLAYOFF_EXPLAINER`; renamed the misnomer `FAAB_RESET` → `FAAB_TOURNAMENT_BUDGET` (display denominator only, comment cites DECISIONS). Grep found + fixed the siblings too: `/waivers` `wv-resetbanner`, dashboard `PrimaryBanner` playoff sub + `ReinforceModule` pill/note, marketing-landing bullet. jsdom render test for `ReinforceModule` + source-contract check for `PLAYOFF_EXPLAINER`; full gate green (2809 tests + web build). Out of scope + LEFT (non-user-facing, noted): `schema.prisma:173` + `packages/recompute/src/transition.ts:14` stale reset comments, and the `wv-resetbanner`/`db-rf-tag`/`isPlayoffPhase` gate names. --- Original context: The engine reset was already stripped (`transitionStore.ts`, `cca831c`) and DECISIONS.md corrected (`7b12f18`), but the UI STRINGS in `apps/web/app/playoffs/components.tsx` were missed — `ReinforceModule` (the `FAAB_RESET=100` meter caption, the `po-reset-tag` "FAAB reset to $100" pill, and the "Each surviving manager gets a clean $100 …" copy) plus the `PLAYOFF_EXPLAINER` "FAAB reset to $100" clause. Fix = rewrite the copy to the one-$100-whole-tournament rule (and reconsider the `FAAB_RESET=100` framing / the meter denominator). Belongs to the FAAB thread; do NOT fix here — this is the tracking row only. Effort S, risk contained (route-scoped copy, no engine/schema/RLS). |

---

## Triage — open feature work

| ID | Item | Value | Effort | Risk | Status | Notes |
|----|------|-------|--------|------|--------|-------|
| **T5/T6** | Game detail — click a game (dashboard + Quiniela) → both squads (XI+subs+cards) + every player's live pts + fantasy-owner overlay | High | L | contained (new loader; review-class) | `DONE` | `feat/game-detail` — full DoD gate green (2466 tests + web build); merged to main `7b95103` (review-class). "Games tab" resolved → `/pool` (Quiniela). New `/games/[matchId]` route + pure `buildGameDetail` + read-only `loadGameDetail` (no engine re-run, no RLS/migration, no Realtime). |
| **T11** | Full stat sheet for previous matchday — in Lineup, Vs the Field, Waivers | Med | M | contained (reveal-adjacent → review-class) | `DONE` | `feat/prior-matchday-selector` — merged to main `9c9ceb5` (selector) + `b309003` (corrections) + `938ab21` (R2). Prior-matchday selector on all three surfaces, every period read routed through the EXISTING period-aware models with a `periodId` param (shared pure `selectablePeriods.ts`); selectable set = started periods only (`isPickLocked` on `matches[0]`), future/unstarted NEVER selectable; prior views strictly read-only; `PlayerScoreSheet` reused verbatim; NO new table/RLS/migration/Realtime. **Corrected by `fix/t11-corrections` (merged `b309003`):** (A) prior **Lineup** now renders from the period's `lineup_slot` snapshot so a fielded-then-dropped player still appears (`snapshotPlayers`); (B) **Waivers** drops the over-applied selector — period concept confined to matchday-labelled Batch results. Display-only; +2 jsdom render tests; gate green. **Round 2 — `fix/t11-corrections-2` (merged `938ab21`):** (A-2) the **Lineup** pitch shows per-player points on every LOCKED-ON-PLAY tile (the pill now keys on the SAME `!movable` condition the bench uses — decoupled from `readOnly`/`slotKind`, so it works on the CURRENT matchday once matches play, not only read-only priors; movability unchanged via `movable ? onSelect : onScore`) + the manager's canonical matchday total (`score_manager_period.points`, single-sourced via `selectManagerPeriodTotal`, never re-summed, so it matches standings; banner shows whenever a stored total exists — prior OR current-with-scoring); (B-2) **Batch results** group by player (`groupResultsByPlayer`→`WvResultGroup`) — one entry per contested player with all bids (winner + losers) beneath. Display-only, no movability/write/engine/RLS change; tests incl. an editable-period render proof; gate green (2515 tests + web build). |
| **T12** | "Season" tab: each team's score by matchday | Med | M | contained | `DONE` (2026-06-21) | `feat/season-tab` — dedicated **Season** tab + a managers × matchdays score grid on `/standings`, built by pure `buildSeasonGrid(view)` (`@app/recompute`): a re-projection of the EXISTING `StandingsView`, **no new query / no new scoring** — each cell = that cumulative row's `perPeriod` points (re-keyed by `periodId`), Total column = the cumulative PF (so the grid can never disagree with Cumulative). Started-set convention mirrors the Matchday tab (`live || view.matchday[id].length>0`); unstarted matchdays render blank, a live-but-unscored one is a real 0. `SeasonGridPanel` = scrollable `<table>` + sticky Manager column (`.st-season-*`). +7 contract tests; adversarially reviewed (4-lens workflow) → 7 confirmed findings, all P3 polish, all fixed pre-merge. Full DoD gate green (**2522** tests + web build); contained → Code-autonomous merge on green. |
| T13 | Lineup-wide kit jerseys: replace PlayerAvatar discs with JERSEY_BG kit chips on BOTH starters and bench (shipped lineup uses discs — prototype JERSEY_BG was never ported to apps/web). Preserve flag badge + real-XI dim + lock states. | Med | M | review-class | `DONE` | Merged to main `e585f1b` (feat(lineup): T13 lineup-wide flag-kit jerseys + neutral kit module). VERIFIED in repo: the neutral resolver lives at `apps/web/src/kit/kitOf.ts`; `lineup/components.tsx` `PlayerKit` replaces the position-color `PlayerAvatar` disc with the `.sl-kit` flag-kit chip on BOTH pitch tokens and the bench, `kitBg` resolved once upstream in `SetLineupClient` via the shared `kitOf` (no `background-size:cover`). Original note: kit resolver neutralized to apps/web/src/kit/ (out of vsfield/); shared .sl-kit silhouette promoted from t13 .sl-bench-kit; feat/t13-bench-kit-chips was reference-only for the chip silhouette |
| **T14** | Vs-the-Field benches: show each bench player's points + click → player card | Med | M | contained | `DONE` | Merged to main `812cda3` (feat(vsfield): T14 bench points + player-card drill-in). VERIFIED in repo: `vsfield/components.tsx` `BenchScoreChip`/`BenchToken` render each bench player's real `score_player_match.points` (via `benchFor(view.benches, …)`), and `BenchStrip`'s `onOpenPlayer={setBoxPlayer}` opens the reused `PlayerScoreSheet` from `VsFieldClient` (`Benches.test.tsx` + `loadVsField.benches/points.test.ts` cover it). Extends shipped T1 (`loadVsField` already carries benches). |
| **T-3RD** | Quiniela 3rd-place match — source/render/score the lone `period_id IS NULL` fixture ("Match for 3rd place") | Low–Med | S–M | review + migration | `DONE` — **MERGED** `4aadc82`/`8d3c76c` (Sergio = merge authority + applies migration) | Shipped **Option B** (IO-loader period-kind synthesis) on worktree `t-3rd-pool-third-place`; **Option A rejected** in discovery (`loadPlayoffs` keys rounds on `period.kind`, so a real 6th `knockout_round` period pegs `playoffsView` `liveIdx` forever → tournament never `complete`). The match stays **period-less** (invisible to lineups/scoring/guillotine/playoffs); a pure `resolvePoolPeriod` maps the new additive `fifa_match.is_third_place` flag → synthetic `{ "knockout_round","3P" }` for the pool engine at one seam (`loadPool` fixtures + `leaderboardMatches` projections; `getMatchFacts` write-path DRAW guard). Pure `@app/pool` engine + `poolView.ts` **byte-untouched** (defensive non-canonical-label branch renders "3P" after the Final; non-pickable until both teams resolve ~Jul 15). Flag set by `@app/ingest mapMatchRow` (`/3rd place|third place/i`); `derivePeriodLabel` guarded null before `/final/`; `"3P"` is pool-local (NOT in `@app/shared KNOCKOUT_ROUNDS`). Migration `20260628130000_fifa_match_is_third_place` (additive, defaulted, NO RLS/backfill). Full DoD gate green (2697 tests + web build) + gated-PG `migrate deploy` proven on throwaway Postgres (pool-RLS 3/3 + advance-store 3/3). See DECISIONS/PROJECT/ARCHITECTURE 2026-06-28 (T-3RD). **Go-live (Sergio):** `prisma migrate deploy` on `DIRECT_URL` from a Render shell, then confirm the 3rd-place fixture shows in the `/pool` bracket (TBD until ~Jul 15) and a pick saves. Chat-cleared + merged (`4aadc82`/`8d3c76c`); the `migrate deploy` go-live above remains Sergio's step. |
| **T2** | Waiver watchlist / "star" a player to track | Low | M | migration | `DONE` — **MERGED** `fb808bf` | `feat/waiver-watchlist` (off `d15a470`). New private `watchlist` table + owner-only RLS (4 `TO authenticated` owner-only policies — exactly ONE SELECT, no league-visible reveal); scope-agnostic, fully-decoupled toggle API (`POST /api/manager/watchlist`, touches no bid/roster/budget/scoring); read = `loadWaivers` self-scoped id-set + FA-row star + card star + "Watched" filter. Full DoD gate green (**2761 passed** + web build + gated-PG RLS 11/11). Merged by Sergio (migration + RLS applied). See PROJECT/DECISIONS/ARCHITECTURE 2026-06-30 (T2). |
| **T15** | Mobile UX/UI audit → responsiveness overhaul, bottom nav first (target: Apple-feature-worthy) | High | L (epic) | review/design | `TODO` | **EPIC, not a single thread** — decompose: read-only audit punch-list → AppShell/bottom-nav → per-screen passes; canonical `design/design_reference/`, verify on live Render |
| **T16** | Re-skin `/games/[matchId]` game-detail to the match-detail design handoff | Med | M | review/design | `DONE` | `feat/game-detail-reskin` — merged to main `cbf4d36`. Scoreboard + FANTASY exposure line + YOUR-XI stake strip + two tabs (**Lineups** formation pitch w/ flag-kit jersey tokens [`kitOf`] + enriched XI/Subs/Bench lists; **Ratings** podium + Fantasy-MVP + ranked board); `PlayerScoreSheet` drill-down reused unchanged. Visual-only EXCEPT the one approved **Ratings additive read** (read-only `ratingPlayerMatch.findMany` → nullable `PlayerLine.rating`, resolved in the pure builder via shared `resolveRating`, manual>balldontlie; NO migration/RLS/Realtime/engine/validator). Stats/Events/Standings tabs deferred (data not loaded → T16b/T17/T18). +rating-resolution test; **2523** tests + web build green; 4-lens review → no P0–P2, 3 P3s fixed. |
| **T16b** | Game-detail **Events timeline** tab + scoreboard scorers row | Med | M | review-class (pure-builder contract change) | `DONE` | `feat/game-detail-events` — merged to main (ff). Pure `buildGameDetail` now emits an ordered **`events[]`** (goals w/ scorer+assist + replayed running score · subs on/off · yellow+red cards · synthesized KO/HT/FT markers from `event.period`) consumed by the **Events tab** (between Statistics & Ratings) AND the **scoreboard scorers row** (⚽ surname + minute, grouped by side, own goals tagged `(OG)`). Classification keys on the SHARED `@app/recompute` predicates — `classifyCard` + the newly **export-only** `isGoalEvent`/`isOwnGoalEvent`/`overturnedGoals` (scoring byte-identical) — so own goals credit the opposing side, VAR-disallowed goals are excluded, every `varDecision` row dropped (no VAR display). A terminal computed-vs-stored mismatch / unresolved-scorer goal is surfaced as a soft `eventScoreAnomaly` (never a silent desync). **Two additive selects** (`assist_player_id` + `period`) on the existing `event_match` read — both columns pre-exist → **NO migration/RLS/Realtime/validator/engine-logic change**; the `@app/recompute` touch is export-only. Narrow safety note: `event_match` **is** a scoring input (unlike T17's `stat_team_match`), but only the DISPLAY loader changed. 4-lens adversarial review → no P0/P1; 3 P2 test-coverage gaps closed + cheap P3 robustness fixes; full DoD gate green (**2594** tests + web build). |
| **T17** | Game-detail **Statistics** tab (team match-stats) | Med | L | ingest-retain + read + design | `DONE` | `feat/team-match-stats` — merged to main `1662356` (ingest+read) + `4840f1f` (backfill CLI). (1) **Ingest-retain:** `mapTeamStat` now writes the full remaining team payload to `stat_team_match.extra` via `buildTeamStatExtra` + `TEAM_STAT_EXTRA_OMIT` (the 3 typed feed keys + `match_id`/`team_id`/`is_home`) — typed columns + upsert key BYTE-untouched; **NO migration** (`extra` column pre-existed). (2) **Additive read:** read-only `statTeamMatch.findMany` in `loadGameDetail` (mirrors the T16 Ratings read — loader thin, pick in the pure builder; owner-bypass; NO RLS/migration/Realtime). (3) **Pure builder:** `buildGameDetail` emits a `statistics` view-model (home/away by `team_id`→home/away id match, from typed cols + parsed `extra`; accuracy/duels derived as own-rate %; render-safe nulls → "–"); returns null → tab hidden. (4) **UI:** Statistics tab between Lineups & Ratings (shown only when non-empty), home-vs-away comparison bars per `design/design_reference/match_detail/`, ds tokens. **Display-only / fantasy-safe** (grep-confirmed: `stat_team_match` has zero engine reads). **Scoped out:** the All / 1st-half split (no per-half team data in the feed) — full-match only. **⚠️ Operator go-live step (one-time backfill):** completed matches ingested before this landed have empty `extra` (rich rows show "–") and `ingestSettle` does NOT re-pull team stats → run the new **`job:ingest-team-stats`** worker CLI once after deploy (`pnpm --filter @app/worker job:ingest-team-stats` in the wc-fantasy-worker Shell): lists `status='completed'` matches → `ingestTeamStats` (same `mapTeamStat`/`upsertTeamStat` path, foreign-guard + per-match isolation, **no dirty-mark / no recompute**, idempotent). +ingest extra tests + `ingestTeamStats` job tests + builder statistics contract pin + loader read pin; **2530** tests + web build green. See PROJECT.md → 2026-06-22 (T17) + ARCHITECTURE → `stat_team_match`. |
| **T18** | Game-detail **Standings** tab (real FIFA group table) | Low–Med | M | **migration** (new table + RLS) + ingest | `DONE` | `feat/game-detail-standings` — full DoD gate green (**2613** tests + web build + gated `group_standing` RLS integration proven on real Postgres), merged to main `79162b6` (**migration-class**: new table + RLS; **operator go-live still owed** — Sergio applies the prod migration + runs the backfill CLI, see ⚠️ below). **Premise correction:** `fifa_group`/`fifa_stage` + `fifa_team.group_id`/`fifa_match.group_id` already EXIST in the schema but are **never populated** (the match mapper only reads `group.name` transiently for the period label) → group is derived via the **standings rows' own `team_id`**, NEVER `fifa_match.group_id` (NULL). **D1 self-contained schema:** new `group_standing` table (PK `team_id`, denormalized `bdl_group_id`+`group_name`, `season` default 2026) — **NO** FK to `fifa_group`, **NO** change to `fifa_team`/`fifa_match`. **Feed:** new `@app/feed` `groupStandings()` (GET `/group_standings`, season-scoped, **non-paginated**; ALL-STAR-or-higher tier — the app's GOAT key covers it, same credential) — live-smoke confirmed 48 rows / 0 null cols / `group.name`="Group A". **Ingest:** `mapGroupStanding` (nested `team.id`/`group.id`) + `ingestGroupStandings` (single call, `eachItem` isolation) + store foreign-guard (skip a team not in `fifa_team`) — **NO dirty-mark / NO recompute** + `job:ingest-group-standings` CLI + a daily `wc-fantasy-group-standings` render.yaml cron (mirrors period-close). **RLS:** `ENABLE` + `group_standing_select_all` (`FOR SELECT TO authenticated USING (true)`), NO anon, NO realtime publication; server reads owner-bypass (defense-in-depth) — proven by a role-switched gated integration test (anon→0 rows, authenticated→all, owner→all). **Read+builder:** additive `groupStanding.findMany` (group-scoped via the two team_ids) → pure `buildGroupStandings` returns null (tab hidden, A1) when a side is TBD/null, the two teams are in DIFFERENT groups (knockout), or no rows ingested; else sorted by feed `position`, top-2 green cutline + the two in-match teams highlighted + tie-break footnote. **UI:** Standings tab LAST (`Lineups·Statistics·Events·Ratings·Standings`), `.gd-gr-*` ported from handoff `.md-gr-*`. **Display-only / fantasy-safe** (grep-confirmed: ZERO engine reads of `group_standing`; `packages/scoring`+`packages/recompute` byte-untouched). **Scoped out (D2):** the `Last`/form column (the `group_standings` feed has no form field) + the cross-group best-third-placed race (tournament-wide). **⚠️ Operator go-live (Sergio):** (1) `prisma migrate deploy` on `DIRECT_URL` from a Render shell applies `20260626120000_group_standing` (the same path `match_lineup_entry` reached prod); (2) run **`job:ingest-group-standings`** once post-deploy to populate the table. See PROJECT.md → 2026-06-26 (T18) + ARCHITECTURE → `group_standing` + DECISIONS → 2026-06-26 (T18). |
| **T-MPITCH** | Game-detail mobile pitch — formation grid + definite-height chain + full-token budget + halfway-line containment | Med | S | CSS/presentation | `DONE` ✅ | Shipped `33e71e5` + `d7efad8` on `main`. Three compounding failures on iPhone (NED–SWE, ARG–AUT dense MD2): column layout caused horizontal scroll → **formation grid** (CSS grid, 100cqw, `--gd-col-w = 100cqw/(cols+1)`); `cqh` container collapsed to zero (no definite-height ancestor) → **`:root:has(.gd-host)`** `:has`-scoped `height:100%` chain; fpts `<b>` pinned fixed size + phalf no containment → **corrected `--gd-line-h`** accounting for padding+gaps + **`overflow:hidden` on `.gd-phalf`**. Gate: iPhone on the densest real fixture (formation holds, no cross-halfway bleed). See DECISIONS.md (2026-06-22 mobile pitch entry). |
| **T-RECON** | Game-detail kickoff-XI reconciliation — pitch = computed kickoff XI, not the over-marked `is_starter` sheet | High | M | review (pure-builder contract change) | `DONE` | `feat/lineup-kickoff-reconciliation` — merged to main `8385c93`. BALLDONTLIE over-marks `is_starter` on some completed matches (a side rendered 12). Read-time cascade in the pure `buildGameDetail`: candidates = `is_starter` ∪ `player_out`; keep iff NOT a `player_in` AND (`player_out` ∨ `minutes>0` ∨ on-field event); a null-minute/no-event flagged starter is a phantom → bench, dropped **only for a terminal match with minute data** (the live-safety gate — a mid-match XI must not collapse on the first posted minute). `starters === pitch`; no-sheet ⇒ empty pitch. ≠11 ⇒ render the kept set as-is + log a `LineupAnomaly` (never pad/drop). Sub-pairing ↓/↑ "for [name]" badges. Three real shapes (England/Czechia/Croatia) pinned RED-first → 11 each. **NO engine/recompute/resolver/scoring/RLS/migration/Realtime/write change; ingest + `PlayerScoreSheet` untouched** (read-time only — stored `is_starter` left as-is). 5-lens review → 1 P1 (global `matchHasMinutes` live collapse) fixed + pinned; 4 lenses pass. Full DoD gate green (**2548** tests + web build). See PROJECT.md → 2026-06-22 + ARCHITECTURE → §25 (Kickoff-XI reconciliation). |

| **T18-form** | Standings `Last`/form chips column (W/L/D) | Low | S | display-only | `TODO` | T18 (D2) shipped WITHOUT the handoff's `Last` column: the BALLDONTLIE `group_standings` feed has **no** form field (form lives only on the separate `match_team_form` endpoint). If wanted, derive each team's last-N results in the PURE `buildGroupStandings` from that group's completed `fifa_match` home/away scores (no schema/feed/migration change) + render `.gd-gr-fc` chips (the CSS ports cleanly from handoff `.md-gr-fc`). |

**Reuse notes (so we don't rebuild):**
- T5/T6 — DONE (merged `7b95103`). Confirmed the box-score gate: `score_player_match` (+ `stat_player_match`) exist for EVERY match participant, not just rostered players (recompute's `playerAppearedInMatch` gate, no roster join), so a full 22+ box score with fantasy points is fully backed by stored data. New `loadGameDetail` reads it read-only; squads from `match_lineup_entry` (starters AND bench), cards/subs from `event_match`, owner overlay from `lineup_slot` (period-keyed) + `roster_player`. Per-player drill-in reuses `PlayerScoreSheet` verbatim.
- T11 — DONE (merged `9c9ceb5`+`b309003`+`938ab21`). The box-score path (`PlayerScoreSheet` → `/api/player-box` → `loadPlayerBox`) was already period-aware and needed NO change; only period SELECTION was missing. Added a shared pure `apps/web/src/period/selectablePeriods.ts` (started-set = `isPickLocked` on `matches[0]`, `periodIsDone` = last kickoff + window, `resolveDisplayedPeriodId` = server-side future-rejection) consumed by Lineup + vsfield. Lineup extends `PeriodTabs` (prior periods the manager played, read-only, default pinned to the live wave); vsfield threads `?period=` → `loadVsField(periodId)` (live sub suppressed for a prior). **`fix/t11-corrections` (merged `b309003`):** Lineup renders a prior from its own `lineup_slot` snapshot (`snapshotPlayers`, dropped-but-fielded players included); Waivers' selector was removed (over-applied) — the period concept is confined to matchday-labelled Batch results, and the FA pool/claims/cards are live/global (drill-down always the period-less `FaPlayerCardSheet`).
- T12 — DONE (`feat/season-tab`). The per-(manager, matchday) points were already on `StandingsView.cumulative[*].perPeriod[*].points` (aligned to `view.periods`); the Season tab is a pure re-projection `buildSeasonGrid(view)` (no new query) rendered by `SeasonGridPanel` — a managers × matchdays matrix, Total = the cumulative PF, unstarted columns blank.
- T14 — `loadVsField` read already carries benches (shipped with T1).
- T18 — DONE (merged `79162b6`, migration-class). `fifa_group`/`group_id` infra already in the schema but UNPOPULATED → group derived via the standings rows' `team_id`, not `fifa_match.group_id`. New self-contained `group_standing` table (PK `team_id`); new feed `groupStandings()` + `mapGroupStanding` + `ingestGroupStandings` (foreign-guard, no dirty/recompute) + `job:ingest-group-standings` CLI + daily render.yaml cron; additive group-scoped read → pure `buildGroupStandings` (null→hide; top-2 cutline + in-match highlight + tie-break footnote). Display-only / fantasy-safe.

**Housekeeping:** ~~the stale `feat/t13-bench-kit-chips` worktree lingers though T13 (`e585f1b`) is merged to main — teardown owed.~~ DONE — the worktree + branch were torn down (`git worktree list` shows only `main`); recorded in `80513af`.

---

## Thread sequence (one theme per thread)

1. ~~**T5/T6** — match/game detail + dashboard game-click.~~ ✅ DONE (`feat/game-detail`, merged `7b95103`).
2. ~~**T11** — prior-matchday stat sheets across the three surfaces.~~ ✅ DONE (`feat/prior-matchday-selector`, merged `9c9ceb5` + `b309003` + `938ab21`).
3. ~~**T12** — "Season" tab + season-by-matchday score grid (data already threaded; presentation only).~~ ✅ DONE (`feat/season-tab`).
4. ~~**T16** — re-skin `/games/[matchId]` to the match-detail handoff (visual + the one Ratings additive read).~~ ✅ DONE (`feat/game-detail-reskin`, merged `cbf4d36`). ~~**T17** — Statistics tab (team-stat ingest-retain + read).~~ ✅ DONE (`feat/team-match-stats`, merged `1662356` + `4840f1f` — **operator backfill owed** for pre-T17 completed matches). ~~**T16b** (Events timeline + scorers row).~~ ✅ DONE (`feat/game-detail-events`). ~~**T18** (Standings — `group_standings` ingest + real group table).~~ ✅ DONE (`feat/game-detail-standings`, merged `79162b6` — migration-class; operator go-live owed). Game-detail series COMPLETE.
5. ~~**T13** — sub kit-chips.~~ ✅ DONE (`feat/lineup-kit-jerseys`, merged `e585f1b`). ~~**T14** — Vs-the-Field bench points + player-card drill-in.~~ ✅ DONE (merged `812cda3`).
6. ~~**T16b** — Events timeline + scoreboard scorers row (pure-builder contract change).~~ ✅ DONE (`feat/game-detail-events`, merged to main ff).
7. ~~**Quiniela playoff bracket** — knockout bracket in the R32 pre-kickoff window + vertical/TBD Picks layout.~~ ✅ DONE (`a0de739` + `b41d1be`; merged + deployed live 2026-06-28).
8. ~~**T-3RD** — Quiniela 3rd-place match (Option B: IO-loader period-kind synthesis, `period_id` stays NULL).~~ ✅ DONE — **MERGED** `4aadc82`/`8d3c76c` (`t-3rd-pool-third-place`; migration-class, Sergio applies `migrate deploy` at go-live).
9. ~~**T2** — waiver watchlist (migration; Sergio merge authority).~~ ✅ DONE — **MERGED** `fb808bf` (`feat/waiver-watchlist`; migration + RLS applied by Sergio).
10. **T15** — mobile UX/UI audit + responsiveness overhaul (**LAST**).

T15 (the mobile audit) is deliberately deferred to the end: running it before the feature threads land would force re-skinning the same screens twice. Grouping otherwise minimizes merges (each merge = a brain-file re-upload). **SEC-P4** (the server-side teams-resolved gate on the pool-pick write path) is now **DONE** (implemented 2026-06-28, MERGED `184391a` 2026-06-29 — see the security table above).

### Commissioner console (Threads 1–5) — its own sequence

The commissioner surface ships as a 5-thread sequence so the privileged write paths land one contained slice at a time on top of a shared shell + audit ledger. Each write thread is migration-adjacent and/or touches live scoring → **Sergio holds the merge**.

1. **Thread 1 — read-only shell + `commish_audit` table + view-as inspector.** `VERIFY` (was IN PROGRESS) — `feat/commish-shell`, **merge HELD** (migration-class). Gated `/commish` page (`resolveCommishAccess`), the additive `commish_audit` ledger (commissioner-only SELECT RLS, server-only writes, not in Realtime), the `recordCommishAudit` server-write **seam (stubbed, no callers)**, the four task tabs as **inert placeholders**, the live Audit-log panel (empty), and the read-only view-as `ManagerView` (record+seed+FAAB+roster, same-league-only, NOT impersonation). Full gate green (2831 tests + build) + gated-PG RLS 9/9 (RED-verified) + `migrate deploy` proven; 6-lane adversarial review = zero findings. **VERIFY = Sergio applies `prisma migrate deploy` on `DIRECT_URL` + merges, then eyeballs `/commish` live (visual fidelity is manual per repo convention).** See PROJECT.md / DECISIONS.md / ARCHITECTURE.md §28 (2026-07-01).
2. **Thread 2 — Tier-1 writes** (penalty entry + rating override) — the first `recordCommishAudit` callers. `VERIFY` (was TODO) — `feat/commish-tier1-writes`, **source-only**, **merge HELD** (stacks on Thread 1; live-scoring writes = Sergio merges). `POST /api/commish/penalty` (UPSERT `manual_stat_player_match`, +2/−2, "Clear"=0/0) + `POST /api/commish/rating` (UPSERT `rating_player_match` source=`manual`, 0–10, "Clear override"=DELETE) — commissioner-gated, reason-required, `invalid_match_player` guard, each a write + ONE `commish_audit` row (`penalty_applied`/`rating_override`) in one transaction, then a synchronous `recomputePlayerMatch` re-score with a FROZEN-period `allowFrozen` override (surfaced). The Stat-corrections tab goes live (match→player pickers + two forms). Engine byte-untouched (empty diff on scoring+recompute). Full gate green (**2873 tests** + build) + gated-PG `commishAuditRls` 10/10 (PART-A confirm-1) + `commishStatWrite` 3/3. PART A also proved the two Thread-1 confirms (audit-read completeness; worker-gate identity). **VERIFY = Chat clears, Sergio merges Thread 1 (migration) then Thread 2 (deploy fires, no `migrate deploy`), then eyeballs `/commish` live.** See PROJECT.md / DECISIONS.md / ARCHITECTURE.md §28.2 (2026-07-01).
3. **Thread 2b — general stat-line editor** (edit any scored feed stat via `manual_stat_player_match.extra` + an adapter overlay). `VERIFY` (was DESIGN-LOCK PENDING → IMPLEMENTED) — `feat/commish-stat-editor`, **source-only**, **merge HELD** (stacks on Thread 1+2; live-scoring write path = Sergio merges). `POST /api/commish/stat` UPSERTs a sparse commissioner overlay into `manual_stat_player_match.extra.statOverrides`; the recompute adapter merges it at the `ScoreInput` level so any of the **23 scoring-relevant** raw feed stats can be corrected and re-score through the byte-untouched engine. **Overlay semantics:** field-level SPARSE — `n(overlay[f] ?? feed[f])` (manual-wins, nullish so an explicit 0 wins; feed passthrough where unset; wholesale-replace REJECTED). **Allowlist = 23** (`OVERRIDABLE_STAT_KEYS`, single source in `adapter.ts`); the 4 inert denominators (`dribblesAttempted`/`duelsLost`/`passesTotal`/`longBallsTotal`) are excluded — a structural allowlist-integrity test probes every `StatRow` field against the engine so "23" can never drift. **Storage:** namespaced `extra.statOverrides` sub-key via read-modify-write that PRESERVES `rolePlayed` (§3) + penalty columns; clear-one = blank a field, clear-all = empty map (drops the sub-key; empty extra → SQL NULL); both idempotent + `dirty:true`. **DECISIVE invariant (verified):** the overlay is carried on `ManualRow.statOverrides` and merged ONLY inside `buildScoreInput` — NEVER into `b.stat` — so `playerAppearedInMatch`/`statHasData` still gate a manual-only correction on a feed non-participant to `scored:false`/pending (Thread 2 semantics survive). One `commish_audit` row per save (`action_type=stat_correction`, `target_ref {matchId,playerId}`, `delta`=raw field-change string e.g. `goals feed→2 · assists 1→feed`, reason recorded); reuses Thread 2's `recordCommishAudit` + `createCommishRescore` (`allowFrozen`) + `restate_pending`/`no_match_participation` verbatim. UI: the `// TODO(2b)` marker in the Stat-corrections panel is replaced by a grouped field editor (current feed value + override input per field, role-gated dimming, clear-all). Engine byte-untouched (empty diff on `packages/scoring`; the sole `packages/recompute` edit is the additive `adapter.ts` overlay + its `prismaStore.ts` parse). Full DoD gate green (typecheck/lint/format/**2920 passed / 74 skipped** + `@app/web` build, `/api/commish/stat` in the route manifest) + gated-PG `commishStatWrite` **8/8** (Thread-2 5 + 2b 3: multi-field SET re-scores + idempotent, rolePlayed-survives-clear, gate-isolation on real Postgres). **VERIFY = Chat clears, Sergio merges (source-only → deploy fires, no `migrate deploy`), then eyeballs `/commish` Stat-corrections → Stat line editor live.** See design/COMMISH_2B_stat_editor.md + PROJECT.md / DECISIONS.md / ARCHITECTURE.md §28.3 (2026-07-01).
4. **Thread 3 — roster / lineup repair** (the "our-fault" moves the worker `commish` CLI already does, surfaced in the console). `TODO`.
5. **Thread 4 — period freeze / unfreeze** (`period.frozen_at`). `TODO`.
6. **Thread 5 — playoff field-lock + cut-schedule** (the deliberately-open commissioner gap; irreversible → type-to-confirm). `TODO`.

Reverse-action execution (undo a `reversible` audit row) rides on top once ≥1 write thread lands. Surfacing the commissioner nav entry on EVERY screen (not just Home + `/commish`) is a small follow-up seam (thread `isCommissioner` from each layout, or memoize `getSessionManager` + source it in the shell) — see DECISIONS §6.

---

## Open questions

- ~~**T5** — screenshot text was truncated; confirm the full intent when the thread opens.~~ RESOLVED 2026-06-21: "the user's own Games tab" = `/pool` (Quiniela, the match pick'em). Detail opens from the dashboard matchday rows AND a separate non-intrusive tap target on the Quiniela fixture cards (the teams-score area → `/games/[matchId]`); the HOME/DRAW/AWAY pick buttons are untouched.

---

## Web Push notifications — DONE in code; one operator step (PUSH-KEYS) open

Web Push is **COMPLETE in code and merged** — `f054ded` (41a: transport + preference model + Settings UI, inert sender) + `2ffb3a8` (41b: the three worker triggers, sender live). Shipped surface: `@app/notify` (pure core + `send` / `prisma` subpaths), the `push_subscription` / `notification_preference` / `notification_sent` tables with self-only RLS (`notification_sent`'s `(manager_id, kind, subject_id)` UNIQUE = server-side send-dedup), `/api/notifications/{subscribe,unsubscribe,preferences,test}`, `apps/web/public/sw.js` + `apps/web/src/notifications/pushClient.ts`, and the three idempotent triggers `draft_turn` / `player_not_starting` / `match_starting`. In-app live updates run on Supabase Realtime — a separate mechanism, unrelated to push. There is **no `PUSH-1..5` epic** and nothing here is unbuilt; the one open child is operator-only:

| ID | Item | Value | Effort | Risk | Status | Notes |
|----|------|-------|--------|------|--------|-------|
| PUSH-KEYS | Set the VAPID keys so device delivery goes live | Med | S | operator / dashboard (no code) | `TODO` (operator — Sergio) | The ONLY remaining step. `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` are `sync:false` and empty on BOTH `wc-fantasy-web` and `wc-fantasy-worker`. Generate one keypair (`npx web-push generate-vapid-keys`), set all three on both services in the Render dashboard (never commit them), then **rebuild web** (the public key is build-time inlined for the browser subscribe). Until then `sendPush` has no signing material and device delivery is dormant — the code is complete, just unkeyed. Real OS-permission + on-device delivery are live-only (not in-session verifiable). See PROJECT.md → 2026-07-01 + DECISIONS.md → 2026-07-01 (Web Push status) + ARCHITECTURE.md §13 (Deploy state). |

---

## Recently shipped (context — not pending)

Shipped this session (2026-07-01):
- **Commissioner console Thread 2 — penalty entry + rating override** (`feat/commish-tier1-writes`, **source-only**, **merge HELD** — stacks on Thread 1; live-scoring writes = Sergio merges) — the first `recordCommishAudit` callers. Two commissioner writes on the Stat-corrections tab, feeding the byte-untouched adapter/resolver (grep-verified empty diff on `packages/scoring` + `packages/recompute`): `POST /api/commish/penalty` UPSERTs `manual_stat_player_match` (penalty won/committed, +2/−2, "Clear"=0/0, absolute+idempotent, `dirty=true`, reason + `entered_by_user_id`) and `POST /api/commish/rating` UPSERTs `rating_player_match` source=`manual` (0–10, "Clear override"=DELETE→resolver falls back to balldontlie). Both: commissioner-gated (401/403 before any read, pure `handleStatCorrection` handlers), reason-required, `invalid_match_player` guard (player's team ∈ match), a write + ONE `commish_audit` row (`penalty_applied`/`rating_override`, `target_ref {matchId,playerId}`) in ONE transaction (shared `recordCommishAudit` seam, tx-injected insert), then a SYNCHRONOUS `recomputePlayerMatch` re-score with a FROZEN-period `allowFrozen` commissioner-override (so a correction past `period.frozen_at` still restates the leaderboard — surfaced via `frozenOverride` + audit `detail` + a panel banner), backstopped by `dirty=true` (the sweep's Phase-1 claim unions stat+rating+manual). The Stat-corrections tab goes live: match→player pickers (URL-driven, refresh-safe) + the two forms; `// TODO(2b)` for the general stat-line editor. Also proved the two Thread-1 confirms (PART A: `commish_audit` read-completeness — foreign+null actor both visible to the commissioner; worker-gate identity — `isCommissionerActor` ≡ `resolveCommissioner`). Naming: `action_type`=`penalty_applied` (the pre-seeded union value, not the prompt's prose `penalty_entry` — flagged). Full DoD gate green (typecheck/lint/format/**2873 passed / 72 skipped**/`@app/web` build, `/commish` `ƒ` + 2 API routes) + gated-PG on throwaway `postgres:16`: `commishAuditRls` 10/10 (incl. confirm-1) + `commishStatWrite` 3/3 (atomic write+audit; idempotent penalty clear; rating set/clear). **Source-only → HELD:** Chat clears, Sergio merges Thread 1 (migration) then Thread 2 (deploy fires, no `migrate deploy`); docs commit `[skip render]`, the feature commit is the push tip. See PROJECT.md / DECISIONS.md / ARCHITECTURE.md §28.2 (2026-07-01).
- **Commissioner console Thread 1 — gated `/commish` shell + `commish_audit` ledger + read-only view-as inspector** (`feat/commish-shell`, migration-class, **merge HELD** — Sergio applies the migration) — the first commissioner-only PAGE + the cross-cutting audit table every later write slice writes into. Ships read-only: the gated route (`resolveCommishAccess` = the shared `resolveCommissioner` predicate, flag OR `smrios07@gmail.com` email — the SAME gate the worker CLI now delegates to), the additive `commish_audit` table (commissioner-only SELECT RLS, **zero** write policies → server-owner-only, not in Realtime, `action_type` = app-union not pg-enum so new slices need no migration), the `recordCommishAudit` server-write **seam stubbed with NO callers** (table ships empty), the four task tabs as **inert placeholders**, the live Audit-log panel (empty-state), and the read-only view-as `ManagerView` (record+seed+FAAB+owned roster, **same-league-only**, explicitly NOT session impersonation). Nav: `commish` NavId + a gated `COMMISH_NAV_ITEM` (kept out of the always-render lists; shown only via the new optional `isCommissioner` prop on `AppShell`/`MoreSheet`, threaded from the hub + `/commish` layout). Byte-untouched: scoring/recompute/resolver/`enforce_lineup_lock`/FAAB engine/transition-advance/existing RLS/Realtime. Full DoD gate green (typecheck/lint/format/**2831 passed / 68 skipped**/`@app/web` build, `/commish` `ƒ`) + gated-PG RLS suite 9/9 (RED-verified) + `migrate deploy` proven on a throwaway PG; a 6-lane adversarial (opus) review of the diff returned **zero findings**. **Migration-class + new privileged surface → HELD:** Sergio applies `prisma migrate deploy` on `DIRECT_URL` from a Render shell, then merges (docs commit `[skip render]`; the feature commit is the push tip). See the Commissioner console (Threads 1–5) block above + PROJECT.md / DECISIONS.md / ARCHITECTURE.md §28 (2026-07-01).
- **FAAB-COPY-P1 — stale "FAAB reset to $100" copy stripped** (`fix/faab-copy-reset-strings`, code `4e98f1d`, MERGED `--ff-only` + pushed on a green gate; docs `[skip render]` follow-up) — the engine reset was removed long ago (`transitionStore.ts` `cca831c`) and DECISIONS corrected (`7b12f18`), but the UI still told managers their FAAB "resets to $100" at the knockouts — the exact opposite of the live rule. Rewrote every **user-facing** FAAB string to the carry-forward rule (ONE $100 for the entire tournament, group spend carries into the playoffs, NEVER reset): `/playoffs` `ReinforceModule` (meter caption → "of your $100 tournament budget", `po-reset-tag` pill → "Carries over · no reset", the "clean $100" sentence) + `PLAYOFF_EXPLAINER`; plus the grep-surfaced siblings — `/waivers` `wv-resetbanner`, dashboard `PrimaryBanner` playoff sub + `ReinforceModule` pill/note, and the marketing-landing bullet. Renamed the misnomer local `FAAB_RESET` → `FAAB_TOURNAMENT_BUDGET` (display denominator only; comment cites the DECISIONS rule — `LEAGUE_SEED_DEFAULTS.faabBudget` is flagged not-for-runtime-reads so the display path is NOT wired to it; no new hardcoded 100 elsewhere). Copy/labels only — **no engine/resolver/validator/schema/RLS/migration change** (the merged no-reset engine guard left intact). Test: jsdom render test for `ReinforceModule` + source-contract check for `PLAYOFF_EXPLAINER`. Full DoD gate green (typecheck/lint/format/**2809 passed / 59 skipped**/`@app/web` build). Contained + code-autonomous → merged on the green gate. LEFT (non-user-facing, out of scope, noted): `schema.prisma:173` + `packages/recompute/src/transition.ts:14` stale reset comments; CSS/gate identifiers `wv-resetbanner`/`db-rf-tag`/`isPlayoffPhase`. See PROJECT.md → 2026-07-01 + DECISIONS §guillotine-FAAB (stale-surface note resolved).

Shipped this session (2026-06-30):
- **`/playoffs` Chocoyo hero re-skin** (`feat/playoffs-chocoyo-reskin`, review-class, **merge HELD**) — the live guillotine hero re-skinned to the CHOCOYO theater (parrot-in-trophy hoisting a **pure-SVG machete** over the doomed), replacing the tiny `.po-parrot` glyph that had shipped INVISIBLE (source smoke green, render wrong). Copy binds to the view-model (`LOWEST {cutCount} GET THE CHOP` → `CHOP!`; "The Chocoyo doesn't miss."; substats; `round.eliminatedIds` struck with the cross-surface `--elim` row-elim treatment). The blade wind→drop→settle is a **CLIENT-side state machine** (clockless loader → transition latch on per-round `status` flipping live→past; one-shot, never on mount, `prefers-reduced-motion`-gated). Hero owns the champion endgame (retires `ChampionBanner`). `buildPlayoffsView`/`guillotine`/`playoffRound`/`loadPlayoffs` **byte-untouched** (git-verified); no migration/RLS/Realtime; `/playoffs` ƒ. **RENDER PROOF is the merge gate** (a source smoke can't prove a screen): `apps/web/scripts/verify-playoffs-hero.mjs` (Playwright + inlined real CSS + served `/brand/*.png`) — trophy image loaded + machete painted + blade drops + swing/sway wired, desktop+mobile, live+dropped+**champion** (12/12) + 6 screenshots eyeballed. **Adversarial multi-agent review (4 lanes, opus) caught 3 real issues, all FIXED:** (1) the trophy `po-squawk` wobble wasn't `prefers-reduced-motion`-gated (fired on the champion hero under reduce — moved into the `no-preference` block); (2) the mobile champion endgame rendered in elimination-red (missing `.mpo-hero.is-champion` → now win/accent); (3) champion state wasn't covered by the harness/smoke → champion coverage added (which now pins (1)+(2)). Full DoD gate green (typecheck/lint/format/**2802 passed / 59 skipped**/`@app/web` build). **PRE-EXISTING follow-up flagged (NOT fixed here — untouched ladder, out of scope):** the ladder rule `.po-col .po-col-future` (playoffs.css) also matches the future-column header `.po-col-tag.po-col-future` (a selector-scope bleed on `origin/main`); a 1-char `>` child-combinator fixes it in a separate contained pass. **Review-class + live screen mid-tournament → merge HELD; live-verify (hero paints + blade animates on a real cut) is Sergio's on Render.** See PROJECT.md/DECISIONS.md/ARCHITECTURE.md §21 (2026-06-30).
- **Team budgets rail** (`feat/waivers-budget-by-team`, MERGED `d06208f`) — `/waivers` now shows every manager's remaining FAAB budget-desc in a rail beside the rolling waiver order. Display-only: reads the existing `manager.faabBudget` column (the same one the FAAB resolver decrements on a won claim — never recomputed from bid history) onto the league-wide manager read the order rail already makes; no engine/route/migration/RLS change. `TeamBudgetsRail` reuses the global `.dtable`/`.row-me` table styling (no new CSS). Source-contract smoke (`teamBudgetsWiring.test.ts`) + fixture updates; full DoD gate green (2766 tests + web build). Contained + pre-authorized → merged `--ff-only` + pushed on the green gate. See PROJECT.md → 2026-06-30. **Follow-up:** `feat/waivers-budget-strike-eliminated` (MERGED, same day) strikes eliminated managers in place (CONTRACT-P3 `playoff_entry` read, mirrors §27's `loadVsField` read but keeps+strikes instead of hiding) — see PROJECT.md → 2026-06-30 (strike-eliminated entry). **Fix (`fix/eliminated-predicate-data-existence`, merge HELD):** the `status="eliminated"` read missed group-phase non-advancers (no `playoff_entry` row) — the rail now strikes off the shared data-existence set (`loadEliminatedManagerIds`), same as the vsfield hide.
- **Vs-the-Field match-card link + live-field elimination hide** (`feat/vsfield-match-click-hide-eliminated`, contained, merged `--ff-only` on a green gate) — each `/vsfield` fixture card is now a tap target to `/games/<matchId>` (mirrors the dashboard `MatchRow` pattern); separately, the live H2H field hides a manager once their `playoff_entry.status = "eliminated"` (CONTRACT-P3 data-existence, never `league.status`), composed loader-side via a new pure `filterEliminatedFromField` **after** `buildVsField` runs — the engine stays byte-untouched, `view.season` keeps every manager, and a prior/historical period via the matchday selector (T11) always shows the full field. No migration/RLS/Realtime/reveal-gate change. Full gate green (2770 tests + web build). See PROJECT.md → 2026-06-30 + ARCHITECTURE §27. **Follow-up (`fix/eliminated-predicate-data-existence`, merge HELD):** the `status="eliminated"` read was WRONG — group-phase non-advancers hold no `playoff_entry` row (`status` NULL), so they were never caught. Both surfaces now derive the set from the shared data-existence helper `loadEliminatedManagerIds` (`@app/faab/prisma`) = playoff-phase-active AND not-in-`alive`-set, phase-gated empty pre-transition. See PROJECT.md → 2026-06-30 (eliminated-predicate fix) + ARCHITECTURE §27 (Follow-up fix). **Champion edge — RESOLVED (follow-up commit on the same held branch):** the survivor set is now `status IN ('alive','champion')` (champion = DISPLAY-alive-equivalent, the terminal form of "survived"), so the champion's `/waivers` budget row is no longer struck AND the sub-60s Final-advance-before-tick `/vsfield` blank window is closed (the winner stays on the field). Display-only — the FAAB enforcement/roster-cap + `loadIsPlayoffParticipant` predicates stay strictly `status==='alive'` (separate axis, untouched). Pinned by the flipped champion-not-struck helper test + a Final-advance-before-tick composition test. See ARCHITECTURE §27.

Shipped this session (2026-06-28):
- **Quiniela playoff bracket gate** (code `a0de739`, docs `43b59c0` `[skip render]`) — `/pool` knockout bracket now gates on the **data-existence playoff phase** (`playoff_entry` existence via `loadPlayoffPhaseActive`, the atomic twin of `league.status='playoff'`), NOT `selectTournamentPhase`. Root cause: `selectTournamentPhase` returns `"group"` through the entire R32 pre-kickoff window (it flips to `"playoff"` only once a knockout match is in_progress/completed), so the bracket never rendered while all R32 matches were `scheduled` — the documented R32-pre-kickoff blind spot (CONTRACT-P2). Fix mirrors CONTRACT-P2/P3: `loadPool` derives `playoffActive` (reuses `loadPlayoffPhaseActive` from `@app/faab/prisma`) and threads it as the 4th arg of `selectPoolPicksView`; the web never reads `league.status`. **View-selection only** — per-fixture `period.kind` derivation, `readVisiblePicks` reveal gate, the group_md Completed archive, and scoring all byte-untouched; no schema/migration/RLS/Realtime change. Verified live (bracket renders, R32 pickable).
- **Quiniela knockout Picks — vertical layout + TBD/non-pickable + group hidden in playoff** (code `b41d1be`, docs `3008126` `[skip render]`). Three coupled changes on `/pool`: (1) the horizontal `.pl-bracket` column scroller replaced by a top-to-bottom stack of `.pl-round` sections R32→R16→QF→SF→Final (mobile-correct; single column ≤560px); (2) a knockout match is pickable (HOME/AWAY) ONLY when BOTH teams are resolved — undecided matches (feed placeholder name `/^Team \d+$/`; confirmed `country`/`abbreviation` are NULL for all teams, so name is the only signal) render "Teams to be decided" / TBD with NO pick buttons, and the raw `Team {id}` name never reaches the DOM; (3) when `playoffActive`, the Picks tab hides the group_md matchday lists AND the Completed archive AND the `unscheduled` section (**render-layer only** — the pure `selectPoolPicksView` stays phase-agnostic so the leaderboard drill-in + totals still see full group+knockout history; a P1 selector-strip was caught in adversarial review and pinned by a `managerPicks.test.ts` seam test). No engine/reveal-gate/scoring/schema change. Verified live. Surfaced **T-3RD** + **SEC-P4** (above).
- **FAAB playoff roster-cap UI parity** (`a552de0`) — fixes the live WC2026 R32 incident where every playoff manager hit a 409 on `POST /api/faab/free-agent`: `FreeAgentPanel`'s squad-full gate read a hardcoded `SQUAD_SIZE=15`, so a 9-man playoff squad (trimmed cap 9) read `9>=15=false` → the drop selector never rendered → the grant sent `dropId:null` → the engine correctly returned drop-required (409). The engine was right; the UI lied about "full" against the wrong cap. Fix (UI/loader parity, **engine BYTE-UNCHANGED** — `resolve.ts`/`validate.ts`/`faEligibility.ts`/`constants.ts` verified): thread the server-derived `WaiversView.rosterCap` (`rosterCapForPlayoffPhase` / `loadPlayoffPhaseActive` — the single cap source) into `FreeAgentPanel` AND `BidComposer` and key squad-full / drop-required / add-enable on it, never `SQUAD_SIZE`; `BidComposer.dropId` widened `string|null`. Group squads are always exactly full → group behaviour byte-identical. Surfaced the separate **FAAB-FA-P2** finding (above, RESOLVED not-a-bug). Full gate green (2678 tests + web build). See PROJECT.md (incident+fix) + DECISIONS.md (UI gates must read the engine cap).

Shipped this session (2026-06-22):
- **T-RECON** — Game-detail kickoff-XI reconciliation (`feat/lineup-kickoff-reconciliation`, merged `8385c93`): read-time cascade in pure `buildGameDetail` recomputes the true kickoff XI (candidates = `is_starter` ∪ `player_out`; drop non-starters with no minutes/events; terminal-match-only gate; ≠11 logs-not-drops). Fixes BALLDONTLIE's over-marked `is_starter` sheet. NO engine/scoring/RLS/migration/Realtime/write change. See ARCHITECTURE §25.
- **T-PITCH** — Game-detail formation pitch renders only the official starting XI (`02ebfe5`): `SquadSide.pitch` is now the `is_starter` subset only (was all players); bench/sub rows listed separately. Pure builder read-time filter; no engine/scoring/RLS/migration/Realtime change. See ARCHITECTURE §25 (precedes T-RECON).
- **T17** — Game-detail **Statistics** tab (team match-stats) (`feat/team-match-stats`, merged `1662356` + `4840f1f`). `mapTeamStat` now **retains the full remaining team payload in `stat_team_match.extra`** via `buildTeamStatExtra` + `TEAM_STAT_EXTRA_OMIT` (mirror of the player-stat `extra` path; 3 typed columns + upsert key BYTE-untouched; **NO migration** — `extra` pre-existed). `loadGameDetail` gains a read-only `statTeamMatch.findMany` (mirrors the T16 Ratings additive read — thin loader, pick in the pure builder; owner-bypass; NO RLS/migration/Realtime). Pure `buildGameDetail` emits a `statistics` view-model (home/away by `team_id`→home/away id match, sourced from the typed columns + parsed `extra`; accuracy/duels derived as own-rate %; render-safe nulls → "–"; null block ⇒ tab hidden). UI: **Statistics** tab between Lineups & Ratings (shown only when non-empty), home-vs-away comparison bars per `design/design_reference/match_detail/` on ds tokens. **Display-only / fantasy-safe** (grep-confirmed zero engine reads). Scoped out: the All / 1st-half split (no per-half team data). **⚠️ Operator go-live step:** run the new `job:ingest-team-stats` worker CLI once after deploy to backfill pre-T17 completed matches (empty `extra`; `ingestSettle` does not re-pull team stats) — display-only, no dirty-mark, idempotent. Full DoD gate green (2530 tests + web build). See PROJECT.md → 2026-06-22 (T17) + ARCHITECTURE.md → `stat_team_match`.
- **T16** — `/games/[matchId]` re-skin to the match-detail design handoff (`feat/game-detail-reskin`, merged `cbf4d36`): scoreboard + FANTASY exposure line + YOUR-XI stake strip + two tabs (**Lineups** formation pitch w/ flag-kit jersey tokens + enriched XI/Subs/Bench lists; **Ratings** podium + Fantasy-MVP + ranked board), `PlayerScoreSheet` drill-down reused unchanged. Visual-only EXCEPT the one approved **Ratings additive read** (read-only `ratingPlayerMatch.findMany` → nullable `PlayerLine.rating`, resolved in the pure `buildGameDetail` via shared `resolveRating`; NO migration/RLS/Realtime/engine/validator). Stats/Events/Standings deferred (data not loaded → **T16b**/**T17**/**T18**). 4-lens adversarial review → no P0–P2, 3 P3 polish fixes; full DoD gate green (2523 tests + web build). See PROJECT.md → 2026-06-21 (T16) + ARCHITECTURE.md → `stat_team_match`.
- **T12** — "Season" tab + season-by-matchday score grid (`feat/season-tab`, contained → Code-autonomous merge on green): a third tab on `/standings` rendering a managers × matchdays score matrix (rows = managers in season-seed order, columns = matchdays in canonical order, cell = that matchday's points, trailing **Total** = the cumulative PF). Pure `buildSeasonGrid(view)` (`@app/recompute`) re-projects the EXISTING `StandingsView` — NO new query, NO new scoring, NO schema/RLS/migration/Realtime change; each cell reads the cumulative row's `perPeriod` points, the started-set mirrors the Matchday tab (`live || view.matchday[id].length>0`), unstarted matchdays render blank / a live-but-unscored one is a real 0. `SeasonGridPanel` = horizontally-scrollable `<table>` + sticky Manager column (`.st-season-*`). +7 contract tests; adversarially reviewed (4-lens workflow → 7 P3 polish findings, all fixed pre-merge); full DoD gate green (2522 tests + web build). Also closes **SEC-P3** (commish decision: public upcoming lineups accepted — no vsfield reveal gate). See ARCHITECTURE.md → §23 + BACKLOG.md → T12.
- **T11** — Prior-matchday stat-sheet selector (`feat/prior-matchday-selector`, merged `9c9ceb5` + `b309003` + `938ab21`, reveal-adjacent/review-class): a matchday selector on Lineup, Vs the Field, and Waivers, routing every period read (current + prior) through the EXISTING period-aware models with a `periodId` param — single read path, no parallel/ungated read. Shared pure `apps/web/src/period/selectablePeriods.ts`: started-set = `isPickLocked` on the first fixture (future/unstarted NEVER selectable), `periodIsDone` = last kickoff + window, `resolveDisplayedPeriodId` rejects a future-period request server-side. Lineup `PeriodTabs` extended with read-only priors (anchored on `periodIsDone`, NOT `period.status`; default pinned to the live wave; the write path is unchanged and rejects edits to played slots via the lock-on-play latch + forfeit play-state rules); vsfield threads `?period=` and suppresses the live subscription for a prior; waivers selector swaps the drill-down to `PlayerScoreSheet` for a prior (FA pool/claims/batch stay live/global). `PlayerScoreSheet` reused verbatim; NO new table/RLS/migration/Realtime. Surfaced **SEC-P3** (the pre-existing inter-matchday-gap XI reveal — flagged, not introduced here). Gate green (2490 tests + web build). **Corrected by `fix/t11-corrections`** (merged `b309003`): (A) prior Lineup renders from the period's `lineup_slot` snapshot (`snapshotPlayers`) so a fielded-then-dropped player still appears; (B) the over-applied Waivers selector is removed — the period concept is confined to matchday-labelled Batch results (FA pool/claims/cards stay live/global). Surfaced **SEC-P3b** (lineup validator has no period-done gate) + **T16** (re-skin `/games/[matchId]`).
- **T5/T6** — Game detail (`feat/game-detail`, merged `7b95103`, review-class): new `/games/[matchId]` route opens a real-match box score (both squads incl. subs + cards) overlaid with the fantasy layer (each player's points + a started/benched/owned manager tag), reached from the dashboard matchday rows + a separate Quiniela tap target. Pure `buildGameDetail` + read-only owner-bypass `loadGameDetail` (NO engine re-run, NO RLS/migration, NO Realtime widening); nation via `fifa_team.name` (never `player.country`); per-player drill-in reuses `PlayerScoreSheet`. (Out of scope, as specified: two-yellow→red banding — rows shown as `classifyCard` returns them.)
- **T-CARD1** — single-sourced `classifyCard` (`refactor/classifycard-single-source`, merged `97e7be2`, review-class): the canonical classifier in `@app/recompute` (`packages/recompute/src/adapter.ts`) is now `export`ed and imported by the web Game-Detail builder (`apps/web/src/games/buildGameDetail.ts`), deleting the replicated copy + its `norm`/`CardKind` duplicates. Param widened `EventRow` → structural `CardEvent`; **engine scoring byte-identical** (function body unchanged, all `adapter.test.ts` engine tests pass unchanged, the "2nd yellow stays a 2nd yellow" test now exercises the shared fn). No server-only/Prisma leak: the main `@app/recompute` entry is IO-free (Prisma is behind the `/prisma` subpath), confirmed by a green `next build`. Gate green (2466 tests + web build).
- **T9** — tied matchups recorded as informational Draws, W-L-D (`8d0c9f4`); equal points → Draw + joint rank, seeding untouched. (Residual, FLAGGED: playoff "power record" surfaces show no D — knockout phase only, not a group-stage regression.)
- **T10** — dedicated `/standings` page: Matchday (default) + Cumulative tabs (`131eaba`).
- **T1** — Vs the Field shows the opponent's bench and yours at the bottom of H2H (`7929512`).
- **T3** — dashboard standings rows link to the manager's scores (`4923765` → `/vsfield?manager=<id>`).
- **T4** — quiniela leaderboard → manager picks reveal (`f9ae476`); stayed read-only (no `pool_pick` subscription).
- **T7** — forfeit-sub spurious error suppressed (`9935472`) + forfeit confirm now survives a period switch via `forfeitsByPeriod` (`0003f50`).
- **T8** — waivers shows each free agent's next opponent (`6776212`).
- Opponent-tag UUID-on-null-name fix → `UNNAMED_OPPONENT` (`0003f50`) — shared `resolveOpponentByPlayer` never surfaces a team UUID.

Earlier:
- VAR phantom-goals fix (`395443e`) — `isGoalEvent` keys on `incident_type === "goal"` exactly.
- Card classifier fix (`0ac4bb8`) — `classifyCard` keys on `incident_type` exactly.
- FAAB live-unowned free agency (`587b3ad`).
