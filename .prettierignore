node_modules
dist
.next
out
build
pnpm-lock.yaml
packages/db/prisma/migrations
**/.prisma

# Local Claude Code state + nested git worktrees (full repo checkouts): never format these.
.claude/
**/.claude/

# The blessed capture session (gitignored, disk-only) — never reformat or touch it.
captures/.auth/

# Source-of-truth brain files + prompts + audit reports + design handoff: hand-authored, do NOT reflow
# (tables, line breaks).
PROJECT.md
ARCHITECTURE.md
DECISIONS.md
BACKLOG.md
SCORING.md
BRAND.md
prompts/
audit/
design/
design_reference/
