# deps
node_modules/

# secrets / env
.env
.env.*
!.env.example

# provisioning input (real league name/emails + ranking — keep only the committed .example)
provision.config.json
provision.ranking.json

# rank:generate output (reviewed locally, then committed to default_rank via `provision rank`)
ranking-draft.csv
apps/worker/ranking-draft.csv

# build output
dist/
build/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# generated (Prisma client, etc.)
.prisma/
**/generated/

# turborepo / caches
.turbo/
coverage/

# logs
*.log

# session/handoff state (Claude Code)
.remember/
# Local Claude Code dir (settings.local.json, nested worktrees) — never stage.
# Keep the committed brain-file memory dir tracked via the negation.
.claude/*
!.claude/memory/
!.claude/skills/
!.claude/agents/
!.claude/hooks/
!.claude/settings.json

# OS / editor
.DS_Store
.vscode/
.idea/
