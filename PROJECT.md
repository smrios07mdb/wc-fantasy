# World Cup Fantasy — Project Brain

## Product summary
A standalone web app for a **private friends' league**: a FIFA World Cup fantasy game
modeled on Sleeper.com's polish and feature depth, redesigned for a **single-tournament**
format. Snake draft (unique player ownership per league), staggered per-match player
locking, head-to-head group stage, guillotine playoffs on a reduced roster, FAAB waivers.
Not client-facing — fun with friends. Guiding constraint: **"boring and reliable" over clever.**

## Build surfaces (who owns what)
- **Claude Code** — implementation
- **Claude Cowork** — progress tracking + manual/operational steps (draft kickoff, stat
  overrides/corrections, FAAB processing oversight)
- **Claude Chat** — ideation, planning, decisions (this surface)
- **Claude Design** — UX/UI

## Working protocol
- **One decision/theme per conversation thread.** Keeps context lean and decisions clean.
- These files are the source-of-truth "brain":
  - `PROJECT.md` (this) — overview, surfaces, protocol, status
  - `DECISIONS.md` — running decision log across all themes + agenda for open ones
  - `SCORING.md` — the locked, build-ready scoring model
  - `ARCHITECTURE.md` — the locked, build-ready stack / infra spec (split out the way SCORING.md was)
- **Start of each new thread:** attach these files (keep them in the Project's knowledge so
  every thread sees them).
- **End of each thread:** update the files with whatever was locked.
- **Verification discipline (applies to Code AND Chat):** state only what's directly verified — read the
  code, queried the DB, or ran a command. Anything not directly observable — Render env/process,
  dashboard config, request origin, deploy status — is labeled an **inference to confirm**, never
  asserted as fact.

## Locked requirements (from brief)
1. Snake draft; unique player ownership per league.
2. Per-player staggered locking — **a player locks the instant he plays ≥1 minute; until then he
   stays swappable** (refined from "locks at his match kickoff" — a benched 0-minute starter is
   *not* locked).
3. ~~Pre-set subs; a sub only fires if the starter played 0 minutes.~~ **Superseded (Theme B): no
   auto-subs.** Substitution is manual — swap any not-yet-played player. Lock-on-play delivers the
   original 0-minute protection.
4. Group stage: head-to-head — **all-play-all ("power record"); top N advance** (refined from 1v1
   and "top X" — N is the playoff field, set at the transition; likely 8 or 10, see Theme C).
5. Playoffs: guillotine (lowest scorer eliminated each round), reduced roster.
6. Waivers: FAAB blind bid. **(Full rules locked — see DECISIONS.md → Theme D: $100 budget,
   daily pre-dawn batch, free-agency fallthrough, reverse-seed rolling tiebreak, playoff
   reinforcement.)**
7. Standalone website.
8. **Live "vs the field" screen** — required by all-play-all; must show points-so-far alongside
   how much is still to come (see ARCHITECTURE.md → Real-time layer).

## Status
| Theme | Status |
|---|---|
| A. Scoring | ✅ LOCKED (see SCORING.md) — **amended:** 6 verification-forced line changes from the BALLDONTLIE field map (3 drops, 2 keep-via-manual, 1 remap); balance untouched; **§8 card-handling clarification** folded in (additive stacking; top minute band = ≥60 catch-all; no point values changed) |
| Data source | ✅ LOCKED — BALLDONTLIE WC API (stats/events) + Sofascore rating scrape + manual failsafe (**amended twice:** (1) locking is play-driven → needs live substitution events; (2) verification — **Sofascore scrape is the PRIMARY rating source** (calibration target), BALLDONTLIE's own `rating` = automatic fallback (provenance unknown); ingestion is **polling** (no webhooks at our tier); tier confirmed **GOAT $39.99/mo**) |
| B. Roster & lineups | ✅ LOCKED — 15-man squad (2/5/5/3), XI of 11, lock-on-play / no auto-subs, multiple-lineups defined, playoff cap ≈9 (7+2) |
| C. League & format | ✅ LOCKED — all-play-all regular season (seed by record, ties by total points), period = matchday wave; **snake draft, per-pick timer = config, autopick queue→best-available**; **playoff field flexible (likely 8 or 10), per-round cut ≈2 tapering to 1 over the 5 WC knockout rounds, fixed at the transition**; **guillotine elimination tie = lowest cumulative tournament total points** (commissioner backstop); **late-correction freeze ≈6h after last FT, commissioner-only after**. Only the recruiting-dependent manager/field number is deferred (config) |
| D. FAAB & Waivers | ✅ LOCKED — $100 (resets to a fresh $100 at the playoffs; single budget across knockout rounds); daily pre-dawn blind-bid batch + $0 free-agency fallthrough; per-player kickoff void+refund; rolling waiver-order tiebreak (seeded once by reverse draft order, carried forward into the playoffs — no re-seed; move-to-bottom only when the tiebreak is used); reinforcement = same FAAB cycle on the playoff field |
| E. World Cup attrition | ✅ RESOLVED — folded into the playoff transition + FAAB (reinforcement now fully specified in Theme D) |
| Architecture & stack | ✅ LOCKED (see ARCHITECTURE.md) — TypeScript/Next.js, Postgres via Supabase (+ Auth + Realtime), Render compute (web + workers + cron), polling ingestion, recompute scoring pipeline, lock-on-play live consumer; live "vs the field" screen + draft-room infra specified (draft *rules* now locked in Theme C) |

**Build progress (Claude Code):** Prompt 01 — repo scaffold + Postgres schema ✅ · Prompt 02 — pure
scoring engine + tests ✅ · Prompt 03 — recompute pipeline (DB→ScoreInput adapter + rating resolver +
dirty-flag sweeper) ✅ · Prompt 04 — standing / all-play-all + seeding + guillotine cut-selection ✅ ·
**Prompt 05 — ingestion + locking, COMPLETE ✅** [05a BALLDONTLIE polling + raw layer + lock-on-play
(`locked_at`); 05b isolated Sofascore scraper + rating-fallback comparison] (283 tests). Key build
facts: match→period resolved by an explicit, structural `fifa_match.period_id` (window-inference
retired; single-league assumption, documented); the player-match dirty invariant is hardened + hoisted
to `@app/db` (`STAT_DIRTY_UPDATE` / `markStatPlayerDirty`, imported by ingestion + scraper); the
scraper is a physically isolated `packages/scrape` + `apps/scraper` (no `@app/ingest` import) resolving
identity by **stored Sofascore IDs only** at scrape time, populated by a verified one-time `keyMatch`
pass (auto-writes unambiguous, flags the rest). **Go-live (post-launch — balldontlie carries the
rating until then):** apply the pending migrations (`period_id`, `kickoff_lock_fallback`, the
`recompute_scope` enum-retire, `sofascore_ids`); confirm the BALLDONTLIE base-path/auth header;
implement `loadSofaIndex` (the Sofascore index source — currently stubbed `[]`) + `pnpm add playwright`
& wire the launcher; smoke-test ingestion against the GOAT trial + a real Supabase. · **Prompt 06 —
server-authoritative draft controller (engine), COMPLETE ✅** (327 tests). New pure `packages/draft`
(`@app/draft`): snake order (`managerForPick`), roster legality (the 2/5/5/3 caps via `@app/shared`),
and autopick selection (queue → best-available, both filtered to available + position-legal) + a
store-backed controller (`startDraft` / `submitPick` / `tickDraft` + completion at 15×N) behind a thin
`DraftStore` port (Memory + Prisma impls). The ONE transaction (guarded pick + `roster_player`
ownership + pointer advance) lives in `commitPick`; it is idempotent (monotonic `current_pick_no`
latch) and backstopped by the `draft_pick` and `roster_player` active-ownership uniques. A worker tick
hook (`tickActiveDrafts`) drives timer-expiry autopicks; `pick_deadline_at` is the only timer source of
truth. **SEAM:** the best-available **default-ranking source is injected** — `getDefaultRanking`
returns `[]` with a `// TODO(confirm):` (no `player.default_rank` column exists), so autopick relies on
each manager's queue until a real ranking is wired. Realtime broadcast + the draft-room UI remain out
of scope (the deferred Design+Code deliverable). · **Prompt 07 — auth (Supabase magic-link + allowlist
+ session→manager binding + the draft-op authz gate), COMPLETE ✅** (406 tests, +79). New pure
`packages/auth` (`@app/auth`): the allowlist gate (`isEmailAllowed`, case-insensitive — a
`// TODO(confirm):`), session→manager resolution (`resolveSessionManager` — dual-key match on the
Supabase uid OR the linked `app_user` email, so it is robust to the unpinned link ceremony), and the
scope-gated act-as assertion (`canActAsManager` — `self` = strict self-match, `admin` = commissioner
override) + a typed `AuthError` family — all DB/Supabase/clock/env-free (`purity.test.ts` proves it,
comment-stripped). The edges live in `apps/web`: `@supabase/ssr` server + browser clients (SEPARATE
from Prisma; authz reads `getUser()`, never `getSession()`) + a session-refresh middleware;
`getSessionManager` / `requireManager` (the reusable gate every later route reuses); the minimal
magic-link sign-in / `/auth/callback` (code-exchange + edge allowlist enforcement: a non-allowlisted
email is `signOut` + denied, never admitted) / sign-out; and `POST /api/draft/pick`, whose
framework-agnostic `handleDraftPick` rejects **401 (no session) / 403 (not your manager) BEFORE**
calling the **unchanged** `submitPick`. Google OAuth is config-gated / seamed-optional. **SEAMS
(`// TODO(confirm):`):** the `user_id`↔`manager` provisioning ceremony (lookup is robust to either
`app_user.id == uid` or email-only linking; no self-serve manager-creation wizard) + email
case-sensitivity. Adversarial review (9 agents, 3 findings, **0 confirmed** — all the single-league
`leagueId`-filter non-issue). **Next: the user-facing critical path** (the WC opens June 11) — the
draft-room UI + Realtime (subscribe to the controller's state, call the now-gated `/api/draft/pick`),
the manager-provisioning ceremony, a minimal lineup-setting flow, and deploy. The draft is the binding
pre-kickoff deadline. All themes remain LOCKED; build is downstream of decisions. · **Prompt 08 —
draft-room screen + Supabase Realtime + worker draft-tick loop, COMPLETE ✅** the deferred Prompt-06 UI:
a gated draft-room screen (pure board / countdown / reducer / pickClient / realtime modules) subscribes
to the controller's state over Supabase Realtime and calls the gated `/api/draft/pick`; a dedicated
worker tick loop drives timer-expiry autopicks. · **Mock-draft smoke
test — the draft exercised end-to-end LIVE ✅** Against the deployed app + a real Supabase, the draft
controller + the draft-room UI + Supabase Realtime + the worker tick/autopick now run together: a draft
started, ran on the per-pick timer, and **completed**, recording **both** manual human picks **and**
timer autopicks. Verified facts: (1) **`default_rank` must be populated for autopick** — an empty ranking
makes `selectAutopick` return null and the draft **stalls**, so the go-live order is **`provision rank` →
`provision draft`**; (2) a clean **~30s pick window** was confirmed and the earlier "born-expired
`pick_deadline_at`" was **ruled out** (a non-simultaneous-read artifact, not a real defect); (3) the
worker **autopicks ~1s after each expiry** (autopicks landing = **verified**; *that they originate from
the Render worker = inference* — no process access, but no local worker was running); (4) picks **stream
across two browser sessions via Realtime** (operator-confirmed); (5) **manual pick recording = verified
working** (human picks recorded alongside autopicks). Engineering follow-ups in DECISIONS.md →
"Mock-draft session — open items": the lobby→active client flip on start, and an autopick empty-ranking
fallback (pre-launch hardening). · **Prompt 09 — draft closeout (lobby→active flip + autopick
totality), COMPLETE ✅** the two mock-draft follow-ups closed: the client lobby→active flip on the
`draft.status` change (partial-safe reducer + authoritative state fetch), and a total autopick
best-available fallback via `orderDraftPool` when the queue/ranking is empty (the empty-ranking stall is
gone). · **Draft surface — verified end-to-end on the live Render deploy ✅**
Both follow-ups above are now **CLOSED** (DECISIONS.md → "Mock-draft session — open items"): the
**lobby→active flip** and **live pick + autopick streaming** were confirmed across **two authed clients
with no reload**. Root fix = the browser Realtime client now authorizes with the **user JWT** (`setAuth`)
before subscribing — the anon socket received zero RLS-gated `postgres_changes`; autopick totality came
via the pure `orderDraftPool` (queue → `default_rank` NULLS LAST → `playerId`). · **Prompt 10 —
set-lineup flow (XI picker + lock-on-play binding), COMPLETE ✅** (605 tests, +~110). New pure
`packages/lineup` (`@app/lineup`): `validateLineup(squad, proposedXI, lockState, period, now)` →
`ok | a typed LineupError family` (`illegal-formation` / `incomplete-xi` / `not-your-player` /
`locked-player-moved` / `wrong-period`), with the Theme-B bounds sourced from `@app/shared`
(`FORMATION_BOUNDS` / `STARTING_XI_SIZE`, never re-derived) and **lock-respecting** on each locked slot's
frozen `is_starter` (a played player can't be moved into or out of the XI); behind a thin `LineupStore`
port (Memory + Prisma impls) + a store-backed `setLineup` controller — the decision core is
**purity-proven** (`purity.test.ts`, comment-stripped). The **player lock is consumed, not
reimplemented**: it is ingestion's `lineup_slot.locked_at` (Prompt 05a), read through the SSR loader. The
ONLY write path is the gated **`POST /api/lineup`**, mirroring `POST /api/draft/pick` (resolve session →
assert it owns the lineup, **401/403 before** the controller, scope `self`), with a **triple-defended
server-authoritative lock re-check**: `validateLineup` on the authoritative lock state → `saveLineup`
writes only `WHERE locked_at IS NULL` and **aborts the whole commit if a locked row would change** → the
`enforce_lineup_lock` trigger is the DB backstop (the Prompt-01 latch is **not regressed**).
`wrong-period` gates on `period.closes_at`, and **future windows are legal by construction** (the
validator's `PeriodWindow` carries no `opens_at`) — so pre-setting upcoming windows is allowed, and an
unplayed, never-locked starter is still backstopped by period close. The **set-lineup screen**
(`app/lineup/`, mapped to `design_reference/setlineup/*` + `shell/*`) renders the formation pitch +
bench, swaps start↔bench with live legality feedback (the SAME `validateLineup`), freezes locked players,
and pre-sets per-period windows; lineup data reaches the browser only via the SSR Prisma loader (**no
browser-direct `lineup_slot` read / no Realtime on the table — Theme F needs no new authenticated SELECT
policy**). Opus adversarial review (6 lenses → verify): **0 blockers, 2 majors fixed pre-merge**
(cross-position swaps so the formation can actually change + the live illegal-formation feedback is
reachable; the FAAB-drop orphan-slot reconcile seam TODO'd). **Launch gates (pre-June-11):** (a) wire the
per-player **kickoff indicator** (`kickoffByPlayer` `TODO(confirm)` — `fifa_match.kickoff_at` via
`player.teamId`); (b) the **runtime lock-freeze + late-edit rejection** is deferred (no drafted roster /
live `locked_at` yet) → fold into the GOAT-trial ingestion smoke test. **Carried TODOs:** FAAB add/drop
reconcile (`saveLineup` orphan-slot DELETE + forbid dropping a locked-slot player), a dedicated
`FormationPicker` / `reshape()`, and a commissioner `admin` lineup scope. **Next: the live "vs the field"
screen** (ARCHITECTURE → Real-time layer) — points-so-far alongside how much is still to come,
all-play-all — then the commissioner/admin surface + the group→playoff transition. All themes remain
LOCKED; build is downstream of decisions. · **Prompt 13 — vsfield RLS migration self-test fix (launch-gating deploy blocker), COMPLETE ✅ (deploy reported green).** Migration `20260606170000_rls_realtime_vsfield`'s embedded self-test set the JWT claim to a **non-uuid label** (`'rls_selftest_user_in'` / `'…_out'`); on Supabase the real `auth.uid()` casts `request.jwt.claim.sub` to `uuid`, so the helper `vsfield_caller_shares_league_with_manager` **`22P02`'d on every clean apply** — `prisma migrate deploy` failed at #10 and blocked the Render deploy on WC-eve. **Fix = valid-uuid literals** (`00000000-…-0001/0002`, canonical-lowercase so `manager.user_id (text) = auth.uid()::text` round-trips), mirroring the Theme F precedent (`20260605170000_enable_rls_public_tables`). Helper / SELECT policies / `supabase_realtime` adds **byte-for-byte unchanged**; cleanup (sentinel-rollback) already present; member-can / non-member-cannot assertion intact (#10 `finished=true` ⇒ both branches green). Proven by a Supabase-faithful Docker repro (uuid-returning `auth.uid()`) that reproduced the Render error byte-for-byte, then applied all 10 clean with zero residue. **Production gate cleared (operator-confirmed):** failed record resolved on the live Supabase DB (`migrate resolve --rolled-back`), branch `fix/vsfield-migration-selftest` merged to `main` (FF, 2 files: the 8/2 migration hunk + RUNBOOK), Render deploy ran #10 clean → green (first green deploy carrying the vsfield RLS migration). Recovery documented in **RUNBOOK d.1**. ⚠️ *Confirm-to-close (not yet observed): worker process up + sign-in→vs-the-field smoke — build/migrate is green; worker + smoke pending operator confirm.* · **Prompt 15 — pre-prod security follow-ups (function hardening), COMPLETE ✅** Migration `20260606180000` on `main` (clean merge `c8f404d`) + Render deploy **GREEN** (embedded self-test passed on live → `EXECUTE` revoke + both `search_path` pins **catalog-verified**): `REVOKE EXECUTE` on `mirror_auth_user_to_app_user()` from `PUBLIC`/`anon`/`authenticated` (`SECURITY DEFINER` kept — trigger still fires) + pin `enforce_lineup_lock` `search_path=''` (`INVOKER`, body unchanged), guarded by a fail-safe catalog-only self-test. Net-new vs prior `main` = only the lineup-lock pin (the mirror revoke + `''` pin were already in `20260606010000`). Behavioral confirms fold into go-live: first allowlisted signup → `app_user` row (trigger fires); first kickoff → locked-player swap rejected (no live locks pre-tournament). Leaked-password protection (HaveIBeenPwned): pending operator dashboard toggle. (Prompt-13 worker+vsfield smoke + Prompt-14 close-out remain separate.) · **Prompt 16 — landing hub (auth-aware root replacing the scaffold), COMPLETE ✅** (690 tests, +5). The Prompt-01 scaffold `/` (`apps/web/app/page.tsx`) is replaced by an **auth-aware server component** that reuses `getSessionManager()` **unchanged** (the Prompt-07 `getUser()`-backed edge) and defers the render to a pure **`selectLandingView(outcome)`** (`apps/web/src/landing/`, IO-free, `@app/auth` type-only import, exhaustive `switch` with a `never` guard so a 5th outcome kind is a compile error). Four states → four renders: **no-session → signin** (single "Sign in" CTA → `/sign-in`, closing the front-door gap); **ok → hub** ("Signed in as {name}" + nav cards to `/draft` / `/lineup` / `/vsfield` + a **POST** sign-out `<form action="/auth/sign-out">`, closing the post-login stranding gap — members land here via the callback's default `/` redirect, `safeNextPath(null)` left unchanged); **no-manager → unlinked** — a **distinct** "not linked to a manager yet — contact the commissioner" state, **NOT** routed to `/auth/denied` (the Prompt-07 provisioning seam; the `handlePick` / `handleVsField` 403-collapse is deliberately split out here); **not-allowlisted → denied** (defensive — the callback already signs these out). The build flips `/` from static `○` to **dynamic `ƒ`** (per-request SSR) — a cached static root would have leaked one user's hub to all, so the dynamic render is a correctness requirement, verified. `pnpm -w typecheck && lint && format:check && test` (690) + `pnpm --filter web build` all exit 0; no out-of-scope churn (no redirect/callback change, no feature-page / API / middleware / auth-core edits, no admin surface, no security-closeout churn, no new routes/env). **Numbering:** this is **Prompt 16**, deconflicted from **Prompt 15 = the pre-prod security follow-ups closeout** already on main (unrelated). **Next follow-up (flagged, not done):** the shared cross-nav strip on the three authenticated layouts so members move between draft / lineup / vsfield without bouncing through `/`. ⚠️ *Merge: implemented + verified on `feat/landing-hub` @ `dd0aed3`; merge to main pending base-reconcile onto `9accb1f` + working-tree cleanup (see DECISIONS → "Landing hub & route-map ground truth").* · **Prompt 17 — shared cross-nav strip on the authenticated screens, COMPLETE ✅** (697 tests, +7). `/draft`, `/lineup`, `/vsfield` had no cross-navigation — reaching one from another meant bouncing through `/`. Closed by a single presentational shared component, `apps/web/app/shell/CrossNav.tsx` (client component for `usePathname()`), mounted once per route-scoped layout — **no shared layout existed** (each screen has its own `app/<route>/layout.tsx`; the route-group refactor to a literal shared layout was rejected as out-of-scope churn). Markup lives in one place — no triplication. Links the other two feature screens + home (`/`); the current screen is indicated via a pure **`selectActiveNav(pathname)`** (`apps/web/src/shell/`, mirroring `selectLandingView` — exact home match; feature routes match exact / trailing-slash / nested sub-path but not a prefix-sibling like `/draftroom`; → `.tab.is-active` + `aria-current="page"`). Sign-out reuses the Prompt-16 hub form verbatim (`<form action="/auth/sign-out" method="post">` → existing 303 handler; native POST, JS-free even inside the client component). Zero new CSS (reuses ds.css `.tabs`/`.tab`/`.btn-ghost`). No new auth / routes / env / middleware; screens stay `getSessionManager()`-gated as before; build keeps `/draft` `/lineup` `/vsfield` dynamic `ƒ`. 7 unit tests (`crossNav.test.ts` — config shape + path-match edges). Full gate + `pnpm --filter web build` exit 0; no out-of-scope churn (hub `/` untouched; no feature-page / API / admin / auth-core / security-closeout edits). **Last navigation follow-up — nothing further flagged from this theme.** Merged to main @ `14a0811` (off current main `52e1416`, ff, no force-push). · **Prompt 19 — landing visual re-skin (apply the XI design system to `/`), COMPLETE ✅** (708 tests, +7). The Prompt-16 four-state `/` is re-skinned to the delivered design as a **purely visual** change: `selectLandingView()`, the four-outcome branch, the session read, and the route set are **byte-for-byte unchanged** (proven by `diff` of `Home()` vs HEAD + a source-contract smoke). The handoff package (`design_handoff_landing`) was **NOT actually vendored** as the prompt claimed (its `ds.css` is byte-identical to the already-vendored `apps/web/app/{draft,lineup,vsfield}/ds.css`; only `landing.css` + `XI Landing.html` were new) — the operator supplied it from Downloads; vendored to `apps/web/app/_landing/`. **The delivered design is a full 10-section public marketing+login page, not a four-state hub re-skin** (the prompt's premise was off): resolved with the operator → the logged-out **`signin`** state renders the full XI marketing landing (`_landing/MarketingLanding.tsx`, ported 1:1 from `XI Landing.html`: nav · hero · brand-trophy band · 4 mechanics rows · scoring 5-col grid (categories only, **no point values** — honesty) · draft-board + flag-pitch showcase · how-it-plays · 6 explore cards · CTA · footer); **`hub`/`unlinked`/`denied`** get branded `ds.css` panels (no design pixel-truth exists for them). The prototype's **two inline self-serve email forms become a "Sign in" CTA → `/sign-in`** (honoring "no self-serve join flow"); the prototype Tweaks panel + `<script>` are dropped (scroll-reveal left static — `.lp-reveal` is fully visible at rest, so the page stays a server component, no client boundary). **CSS is per-route, NOT global** (operator decision — global `ds.css` would double-reset with the existing Tailwind `globals.css` + double-load on the feature routes): `ds.css` + `landing.css` imported in `page.tsx`, scoped under a `.lp` wrapper; root `layout.tsx` untouched (no conflict with Prompt 18's metadata edit). **Two sanctioned `landing.css` adaptations:** `body.lp` → `.lp` with **`overflow-x: clip`** (not `hidden` — would make `.lp` a scroll container and break the sticky nav) + **`color: var(--text-primary)`** (the root `<body>`'s Tailwind `text-slate-900` *class* beats `ds.css`'s `body{color}` *element* rule on `/`, so color-less headings would inherit dark-on-dark). `<Brand/>` (`BrandMark`) sits in every state's header; sign-out stays a **POST** form; **unlinked stays distinct from denied** (only `denied` links `/auth/denied`). Amends **ARCHITECTURE §1 "Tailwind"** (plain CSS consumed as-delivered; see DECISIONS). Opus adversarial review (5 lenses → verify): **0 blockers / 0 majors / 0 confirmed-serious**; folded in the a11y polish it surfaced (decorative trophy/icons `aria-hidden` so the brand link reads "XI WC Fantasy League", not "XI XI …"). Gates: `pnpm -w typecheck && lint && format:check && test` (708) + `pnpm --filter web build` all exit 0; **`/` stays `ƒ`**; `/sign-in` + `/auth/denied` unchanged (`○`). **Deferred follow-up (flagged):** skin `/sign-in` (+ `/auth/denied`) — the login design (`Join.html`) was **not in this handoff bundle**, so not improvised. ⚠️ *Branch `feat/landing-design` (`aa295bd`) **stacks on Prompt 18** (`feat/brand-pwa`) — it imports `Brand.tsx` (landed in 18, not yet on `main`); **merge order: 18 → main, then 19.** Held for clearance; not pushed; no force-push.* · **Prompt 20 — App Shell: `ds.css` global foundation + shell chrome, COMPLETE ✅** (723 tests, +15). The first/foundational screen of the design sprint; every later skin sits on it. **(1) `ds.css` promoted to the single GLOBAL stylesheet:** imported once in root `layout.tsx` **after** `globals.css` (wins cascade ties); canonical byte-identical copy at `app/styles/ds.css` (md5 `66d4bbbc`); the 4 per-route copies stay (double-load harmless; de-dup is post-sprint). Tailwind / `globals.css` / Preflight **coexist** (not retired). **(2) Collision A (body surface) fixed:** root `<body>` dropped Tailwind `bg-slate-50 text-slate-900` (CLASS selectors that out-specified `ds.css`'s `body{…}` ELEMENT rule, pinning the app light); the ds **dark** surface now applies (dark = the `:root` default, no `data-theme` needed); `min-h-screen antialiased` kept. **(3) App Shell chrome:** `app/shell/AppShell.tsx` (+ `shell.css`) — the design's **GlobalTopbar** ported from `App Shell.html` + `shell/*`, a **pure server component** (no client JS: explicit `active` prop instead of CrossNav's `usePathname()`; plain `<a>` nav; POST `<form>` sign-out). `<Brand/>` per **BRAND.md §5** = `BrandBadge` trophy chip + "XI" + `WC Fantasy League` secondary line (brain file overrides the prototype's league-name-as-primary). Nav lists **only the 4 built routes**; the prototype's 14-screen IA + "More"/bell/avatar/mobile-tab-bar/sheets/commissioner (all targeting **unbuilt** screens) are flagged `TODO(confirm)` seams. **(4) Mounting** = per-feature-layout (no route group — the dual-state `/` can't be wrapped unconditionally): `/draft` `/lineup` `/vsfield` layouts swap `<CrossNav/>`→`<AppShell active=…>`; the **hub state only** of `page.tsx` is wrapped (`active="home"`, dropping its `.lp-nav`). `selectLandingView()` + `Home()`'s branch + the `signin`/`unlinked`/`denied` states are **byte-for-byte** (they keep landing chrome). **CrossNav absorbed** — `app/shell/CrossNav.tsx` deleted; the pure `crossNav.ts` helper + test stay/reused. Amends **ARCHITECTURE §1** + **supersedes Prompt 19's "per-route, not global."** **Opus adversarial review (5 lenses → verify): 3 of 10 confirmed, all folded** — (a) a naive height port clipped the fixed-height `/draft` surface (~3300px, browser-reproduced) → restored the design's `.sh-app{height:100%}` + `.sh-content{flex:1;min-height:0;overflow-y:auto}` dual model, **browser-verified** for both fixed-height (/draft) and natural-scroll (/lineup,/vsfield,hub); (b) global dark body made `/sign-in`+`/auth/denied` gray status/helper text illegible (~2:1) → minimal `text-slate-400/300` legibility repair (still Tailwind; full skin stays Prompt 21); (c) duplicate `banner` landmark on `/lineup` → `aria-label="Global"` on the shell `<header>`. Gates: `pnpm -w typecheck && lint && format:check && test` (723) + `pnpm --filter @app/web build` all exit 0; **`/` stays `ƒ`**; `/sign-in`+`/auth/denied` stay `○`; no out-of-scope churn (no Tailwind retirement, no feature-body re-skin, no `selectLandingView`/auth/route/env/middleware/`.gap-*`/admin edits). **Next: Prompt 21 — sign-in / Join skin** (`/sign-in` + `/auth/denied` from `Join.html` + `auth/*`, migrating them off Tailwind). ⚠️ *Branch `feat/app-shell`, off post-19 `main`; held for clearance; not pushed; no force-push.* · **Prompt 21 — sign-in / Join skin (`/sign-in` + `/auth/denied` off Tailwind onto ds), COMPLETE ✅** (735 tests, +12). Second screen of the design sprint: both auth routes migrate off bare Tailwind onto the ds **split-shell** per the repo's canonical `design/design_reference/Join.html` + `auth/*` + BRAND.md §5. **(1) Route-scoped CSS:** new `apps/web/app/_auth/auth.css` ports only the `au-*` rules from `Join.html`'s `<style>`; tokens + `.btn`/`.spinner` come from the **global** ds.css (the `shell.css` model — **no per-route ds.css copy, ds.css not forked**). **(2) Shared chrome:** new `apps/web/app/_auth/AuthChrome.tsx` (parallels `_landing/chrome.tsx`) = `AuthScreen` (split shell + brand panel + form-column slot + foot) + the design's icons + `PrivateTag`; **no `"use client"`** so the client `/sign-in` and server `/auth/denied` both render it. **(3) Brand per BRAND.md §5:** reused Prompt-18 `LockupStacked` (trophy · "XI" · tagline, **not redrawn**) + the `{league} · {season}` row (`WC Fantasy League` — same placeholder the shell uses, no new source/no `SHELL_LEAGUE_NAME` — + `World Cup 2026`) + `PrivateTag` + static value-props; the data-bound `RosterAvatars`/`InviteBanner` are **dropped, not faked** (no manager/invite fetch on these routes); the design's pre-rebrand `AuthLogo` "W" chip is replaced by the XI lockup. **Gold (BRAND.md §1) only in the trophy PNG**; "XI" = `--text-primary`, CTA/links/dots cobalt `--accent`, `PrivateTag` slate `--locked` — no gold leak. **(4) `/sign-in` presentation-only:** Supabase `signInWithOtp` (→ `/auth/callback`), env-gated `signInWithOAuth(google)`+`GOOGLE_ENABLED`, and `createClient` **byte-for-byte preserved**; the one-string `message` became two presentation flags (`error`+`sent`) so the request form + "check your email" confirmation are the two designed views; **no next/safeNextPath introduced** (stays in `auth/callback`). `/auth/denied` keeps its **dual-cause** copy (allowlist OR expired) + back-to-sign-in. **Early-warning seam clean:** the root `denied`/`unlinked` `selectLandingView` states are **independent `page.tsx` components** (already ds via Prompt 19), NOT shared with the `/auth/denied` route → **no `page.tsx` edit**. **Shell-free boundary holds** (no `AppShell`, no auth `layout.tsx`); both stay `○`, `/` stays `ƒ`. Responsive split→stacked collapse (`@media ≤1100px`) ported verbatim; **browser-verified** (desktop split + mobile stack + the denied au-center view). **Opus adversarial review (4 lenses → per-finding verify): 1 of 1 confirmed, folded** — the reused `LockupStacked` double-announced "XI" (trophy `alt="XI"` + visible wordmark) → wrapped the lockup in `aria-hidden` (decorative splash; the `_landing/chrome.tsx` precedent), accessible brand name now from the visible league name + each view's `<h1>` (`Brand.tsx` untouched — shared primitive); **browser-DOM-verified** (aria-hidden, still centered, single `<h1>`). New pure-Node source-contract smoke `apps/web/src/auth/authPages.test.ts` (+12, mirrors `landingPage.test.ts`). Gates: `pnpm -w typecheck && lint && format:check && test` (735) + `pnpm --filter @app/web build` all exit 0; route shapes preserved (`○ /sign-in`, `○ /auth/denied`, `ƒ /`); no out-of-scope churn. **Both `TODO(confirm)`s resolved (Chat clearance, folded as CONFIRMED):** (a) the auth foot stays **prose, never a link** — no commissioner-contact route exists app-wide; do not invent a `mailto`/`href="#"` (a real one is a later prompt); (b) `/auth/denied`'s broadened `<h1>` "We can't sign you in" **stays** (recorded **design-deviation** — the route is dual-cause allowlist-OR-expired, so the design's allowlist-only `DeniedView` copy would mislead; cause-specific copy = a future logic prompt). The `message → error+sent` split is endorsed as presentation. **Next: Prompt 22 — first feature-body re-skin (Draft, from the `design_reference/` Draft screen).** ✅ **CLEARED (Chat); `feat/auth-skin` (off post-20 `main`) pushed to origin (no force-push); Sergio runs the FF merge to `main` + owns the Render deploy + live-verify (Code does NOT merge/deploy).** · **Prompt 22 — Draft body re-skin (`/draft` to the `design_reference/` Draft screen), COMPLETE ✅ + MERGED** (745 tests, +10). The **first feature body** of the design sprint; the ds-aligned skinned set is now **landing (19) → shell (20) → auth (21) → Draft (22)**. A 6-agent adversarial fidelity audit confirmed the `/draft` body was **already byte-faithful** to `design_reference/Draft Room.html` on the global ds.css (the Prompt 08/09 port), so the re-skin reduced to **brand de-dup**: after Prompt 20 wrapped `/draft` in `AppShell` (which owns the trophy · "XI" · league lockup), the body's `.dr-top` doubled it with its own `.dr-logo` "W" + "Snake Draft" wordmark → dropped; `.dr-top` → a de-branded `.dr-status` strip (phase line + connection pill + presence), **no brand mark in the body** (BRAND.md §1/§5). The `.dr-*` layout stays route-scoped in `apps/web/app/draft/draft.css` (the `shell.css`/`_auth/auth.css` convention) on the **global** ds.css — **ds.css not forked**; the per-route `draft/ds.css` copy is left for the post-sprint de-dup; Tailwind/`globals.css`/Preflight retained globally (Lineup + Vs-the-Field still consume them). draft.css stays fully tokenized (zero hex) → cobalt/red/slate only, **no gold leak**. **Presentation only** — `packages/draft`, the gated `POST /api/draft/pick`, `handlePick`, the worker tick, the Realtime subscription, the **server-synced countdown** (`pick_deadline_at`, never the client clock), and the deadline logic are **untouched**; the Prompt-20 fixed-height/internal-scroll model holds; `/draft` stays `ƒ`. **Deferred-not-built (flagged):** the lobby start/sim controls + "Draft starts in" countdown and the Summary projected/grade/value-pick + `proj` sublines are **data-shape seams** (absent from `DraftRoomState` — need loader/payload fields, an early-warning STOP); the autopick queue **editor** (add/drag-reorder/remove + the "Idle → autopick" hint; the queue stays a read-only display) and the board **auto-scroll-to-current-pick** (ref+effect) are out-of-scope follow-ups. New pure-Node source-contract smoke `apps/web/src/draft/draftRoom.test.ts` (+10, mirrors `landingPage`/`appShell`; draft suite **59 green**); a `selectDraftView` extraction was rejected as disproportionate logic churn (the 49 existing pure draft tests already pin the behaviors). Gates: `pnpm -w typecheck && lint && format:check && test` (745) + `pnpm --filter @app/web build` all exit 0; independent adversarial diff review = clean PASS. **Live visual fidelity + the one `// TODO(confirm)` (the de-branded strip label) are the operator gate** on the Render deploy (`/draft` needs auth+DB+Realtime, not in-session renderable), per merge→verify-live. **Merged to `main` at `d9800e7`** (`b135cac` P20 → `7e5d801` P21 → `d9800e7` P22); this thread-close docs record was added separately (it didn't ride the cherry-picked draft commit). **Next: Prompt 23 — Lineup re-skin (from the `design_reference/` Set Lineup screen).** · **Prompt 23 — Lineup body re-skin (`/lineup` to the `design_reference/` Set Lineup screen), COMPLETE ✅ + MERGED** (757 tests, +12). The **second feature body** of the design sprint. An adversarial fidelity audit confirmed `/lineup` was **already a faithful `.sl-*` port** of `design_reference/Set Lineup.html` on the global ds.css (no raw Tailwind, gold-free) and — **unlike `/draft` and `/vsfield`** — it **never carried a body brand chip** (`sl-topbar` is a plain de-branded screen-title strip: `<h1>Set lineup` + manager name + PeriodTabs), so the Prompt-22-style brand de-dup was **N/A** (nothing to remove). The re-skin reduced to **two presentation-only reconciliations**: **(1) pitch markings** — ported the design's full field markings (penalty boxes + goal areas + halfway line + centre circle, the `.sl-pl-*` set) + mowed-stripe turf, replacing the old inner-frame + single `::before` circle, so the primary surface reads as a pitch; **(2) legend over-claim** — the binary `lineup_slot.locked_at` (+ `LockTag`) is just "Locked", not "Locked · played", so relabelled to "Movable — still swappable" / "Locked — has played, frozen". Files: `SetLineupClient.tsx` + `components.tsx` + `lineup.css` + new pure-Node smoke `setLineup.test.ts` (+12, mirrors `draftRoom`). **Presentation only** — `packages/lineup` (`validateLineup`), the gated `POST /api/lineup`, the server-side lock re-check, `lineup_slot` persistence, `requireManager` **untouched**; `/lineup` stays `ƒ`. **Flagged-not-built (data-shape / live-feed seams):** `FormationPicker`, flag-kit jerseys + `<Flag>` chips, live `ScorePill`/`PlayerScoreSheet`, the 3-state lock·playing split, next-lock countdown, per-player kickoff chip, connection banners, autosave. **Merged to `main` at `43490aa`** (off post-22 `71c96d8`). **Next: Prompt 24 — Vs-the-Field re-skin (ran in parallel, same `71c96d8` base).** · **Prompt 24 — Vs-the-Field body re-skin (`/vsfield` to the `design_reference/` Vs the Field screen), COMPLETE ✅ + MERGED** (770 tests, +13). The **third feature body and FINAL screen of the design sprint** (ran in parallel with Prompt 23 — a sibling branch off the same `71c96d8`, touching disjoint files). A fidelity audit confirmed `/vsfield` was **already a faithful `.vf-*` port** of `design_reference/Vs the Field.html` on the global ds.css (gold-free), so — like Prompt 22 — the re-skin reduced to **two presentation-only changes**: **(1) brand de-dup** — drop the body `.vf-logo` "W" badge now that the App Shell topbar (Prompt 20) owns the trophy · "XI" · league lockup; `.vf-top` → a de-branded `.vf-status` stack (screen label "The Field" + the live period line) + tabs + Updated/ConnPill (mirrors Prompt 22's `.dr-status`). **`.vf-logo` was vsfield-LOCAL** — BRAND §5 calls it "the shared chip," but in the implemented codebase it was defined+used ONLY in `apps/web/app/vsfield/*` (exactly like `.dr-logo` was route-scoped to draft), so removing the def + usage is **not** a shared-file edit. **(2) pitch markings** — the original port dropped the design's faint centre circle (`.vf-pitch::after`) + halfway line (`.vf-pitch-{v,h}::before`); ported verbatim (white `rgba .05`, no gold) — the same gap/fix as Prompt 23. **Avatars stay initials** (BRAND §6 — no parrot). The **natural-scroll height model is preserved** (`.vf-app` keeps `min-height:100%`; `shell.css` classes `/vsfield` as a natural-scroll screen where `.sh-content` owns the single scrollbar — NOT forced to the design's `height:100%`, which would fight the shell). The **live indicator is red `#FF4D4D`** — design-canonical (broadcast/match-LIVE pulse, not green; see DECISIONS → "Lineup + Vs-the-Field skins"). Files: `VsFieldClient.tsx` + `vsfield.css` + new pure-Node smoke `vsFieldSkin.test.ts` (+13). **Presentation only** — `packages/vsfield` (`buildVsField`), the Prompt-04 pairwise helper, `loadVsField`, the authed read `GET /api/vsfield` (**401, no 403** — league-scoped), the JWT-authed Realtime subscription + polling fallback, the RLS/publication migration, `requireManager` **untouched**; `/vsfield` stays `ƒ`. **Flagged-not-built (out-of-scope seams):** the per-player H2H XI list (needs player names + `score_player_match`), the scoring FeedPanel (event-level rows), the clickable match-card filter. **Merged to `main`** (`fee577f` via merge `37fd7c6`). 🎉 **DESIGN SPRINT COMPLETE** — the five screens (landing · auth · Draft · Lineup · Vs-the-Field) re-skinned to `design_reference/`, all sitting on the Prompt-20 App Shell foundation: **landing (19) → App Shell (20) → auth (21) → Draft (22) → Lineup (23) → Vs-the-Field (24)** (`main` = 68 files / 770 tests). The only remaining sprint work is the **operator gate**: live authenticated visual fidelity on the Render deploy (the feature bodies need auth+DB+Realtime, not in-session renderable), folded into the go-live / GOAT-trial verify-live. · **Post-provision fix — `resolveSessionManager` hardening (`1b28e3b`), COMPLETE ✅** `resolveSessionManager` now **throws `AmbiguousManagerLinkError`** when the Supabase uid resolves to manager A but the linked `app_user` email resolves to manager B (was **silently returning the uid match** — a split link could bind a session to the wrong manager). · **Post-provision fix — match-schedule mapper shape (`6ca5829`), COMPLETE ✅** `mapMatchRow` + `derivePeriodLabel` reconciled with the GOAT API's **nested** matches response (`home_team`/`away_team`/`stage`/`group` are objects, not flat strings). Verified against live **2026 schedule data: 104 fixtures, 103 carrying a `period_id`** (3 group MDs + 5 KO rounds); the 1 excluded fixture is the **3rd-place match** (no 9th period bucket). · **Post-provision fix — scoring mappers shape (`f3db93a`, branch `fix/scoring-mapper-shape`), COMPLETE ✅** (782 tests) `mapEvent`/`mapShot`/`mapTeamStat`/`mapStatLine` reconciled with the documented GOAT shapes. **Critical:** `mapEvent` read flat `player_id` but the API **nests** `player`/`assist_player`/`player_in`/`player_out` (the **same root cause** as the match mapper); also `mapShot` `minute`→`time_minute`, `mapTeamStat` `possession`→`possession_pct`, and `referee` typed as an **object, not a string**. New **`FeedShapeMismatchError`** gives **per-item isolation** — one bad row is logged + skipped, never halts the batch. Shapes verified against live **2022** match data (match 1030, Qatar v Ecuador). · **Prompt 25 — FAAB batch engine (pure resolver + bid route + cron body), COMPLETE ✅ + MERGED** (901 tests). The first of two Waivers prompts (the **engine**; the `/waivers` UI is Prompt 26) — implements the LOCKED DECISIONS §D blind-FAAB → free-agency algorithm with **no UI**. **New `packages/faab` (`@app/faab`)** mirrors the draft/lineup pure-core + injected-IO shape: a **PURE, IO-free batch resolver** (`resolveFaabBatch`) encoding all 8 §D steps — void+refund a bid whose add target already kicked off, highest-bid-first player-by-player, tie → rolling waiver order, **move-to-bottom ONLY when the tiebreak is actually used** (winning on amount alone never moves you), own-bids resolve highest-first-skip (emergent from the global-highest loop against the manager's updated budget/roster/ownership), $0 bids legal, every won claim is add/drop + a budget debit, contiguous 1..N waiver order — proven clock/Prisma/Supabase-free by a **70-case purity matrix** (7 modules × 10 forbidden patterns). **Gated `POST/PATCH/DELETE /api/faab/bid`** (submit / edit / cancel) via the framework-agnostic `handleBid` on the `/api/draft/pick` template: `requireManager` → `assertCanActAsManager({scope:"self"})` → **401 (no session) / 403 (not your manager) BEFORE any DB write** (verified structurally with an exploding-Proxy store), then submission validation — `amount ≥ 0`, **over-commit rejection** (`amount ≤ budget − other pending bids`), add unowned league-wide + not-kicked-off, drop owned + `≠ add` + required-when-full + **drop-lock validation** (can't drop a player locked by play in an active matchday), 2/5/5/3 roster legality. **Real cron body** in `apps/worker/src/jobs/faabBatch.ts` (replaces the live-but-inert placeholder): opens a `FaabBatch`, reads pending bids + managers + roster + add-target kickoffs, calls the pure resolver, then writes ALL outcomes in **ONE atomic `$transaction`** (the no-double-spend / valid-drop guard) — assign winners (add/drop + debit), refund voids, mark losers, apply the move-to-bottom reorder as the schema-mandated **TWO-PHASE reassignment** (negate → final 1..N, preserving the contiguous `manager_waiver_order_uq` invariant), stamp each bid's `batchId` + terminal status (`won`/`lost`/`voided_refunded`), write the batch `complete`. **Idempotent re-run** (a `done` batch is not re-processed: guarded `updateMany WHERE status='pending'` + no-pending-bids → no batch row). **Lineup-slot release behind the `@app/lineup` domain boundary:** a won drop's UNLOCKED `lineup_slot` rows are released (so a dropped starter stops scoring) via exported `@app/lineup/prisma` `releaseDroppedPlayerSlots(tx,…)` inside the same batch transaction — **faab never touches `lineup_slot`**; the lock-on-play check (`findLockedSlotPlayerIds`) lives there too. **Cross-player equal-amount ordering = waiver order** (by the leading bidder's `waiverOrderPosition`, not player id / insertion). `addTargetKickoffAt` resolves through **`fifa_match.kickoff_at` via `player.team_id`** — the SAME schedule field lock-on-play derives `lineup_slot.locked_at` from (no second clock). **Deferred / flagged:** the `faab_bid.priority` column migration is deferred to **Prompt 26** (pending-claim reorder NOT exposed; the engine resolves own bids by **amount**, with `priority` reserved as the intra-manager equal-amount tiebreak for a future migration); a free-agency **FA `Acquire` route does NOT exist** (flagged sibling concern, not built here); the playoff FAAB reset + waiver carry-forward belong to the separate group→playoff transition prompt (the engine only READS current budget/order). `faab_bid` RLS already present (Prompt-05 invariants migration; no gap). Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (901) + `pnpm --filter @app/web build` + worker build all exit 0; no churn to scoring / recompute / standings / draft / ingestion / auth signatures. **Merged to `main` @ `2145700`.** **Next: Prompt 26 — Waivers UI (`/waivers` to the `design_reference/` Waivers screen, on this engine).** · **Prompt 33 — draft-pool emoji flags (`<Flag>` + `src/draft/flag.ts`), COMPLETE ✅** (1074 tests). Pure `src/draft/flag.ts` resolver (ISO alpha-3 / FIFA alpha-3 / English name → ISO alpha-2 → Unicode regional-indicator pair); sole `<Flag>` React render surface; `countryFlag` wired to pool rows + queue + nation-filter chips. **Prompt 34 — draft nation binding, COMPLETE ✅.** DraftPlayer.country now sourced from FifaTeam.name via player.team join in `loadDraftRoom.ts` (was the never-written `player.country` scalar). · **Prompt 35 — flag-resolver gap closure + collapsible nation filter, COMPLETE ✅** (1085 tests). All 48 distinct FifaTeam.name values in the 2026 dataset now resolve to flags (zero placeholders): home nations (England/Scotland/Wales/Northern Ireland → 🇬🇧 interim), Curaçao, DR Congo, Côte d'Ivoire, Bosnia, Cabo Verde (Intl Node-25 name mismatch), and 11 FIFA formal names. Collapsible nation chip grid added (default collapsed, active-selection header with ✕ clear). CLIENT-ONLY (`flag.ts` / `flag.test.ts` / `components.tsx` / `draft.css`). · **Prompt 36 — home-nation inline SVG flags, COMPLETE ✅** (1089 tests). England = St George's Cross (#CE1124 on white), Scotland = Saltire (#005EB8, white diagonals) — inline SVG, universal, no dep, no asset files, no emoji tag sequences. All four home-nation → GB fallbacks removed from `src/draft/flag.ts` (England/Scotland/Wales/N. Ireland → null); `Flag.tsx` intercepts via exported `isHomeNation()` predicate before the emoji path. 46 other nations unchanged (emoji mechanism). CLIENT-ONLY (`flag.ts` / `flag.test.ts` / `flagWiring.test.ts` / `Flag.tsx`). · **Prompt 37 — dashboard home foundation + pre-draft + draft phases, COMPLETE ✅** (1122 tests, +33). The Prompt-16/19 nav-card hub is replaced by a phase-aware `<Dashboard>` component on `/`. Phase derived from `draft.status` via the new pure `selectDashboardPhase` selector (`src/dashboard/`; `never` exhaustiveness guard; 5 unit tests). `loadDashboard` (`app/_dashboard/loadDashboard.ts`) reuses `loadDraftRoom` DIRECTLY — no second draft source, no re-derivation. `DashboardData { phase, draft }` feeds `PrimaryBanner` (phase-coloured `--phc` CSS var) + the `modulesFor`/`renderModule` module router in `Dashboard.tsx`. Four modules shipped: pre-draft → `LeagueInfoModule` + `ReadinessModule`; draft → `DraftFormingModule` + `RecentPicksModule`. All data from `DraftRoomState`; no additional reads. Route-scoped `dashboard.css` on global `ds.css`; zero hex; no gold; `--phc` for phase colour (the `shell.css` convention). `page.tsx` `Hub` now `async`; calls `loadDashboard(managerId)`; renders `<Dashboard data={data} />`; all non-hub branches + `selectLandingView` + force-dynamic are **byte-for-byte unchanged**. **Two open data-shape seams (STOP-flagged in source):** (a) `draft.scheduled_start_at` does not exist — pre-draft countdown deferred, renders "waiting for commissioner"; (b) per-manager `is_ready` does not exist server-side — readiness grid deferred, all dots off. Both are candidate future migrations. **Post-draft phase = minimal interim** (group-phase module set is Prompt 38; playoff/complete deferred). 30-assertion source-contract smoke `dashboard.test.ts`. `landingPage.test.ts` hub assertion updated to check `<Dashboard>` delegation (FEATURES array gone). `feat/dashboard-home-foundation` @ `5b6f5ec`, **merged to `main`**. · **Prompt 38 — dashboard group phase, COMPLETE ✅** (1175 tests, +53). Extended the P37 foundation with the full group phase: pure `selectTournamentPhase(matches[])` (IO-free; `round null = group, non-null = knockout`; no migration) composed with `selectDashboardPhase` via a private `"post-draft"` intermediate; `DashboardPhase` widened to the six-member union (`pre-draft | draft | pre-kickoff | group | playoff | complete`); `loadDashboard` queries `fifa_match` (SELECT only) and calls `loadVsField` READ-ONLY for the group phase. Group modules shipped: `RecordModule`, `StandingsModule`, `MatchdayModule` (all sourced from `VsFieldView`). `PrimaryBanner` extended for all six phases. Playoff and complete remain honest interims (`// STOP(P38)`) pending Guillotine and recap. Gates: 1175 tests ✓, typecheck ✓, lint ✓, format:check ✓, build ✓. **Merged to `main`.** **On horizon: Guillotine bracket unblocks dashboard playoff/complete.** · **Prompt 39 — self-service display-name rename (`/settings` profile + `POST /api/manager/display-name`), COMPLETE ✅** (1213 tests, +38). The App Shell's Settings `TODO(confirm)` seam is now a real minimal route. **New files:** pure helper `apps/web/src/manager/displayName.ts` (`validateDisplayName`: trim + collapse whitespace, reject empty / >40 chars, no charset restriction), framework-agnostic handler `src/manager/handleDisplayNameRename.ts` (mirrors `handleDraftPick` — `resolveManager` injected, `NameTakenError` domain error for the unique-violation, 401/403/400/409/200 status map), thin route `app/api/manager/display-name/route.ts` (maps Prisma `P2002` → `NameTakenError`), migration `20260610120000_manager_display_name_unique` (raw-SQL: `CREATE UNIQUE INDEX "manager_league_id_lower_display_name_key" ON "manager" ("league_id", lower("display_name"))` — case-insensitive per-league uniqueness), `/settings` route (`app/settings/layout.tsx` + `page.tsx` + `settings.css` + `src/settings/SettingsClient.tsx` client island), `src/settings/settings.test.ts` source-contract smoke. **Modified:** `src/shell/crossNav.ts` (`"settings"` added to `NavId` + `NAV_ITEMS`), `AppShell.tsx` (gear glyph), `schema.prisma` (`/// Prompt 39:` comment on `displayName`), `crossNav.test.ts` (updated nav-item list + `selectActiveNav("/settings")`). **Writes ONLY `manager.display_name`** — `app_user.display_name` (vestigial) untouched. **Realtime propagation caveat:** a rename appears to other clients on next server render/navigation; no publication change (not time-sensitive, avoids the RLS-publication trap). **Scoring/engine/draft/lineup/vsfield/worker untouched.** No existing-duplicate failure found on current empty dev DB; operator must verify before production migration (`SELECT league_id, lower(display_name), array_agg(id) FROM manager GROUP BY 1,2 HAVING count(*)>1`). **Live-deploy-only inferences to confirm:** Settings link in the AppShell nav, rename toast on 200, cross-client name propagation on next page load. `feat/profile-rename` off current `main`; **merge held for Chat clearance; no force-push.** · **Prompt 40 — pick'em pool data layer + engine (backend only; NO UI), COMPLETE ✅** (1283 tests, +70). The first of two pool prompts (the **engine + data**; the `/pool` UI + knockout bracket + leaderboard screen + nav + Realtime client are **Prompt 41**). A per-match **1X2 pick'em** (option A, NOT an advancement bracket) scored **separately** from the player engine: +1 per correct pick. **New `packages/pool` (`@app/pool`) is strictly pure** (mirrors `standing.ts` — no IO/clock/DB, grep-proven by `purity.test.ts`): `derivePoolResult` · `scorePick` · `weightForPeriod` (flat 1; escalating-knockout weight is a seam keyed on `period.label`) · `buildPoolLeaderboard` (`{played, correct, points}`, `points desc → managerId asc`) · `isPickLocked` · `validatePickSubmission`. **The load-bearing correction (Chat-caught during grounding):** the prompt's `round == null → group` discriminator is **FALSE** — `mapMatchRow` writes `round` = the matchday number for group (NON-NULL) / raw round name for knockout, and `groupId`/`stageId` are never populated; the literal contract would misclassify all 72 group matches as knockout (DRAW rejected, nothing scores). Fixed by discriminating on **`period.kind`** (`group_md`/`knockout_round`/null), resolved by the IO loader from `fifa_match.periodId → period.kind` — the same signal locking/recompute trust; `periodKind==null → null` (honest unscored, no round-regex guess); the stale `fifa_match.round` schema comment was corrected. **Schema:** `pool_pick` (`PoolPrediction {HOME DRAW AWAY}`; `UNIQUE(manager_id, match_id)`; indexes `(league_id,match_id)`+`match_id`) + migration `20260610130000_pool_pick`. **RLS mirrors `faab_bid`** (auth.uid→manager→league): **league-scoped SELECT** (whole-field reads — `standing_select_league_member` shape, no SECURITY DEFINER since the row carries `league_id`), **own-`manager_id` INSERT/UPDATE**, no DELETE; `pool_pick` added to the `supabase_realtime` publication now (the Realtime-RLS trap — P41's subscription would otherwise be silently empty). **Anti-copying lives in the read QUERY, not RLS** (no clock in RLS): own picks always + others' only for `kickoffAt <= now`. **Theme-F embedded self-test** (cross-league isolation + own-row write) **verified against a UUID-returning `auth.uid()` shim** on a throwaway Postgres — proven the uuid cast is live (non-uuid `sub` → `22P02`) and the predicate discriminates a leaky policy, closing both false-green traps (text-shim cast + owner-bypass); migration applies clean with **zero drift**. **Server path** (`apps/web/src/pool/` + `app/api/pool/pick`, the `/api/faab/bid` template): store port (Memory+Prisma doubles) + framework-agnostic `handleSubmitPick`/`handleReadPicks` → 401/403 (scope "self") BEFORE any write → pure validate → upsert; server `now` authoritative. **Nothing else touched** — no change to scoring / recompute / standings / draft / lineup / vsfield / faab / ingestion / auth signatures. Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1283, +70: pool engine 33 + purity 27 + handler 10) + `apps/web` typecheck all exit 0. **Branch `feat/pool-engine` off `main`; brain files ride with the feature (no separate docs commit); merge HELD for Chat clearance; no force-push.** **Next: Prompt 41 — Pool UI** (pick surface + knockout bracket layout + leaderboard + nav + the Realtime subscription: `realtime.setAuth(token)` before subscribe, gated on `INITIAL_SESSION`, re-subscribe on `TOKEN_REFRESHED`). · **Prompt 41a — Web Push transport + subscription + preferences + Settings UI (INERT sender), COMPLETE ✅** (1415 tests, +132). The first of two notification prompts (the **transport + preference model + UI**; the three triggers are **41b**). Notifications = **Web Push over the PWA** — **NO new vendor** (Render + Supabase only), **NO Realtime** (push is server→device, sidestepping the `postgres_changes`/RLS-publication path entirely — stated explicitly in the migration + DECISIONS). Landed **inert**: `dispatchToManager` is built + unit-tested but **called by NOTHING** (same as the faab/period-close crons landed plumbing-first). **New `packages/notify` (`@app/notify`)** mirrors the faab/draft pure-core + injected-IO-port shape (purity-proven, 77 assertions): pure `buildPushPayload`/`buildTestPayload` + `validatePreferenceInput` + the delivery policy `dispatchToManager` + the `NotifyStore` port + `MemoryNotifyStore` double on the `.` surface; the IO adapters are separate subpaths — `@app/notify/send` (`sendPush`, the web-push/VAPID wrapper) + `@app/notify/prisma` (`createPrismaNotifyStore`). **`dispatchToManager` order = preference gate → subscriptions (early-out WITHOUT claiming if none, so a later subscribe still notifies) → `claimLedger` (idempotency) → fan-out + prune 404/410**; claim-BEFORE-send = **at-most-once** (a transient send failure does NOT re-fire next poll — the chosen trade vs. spamming a lock screen). **Migration `20260610140000_notifications`** (3 additive tables): `push_subscription` (endpoint UNIQUE; SELF-ONLY SELECT/INSERT/DELETE RLS), `notification_preference` (PK manager_id, three bools DEFAULT true, lazily upserted-with-defaults on first read; SELF-ONLY SELECT/INSERT/UPDATE), `notification_sent` (**UNIQUE(manager_id, kind, subject_id)** = the load-bearing idempotency ledger for 41b's pollers; **RLS ENABLED + ZERO policies = default-deny** = "service-role write only, no client read"; history-read policy left a `TODO(confirm)` seam). RLS mirrors the `pool_pick`/`faab_bid` `auth.uid()→manager` idiom but **self-only** (not league-scoped). **Theme-F embedded self-test** (self-only isolation on the two policied tables + structural default-deny on `notification_sent`) **verified against a UUID-returning `auth.uid()` shim on a throwaway Docker Postgres** (catches the `(auth.uid())::text` cast trap), **negative-control-proven** (the harness raises on a forced violation), applies clean with **zero drift**. **Service worker** = plain `apps/web/public/sw.js` (served at `/sw.js`; `push` → `showNotification` from the JSON payload + `notificationclick` → focus/open; **no fetch interception / no caching** — boring + reliable; manifest + Prompt-18 metadata untouched). **Gated routes** (the `handleDisplayNameRename` template — framework-agnostic handler + thin route + injected `resolveManager`, 401/403/400/200): `POST /api/notifications/{subscribe,unsubscribe,preferences,test}`; `/test` sends `buildTestPayload()` via the store's send **bypassing the ledger** (transport probe). **Browser enrolment** (`apps/web/src/notifications/pushClient.ts`) is the injectable `enableBrowserPush(env)` (permission → register `/sw.js` → subscribe with the VAPID key as bytes → POST `/subscribe`) so the permission→subscribe path is **unit-tested in Node with fakes** (no DOM) + `urlBase64ToUint8Array`. **Settings UI** fills the AppShell **Notifications `TODO(confirm)` seam** (touched only that seam): a `NotificationsClient` island (three toggles seeded from `getPreference` + "Enable browser notifications" + "Send test"), ds.css toggles (cobalt accent, no gold). **VAPID env** on **web + worker** (`NEXT_PUBLIC_VAPID_PUBLIC_KEY` build-time+server, `VAPID_PRIVATE_KEY` + `VAPID_SUBJECT` server, all `sync:false`); operator key-gen = `npx web-push generate-vapid-keys` (NOT committed); render.yaml comments note the worker copy is **inert until 41b**. **Out of scope (seams intact):** the three triggers (41b), any worker hot-path edit, email, notification-history UI, any publication/RLS-broadcast edit. Tests (+30): notify pure (preferences 6 + payload 5) + dispatch 8 + purity 77-assert + handlers 14 + pushClient 6 + notifications source-contract smoke + renderBlueprint VAPID + settings smoke updated. Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1415) + `pnpm --filter @app/web build` all exit 0; no out-of-scope churn (scoring/recompute/draft/lineup/vsfield/ingestion/Realtime untouched). **Live-deploy-only inferences to confirm:** the real OS permission prompt, actual delivery to a device, installed-PWA push on iOS (installed-PWA only) / Android (in-browser or PWA). **Branch `feat/notify-transport` off `main`; merge HELD for Chat clearance; no force-push.** **Next: Prompt 41b — wire the three triggers (draft-turn / player-not-starting / match-starting) to `dispatchToManager` in the worker.** · **Prompt 41b — the three triggers wired (sender now live), COMPLETE ✅ (merge HELD).** Each trigger = a **PURE selector** (`apps/worker/src/notify/selectors.ts`, IO-free, 14 tests) + an **IO dispatch** through the unchanged `dispatchToManager` (`triggers.ts`, 8 tests); idempotency is the `notification_sent(manager,kind,subject_id)` UNIQUE ledger **inside dispatch** (selectors stay stateless, re-emit each tick, ledger collapses to one send). **`draft_turn` `${draftId}:${pickNo}`** — piggybacks the existing 2s draft ticker via a new injected **`afterTick`** hook on `startDraftTicker` (so `draft.ts` imports no `@app/notify`); reuses `DraftStore.loadDraft` + the **shared** store the ticker drives → sees the post-autopick pointer; catches turns advanced by human pick **or** autopick without touching the controller; one per turn, **no on-deck**, drops out on complete. **`player_not_starting` `${matchId}:${playerId}`** — hooks the pre-match `match_lineups` pull: `ingestLineups` now **returns the official-XI starter BDL ids** it already fetched (no 2nd feed call), the worker compares vs the match-period's fantasy **is_starter** slots and alerts owners of any starter **not in the XI + still unlocked** (`locked_at` null) — the high-value, time-sensitive one (swap window before the reserve's kickoff). **`match_starting` `${matchId}`** — each 60s scheduler tick, **owners-only** (whole roster, either team) for fixtures kicking off within **`NOTIFY_MATCH_LEAD_MIN`** (default 15; `[now,now+lead]`, past-kickoff excluded). New **worker-local trigger-read port** (`notify/store.ts` + `prismaStore.ts` + `memoryStore.ts`) holds the two reads `@app/ingest` shouldn't (keeps lock derivation IO-free); roster ownership **global** (one-league assumption, noted in adapter). Each dispatch **isolated** (try/catch) so a notify failure never starves the autopick/ingestion loops. **Hook points:** `index.ts` `afterTick`→`dispatchDraftTurns`; `scheduler.ts` pre_match→`dispatchPlayersNotStarting`, tick-tail→`dispatchMatchStarting`; `wiring.ts` constructs the notify+trigger+shared-draft stores. **Added `@app/notify` to the worker `package.json`** (the 41a "worker already depends" note was VAPID-env-only, not a dep). **Grep-clean:** `packages/draft` core + `packages/ingest/lock.ts` IO-free; Supabase/clock/web-push confined to the worker IO layer. **SCORING.md untouched** (no scoring change). Test delta **+24** (1415→1439): selectors 14, triggers 8, ingest `ingestLineups`-returns-XI 1, draft `afterTick` net +1. Gates: `pnpm typecheck` (all packages) + `lint` + `format:check` + `test` (1439) + `pnpm --filter @app/web build` all exit 0. **Live-only inference:** actual device delivery (no push service in CI) — confirm on deploy. **Branch `feat/notify-triggers` off `main`-state (`bafd86c`, post-41a-merge); merge HELD for Chat clearance; no force-push.** This was the **last notifications prompt** — nothing further flagged.
