# World Cup Fantasy — Project Brain

## Product summary
A standalone web app for a **private friends' league**: a FIFA World Cup fantasy game
modeled on Sleeper.com's polish and feature depth, redesigned for a **single-tournament**
format. Snake draft (unique player ownership per league), staggered per-match player
locking, head-to-head group stage, guillotine playoffs on a reduced roster, FAAB waivers.
Not client-facing — fun with friends. Guiding constraint: **"boring and reliable" over clever.**

## Build surfaces (who owns what)
- **Claude Code** — implementation
- **Claude Cowork** — progress tracking + manual/operational steps (draft kickoff, stat
  overrides/corrections, FAAB processing oversight)
- **Claude Chat** — ideation, planning, decisions (this surface)
- **Claude Design** — UX/UI

## Working protocol
- **One decision/theme per conversation thread.** Keeps context lean and decisions clean.
- These files are the source-of-truth "brain":
  - `PROJECT.md` (this) — overview, surfaces, protocol, status
  - `DECISIONS.md` — running decision log across all themes + agenda for open ones
  - `SCORING.md` — the locked, build-ready scoring model
  - `ARCHITECTURE.md` — the locked, build-ready stack / infra spec (split out the way SCORING.md was)
- **Start of each new thread:** attach these files (keep them in the Project's knowledge so
  every thread sees them).
- **End of each thread:** update the files with whatever was locked.
- **Verification discipline (applies to Code AND Chat):** state only what's directly verified — read the
  code, queried the DB, or ran a command. Anything not directly observable — Render env/process,
  dashboard config, request origin, deploy status — is labeled an **inference to confirm**, never
  asserted as fact.

## Locked requirements (from brief)
1. Snake draft; unique player ownership per league.
2. Per-player staggered locking — **a player locks the instant he plays ≥1 minute; until then he
   stays swappable** (refined from "locks at his match kickoff" — a benched 0-minute starter is
   *not* locked).
3. ~~Pre-set subs; a sub only fires if the starter played 0 minutes.~~ **Superseded (Theme B): no
   auto-subs.** Substitution is manual — swap any not-yet-played player. Lock-on-play delivers the
   original 0-minute protection.
4. Group stage: head-to-head — **all-play-all ("power record"); top N advance** (refined from 1v1
   and "top X" — N is the playoff field, set at the transition; likely 8 or 10, see Theme C).
5. Playoffs: guillotine (lowest scorer eliminated each round), reduced roster.
6. Waivers: FAAB blind bid. **(Full rules locked — see DECISIONS.md → Theme D: $100 budget,
   daily pre-dawn batch, free-agency fallthrough, reverse-seed rolling tiebreak, playoff
   reinforcement.)**
7. Standalone website.
8. **Live "vs the field" screen** — required by all-play-all; must show points-so-far alongside
   how much is still to come (see ARCHITECTURE.md → Real-time layer).

## Status
| Theme | Status |
|---|---|
| A. Scoring | ✅ LOCKED (see SCORING.md) — **amended:** 6 verification-forced line changes from the BALLDONTLIE field map (3 drops, 2 keep-via-manual, 1 remap); balance untouched; **§8 card-handling clarification** folded in (additive stacking; top minute band = ≥60 catch-all; no point values changed); **feat/scoring-promote-lines** added five §4 lines (shots on target +1/3, ball recoveries +1/5 outfield, big chances created +1/1, accurate crosses +1/4, touches +1/25) promoted out of `extra`, recalibrated possession lost −1/8 → −1/10, and **rejected aerials** (⊂ duels, would double-count) — a scoring change ⇒ re-ingest completed matches + full recompute/standings restate required |
| Data source | ✅ LOCKED — BALLDONTLIE WC API (stats/events) + Sofascore rating scrape + manual failsafe (**amended twice:** (1) locking is play-driven → needs live substitution events; (2) verification — **Sofascore scrape is the PRIMARY rating source** (calibration target), BALLDONTLIE's own `rating` = automatic fallback (provenance unknown); ingestion is **polling** (no webhooks at our tier); tier confirmed **GOAT $39.99/mo**) |
| B. Roster & lineups | ✅ LOCKED — 15-man squad (2/5/5/3), XI of 11, lock-on-play / no auto-subs, multiple-lineups defined, playoff cap ≈9 (7+2); transition + playoff lineup mode implemented (Phases 1+3, 87a7e1a→3952e62) |
| C. League & format | ✅ LOCKED — all-play-all regular season (seed by record, ties by total points), period = matchday wave; **snake draft, per-pick timer = config, autopick queue→best-available**; **playoff field flexible (likely 8 or 10), per-round cut ≈2 tapering to 1 over the 5 WC knockout rounds, fixed at the transition**; **guillotine elimination tie = lowest cumulative tournament total points** (commissioner backstop); **late-correction freeze ≈6h after last FT, commissioner-only after**. Only the recruiting-dependent manager/field number is deferred (config); transition + playoff lineup mode implemented (Phases 1+3, 87a7e1a→3952e62) |
| D. FAAB & Waivers | ✅ LOCKED — $100 (resets to a fresh $100 at the playoffs; single budget across knockout rounds); daily pre-dawn blind-bid batch + $0 free-agency fallthrough; per-player kickoff void+refund; rolling waiver-order tiebreak (seeded once by reverse draft order, carried forward into the playoffs — no re-seed; move-to-bottom only when the tiebreak is used); reinforcement = same FAAB cycle on the playoff field; transition + playoff lineup mode implemented (Phases 1+3, 87a7e1a→3952e62) |
| E. World Cup attrition | ✅ RESOLVED — folded into the playoff transition + FAAB (reinforcement now fully specified in Theme D) |
| Architecture & stack | ✅ LOCKED (see ARCHITECTURE.md) — TypeScript/Next.js, Postgres via Supabase (+ Auth + Realtime), Render compute (web + workers + cron), polling ingestion, recompute scoring pipeline, lock-on-play live consumer; live "vs the field" screen + draft-room infra specified (draft *rules* now locked in Theme C) |

**Live-incident fixes (2026-06-11, MD1):** **premature lineup locks** — ~35 MD1 `lineup_slot` rows
carried `locked_at ≈ now` while their `fifa_match` was still `scheduled` (kickoff days out). Root cause:
the lock-write path (`packages/ingest/src/lock.ts` → `ingest.ts` → `setLockedAt`) stamped `kickoffAt`
for any official-XI starter / entering sub **with no `now` gate**, and both read sites
(`loadLineup.ts`, `loadVsField.ts:148`) treated *presence* of `locked_at` as locked. Fix **merged to
`main`** (write-side now-gate `ee4c18b`, read-side predicate `fab4105`): (1) **write side** — the pure
primitives now take `now` and emit a lock only once its instant has arrived (starter `now >= kickoff`,
sub `now >= entry`), threaded via `MatchCtx.now` from the scheduler tick — a self-guarding write
boundary; (2) **read side** — both sites use the new shared pure `isLockedNow` (`@app/shared`):
locked iff `locked_at != null && locked_at <= now`; (3) a **cleanup SQL artifact** (Sergio runs against
live) nulls the bad MD1 stamps whose fixture is still `kickoff_at > NOW()`, leaving completed-match
locks intact. +8 tests (1719 → 1727), all gates green. See DECISIONS (lock invariant) + ARCHITECTURE §3.

**Live-incident fix (2026-06-12, MD1): `locked_at` UNDER-stamped for players who played (the opposite of
the above).** Genuinely-played slots were left `locked_at = NULL` — e.g. **Raúl Rangel** (Mexico–SA, 90′,
rating 7.3) — because the only lock writers were the **racy** pre-match XI-pull + per-event live sub-locking;
the settle pass held the appearance proof and wrote nothing. **Fix merged (`e888f66`):** `reconcileAppearanceLocks`
stamps every **appeared** player (the `score_player_match` participant set scoring already gates via
`playerAppearedInMatch`) at kickoff, called from **both `ingestLive` and `ingestSettle`**, monotonic +
**period-scoped** `setLockedAt` (only `locked_at IS NULL` slots in the match's OWN `period_id` — never the
ambiguous `team → future-fixture` join). The Rangel-class is pinned by existing tests (`lock.test.ts`
`lockInstantsFromAppearances`/`lockInstantFromSub`, `adapter.test.ts` `playerAppearedInMatch` all-null-stub
exclusion, `ingest.test.ts` settle reconcile incl. monotonic latch). **Despite the merge, 5 prod slots were
under-locked and hand-backfilled** — the **deploy-timing lesson**: `e888f66` was committed **2026-06-12 06:28Z**,
*after* both completed fixtures (Mexico–SA kickoff 19:00Z, Korea–Czech kickoff 02:00Z) had finished; a merged
fix only self-heals a completed match while it is still settling (`hasRating` false, `≤ kickoff + 12h`) **and**
the running Render worker has redeployed to that SHA. "On `origin/main`" ≠ "running on Render." **Residual gap
— now CLOSED (`1723b5a`):** `reconcileAppearanceLocks` ran only on a live/settle tick, so a completed
match that had already dropped from the poll (rating landed, or 12h elapsed) was never reconciled again; a
bounded **48h "recently-completed matches" sweep** (`sweepCompletedMatchLocks`, hourly schedule-sync cadence)
now reconciles those and removes the manual-SQL dependency. See DECISIONS (appeared ⇒
locked backstop) + ARCHITECTURE §3.

**Live-incident fix (2026-06-11, MD1): `score_player_match` generated for NON-participants** —
`score_player_match` rows were written for players who never appeared in a match. Trigger: the
completed **Mexico–South Africa** fixture charged every rostered **GK/DEF** whose team was neither
home nor away a phantom **−1** (the match's goals scored as "conceded" by their uninvolved team),
dragging the whole field negative; MID/FWD non-participants scored 0. Two defects on the per-match
scoring path (`packages/recompute`): it scored ANY dirty `(match, player)` with **no participation
check** (and `markStatPlayerDirty` mints all-null stubs / an upstream player↔match mis-join tags
cross-team rows), and `concededByPlayerTeam` had **no team-in-match guard** (§6 conceded is gated on
role, not minutes). Fix **merged to `main`** (SHA rebased-rewritten; in-main evidence: `recomputePlayerMatch`
participant gate + `deleteScorePlayerMatch` in `packages/recompute` + `ops/2026-06-11-clear-nonparticipant-scores.sql`
artifact): `recomputePlayerMatch`
now gates on the pure `playerAppearedInMatch` (team-in-match **AND** an appearance signal) and a
non-participant gets **no row** (bogus row deleted via new `deleteScorePlayerMatch`, rollup
re-enqueued); `concededByPlayerTeam` hardened to require team-in-match. The **rollup was correct and
untouched** (`recomputeManagerPeriod`/`job:recompute` only re-sum). Live remediation =
`ops/2026-06-11-clear-nonparticipant-scores.sql` (delete cross-team + stub rows) **then**
`pnpm --filter @app/worker job:recompute -- --period "MD1"` — restate-alone won't clear bogus rows.
+9 regression tests; full suite **1747** green. See DECISIONS (participant invariant) + ARCHITECTURE §3/§7.

**Lineup forfeit engine (C1, 2026-06-12):** replaces the cancelled keeper-lock prompt. New rule (DECISIONS
Theme-B forfeit amendment): a played player is **NOT hard-locked** — his slot stays movable; benching a
played starter is a **FINAL, one-way FORFEIT** (`lineup_slot.voided_at` stamp + `is_starter=false`),
forfeiting his period points and barring his return to the XI. **C1 = data + server engine + read contract
only; the destructive-confirm UI is C2.** **Merged to `main`** (`b63f0a4` engine, `9811ff4` tests +
recompute-mirror drift guard, `121f45f` docs). Pieces: (1) migration `20260612120000_lineup_forfeit_voided_at` adds the
column AND extends `enforce_lineup_lock()` to permit EXACTLY the forfeit transition + back-stop the one-way
door (no un-void / no start-of-voided), self-tested + **verified on throwaway Postgres with a uuid-returning
`auth.uid()` shim**; (2) `@app/lineup` engine — `validateLineup` now keys off per-slot play state
(`hasPlayed` = a `score_player_match` row exists, `voided`) + a forfeit-confirm id set; the store stamps
`voided_at` and enqueues a manager-period recompute in-tx; (3) read contract `PeriodLineup.slotMeta`
(`hasPlayed/pointsAtStake/voided/movable`) for C2, rendered by nothing in C1. **Two reconciliations
(reported, not silently chosen):** the read-site change is an **augment, not a replace** (keep `locks` so the
unchanged client exposes no destructive bench affordance; `loadVsField` untouched), and `locked_at` is
**retired from movability but kept** (worker stamping + the IN-direction hindsight backstop intact; full
retirement proposed for C2). **No live destructive path pre-C2:** the route passes no confirm → benching a
played starter is rejected (`forfeit-requires-confirm`), behaviour byte-identical to today. Rollup
unchanged (starters-only). +N tests, all gates green. See DECISIONS (forfeit amendment) + SCORING (principle
6) + ARCHITECTURE §16.

**Lock/forfeit state — reconciled + forfeit UI shipped (2026-06-12):** *(the lock-state "fixed/retired" claim in this entry is **SUPERSEDED** — see "cross-match / non-participant lock leak" below; the over-stamp direction was NOT fully fixed and the statusgate branch was un-retired.)* **Both lock directions are now fixed in `main`** — premature
**over**-stamping (now-gate write `ee4c18b` + now-respecting read `fab4105`) and played-but-unlocked
**under**-stamping (appeared⇒locked backstop `e888f66` + bounded 48h sweep `1723b5a`). The **C1 forfeit engine
is merged** (`b63f0a4` / `9811ff4` / `121f45f`) — earlier "merge HELD" notes above are superseded. **C2 forfeit
UI is merged and render-verified** (`9bee8d1`) — played starters show a pts badge + "Forfeit" affordance (no
padlock — movable, not hard-locked); `ForfeitConfirmSheet` is the one-way confirm gate; Cancel reverts the bench
swap fully; pre-flight `fillEligibleIds` empty → affordance disabled; voided slots render strikethrough +
non-interactive. See ARCHITECTURE §16 (C2 section) + DECISIONS (forfeit amendment). The
throwaway branch **`fix/premature-locks-statusgate` is retired**: rebased onto `main` it showed **zero unique
delta** and never carried the agreed four-layer fix — branch + worktree deleted. The **"four-layer fix" plan is
superseded**: **Layer 2** (no stamp on a not-yet-kicked-off match) is **already met by the in-main now-gate**,
leaving only **optional, non-blocking hardening** — **Layer 1** (kickoff-import: **skip/flag** a missing kickoff
instead of coalescing to ≈now) and **Layer 4** (live cleanup SQL: drop the `p.label = 'MD1'` filter to clean
**all periods**). See DECISIONS (lock invariant + forfeit amendment).

**Live-incident fix (2026-06-12 evening, MD1): cross-match / non-participant lock leak — the THIRD recurrence**
(`fix/premature-locks-statusgate`, merge HELD). During the Canada–Bosnia live window, the sub-event lock path
stamped `locked_at` on **44** MD1 `lineup_slot` rows belonging to **non-participants** — pooled WC players
(Mbappé, James Rodríguez, Ronaldo, …) whose own fixtures are `scheduled` Jun 13–18, **one slot each** (ownership
is unique per league). **Root cause:** `feed.matchEvents` returned **foreign-fixture** substitution events (the
GOAT `match_id` filter is not honoured); `ingestLive` applied the **live** match's kickoff + period and
`setLockedAt` stamped each stranger at `kickoff + their-other-match minute`. Because those instants are
legitimately past, the now-gate could not catch it — **the defect is identity/scoping, not timing.** The
2026-06-11 "`≈ now`" reading was the **same** leak, misdiagnosed; **`ed7717a`'s retirement of
`fix/premature-locks-statusgate` was premature** and is un-retired here. **Fix:** one **`lockSlot`** write
boundary enforcing `isLockWriteAuthorized` (**team-in-source-match + status in-play-or-later + now + period**) —
`reconcile`/XI-pull exonerated (they stamp only at kickoff; the appeared set had **zero** strangers); plus
`ingestLive`/`ingestSettle` foreign-event guards, a feed-client `match_id` re-filter, structured
`lock.slot.{stamped,refused}` + `*.foreign_skipped` logs, trigger migration **`20260612220000`** (scheduled-only
self-heal unlock; verified on throwaway Postgres — scheduled-clear succeeds, completed-clear raises), and an
all-periods GUC-wrapped cleanup **`ops/2026-06-12-clear-cross-match-locks.sql`**. **Cleanup-vs-trigger finding:**
the committed `ops/2026-06-11-clear-premature-md1-locks.sql` (bare `set→NULL`, no GUC / `DISABLE TRIGGER`) would
be trigger-blocked as written, yet the Jun-11 stamps were in fact cleared (James now carries a Jun-12 stamp) →
the cleanup that ran used a **manual override not captured in the artifact** (same-leak-twice, not never-cleaned).
**Corrupt-kickoff theory RETIRED** — `mapMatchRow`/`upsertMatch` never coalesce a missing kickoff to `now`
(`Invalid Date` fails loudly). **Remediation order is load-bearing:** Render deploy (migration + new worker) →
cleanup SQL → live-window verify (fact-7 predicate returns 0 throughout; James reads movable, Rangel locked).
Tests **1861 → 1874**. See DECISIONS (RECURRENCE) + ARCHITECTURE §3.

**C2 branch-hygiene incident:** during C2 development, 22 stray session-generated files were committed directly
onto `main` alongside the feature work. Recovered via `wip/c2-recover` (isolated the clean delta), rebuilt clean
on `feat/lineup-forfeit-ui` — `9bee8d1` is tidy. Reinforces the **worktree/branch-disjoint discipline**: feature
work on an isolated branch (or worktree), never directly on `main`, even for small changes.

**Build progress (Claude Code):** Prompt 01 — repo scaffold + Postgres schema ✅ · Prompt 02 — pure
scoring engine + tests ✅ · Prompt 03 — recompute pipeline (DB→ScoreInput adapter + rating resolver +
dirty-flag sweeper) ✅ · Prompt 04 — standing / all-play-all + seeding + guillotine cut-selection ✅ ·
**Prompt 05 — ingestion + locking, COMPLETE ✅** [05a BALLDONTLIE polling + raw layer + lock-on-play
(`locked_at`); 05b isolated Sofascore scraper + rating-fallback comparison] (283 tests). Key build
facts: match→period resolved by an explicit, structural `fifa_match.period_id` (window-inference
retired; single-league assumption, documented); the player-match dirty invariant is hardened + hoisted
to `@app/db` (`STAT_DIRTY_UPDATE` / `markStatPlayerDirty`, imported by ingestion + scraper); the
scraper is a physically isolated `packages/scrape` + `apps/scraper` (no `@app/ingest` import) resolving
identity by **stored Sofascore IDs only** at scrape time, populated by a verified one-time `keyMatch`
pass (auto-writes unambiguous, flags the rest). **Go-live (post-launch — balldontlie carries the
rating until then):** apply the pending migrations (`period_id`, `kickoff_lock_fallback`, the
`recompute_scope` enum-retire, `sofascore_ids`); confirm the BALLDONTLIE base-path/auth header;
implement `loadSofaIndex` (the Sofascore index source — currently stubbed `[]`) + `pnpm add playwright`
& wire the launcher; smoke-test ingestion against the GOAT trial + a real Supabase. · **Prompt 06 —
server-authoritative draft controller (engine), COMPLETE ✅** (327 tests). New pure `packages/draft`
(`@app/draft`): snake order (`managerForPick`), roster legality (the 2/5/5/3 caps via `@app/shared`),
and autopick selection (queue → best-available, both filtered to available + position-legal) + a
store-backed controller (`startDraft` / `submitPick` / `tickDraft` + completion at 15×N) behind a thin
`DraftStore` port (Memory + Prisma impls). The ONE transaction (guarded pick + `roster_player`
ownership + pointer advance) lives in `commitPick`; it is idempotent (monotonic `current_pick_no`
latch) and backstopped by the `draft_pick` and `roster_player` active-ownership uniques. A worker tick
hook (`tickActiveDrafts`) drives timer-expiry autopicks; `pick_deadline_at` is the only timer source of
truth. **SEAM:** the best-available **default-ranking source is injected** — `getDefaultRanking`
returns `[]` with a `// TODO(confirm):` (no `player.default_rank` column exists), so autopick relies on
each manager's queue until a real ranking is wired. Realtime broadcast + the draft-room UI remain out
of scope (the deferred Design+Code deliverable). · **Prompt 07 — auth (Supabase magic-link + allowlist
+ session→manager binding + the draft-op authz gate), COMPLETE ✅** (406 tests, +79). New pure
`packages/auth` (`@app/auth`): the allowlist gate (`isEmailAllowed`, case-insensitive — a
`// TODO(confirm):`), session→manager resolution (`resolveSessionManager` — dual-key match on the
Supabase uid OR the linked `app_user` email, so it is robust to the unpinned link ceremony), and the
scope-gated act-as assertion (`canActAsManager` — `self` = strict self-match, `admin` = commissioner
override) + a typed `AuthError` family — all DB/Supabase/clock/env-free (`purity.test.ts` proves it,
comment-stripped). The edges live in `apps/web`: `@supabase/ssr` server + browser clients (SEPARATE
from Prisma; authz reads `getUser()`, never `getSession()`) + a session-refresh middleware;
`getSessionManager` / `requireManager` (the reusable gate every later route reuses); the minimal
magic-link sign-in / `/auth/callback` (code-exchange + edge allowlist enforcement: a non-allowlisted
email is `signOut` + denied, never admitted) / sign-out; and `POST /api/draft/pick`, whose
framework-agnostic `handleDraftPick` rejects **401 (no session) / 403 (not your manager) BEFORE**
calling the **unchanged** `submitPick`. Google OAuth is config-gated / seamed-optional. **SEAMS
(`// TODO(confirm):`):** the `user_id`↔`manager` provisioning ceremony (lookup is robust to either
`app_user.id == uid` or email-only linking; no self-serve manager-creation wizard) + email
case-sensitivity. Adversarial review (9 agents, 3 findings, **0 confirmed** — all the single-league
`leagueId`-filter non-issue). **Next: the user-facing critical path** (the WC opens June 11) — the
draft-room UI + Realtime (subscribe to the controller's state, call the now-gated `/api/draft/pick`),
the manager-provisioning ceremony, a minimal lineup-setting flow, and deploy. The draft is the binding
pre-kickoff deadline. All themes remain LOCKED; build is downstream of decisions. · **Prompt 08 —
draft-room screen + Supabase Realtime + worker draft-tick loop, COMPLETE ✅** the deferred Prompt-06 UI:
a gated draft-room screen (pure board / countdown / reducer / pickClient / realtime modules) subscribes
to the controller's state over Supabase Realtime and calls the gated `/api/draft/pick`; a dedicated
worker tick loop drives timer-expiry autopicks. · **Mock-draft smoke
test — the draft exercised end-to-end LIVE ✅** Against the deployed app + a real Supabase, the draft
controller + the draft-room UI + Supabase Realtime + the worker tick/autopick now run together: a draft
started, ran on the per-pick timer, and **completed**, recording **both** manual human picks **and**
timer autopicks. Verified facts: (1) **`default_rank` must be populated for autopick** — an empty ranking
makes `selectAutopick` return null and the draft **stalls**, so the go-live order is **`provision rank` →
`provision draft`**; (2) a clean **~30s pick window** was confirmed and the earlier "born-expired
`pick_deadline_at`" was **ruled out** (a non-simultaneous-read artifact, not a real defect); (3) the
worker **autopicks ~1s after each expiry** (autopicks landing = **verified**; *that they originate from
the Render worker = inference* — no process access, but no local worker was running); (4) picks **stream
across two browser sessions via Realtime** (operator-confirmed); (5) **manual pick recording = verified
working** (human picks recorded alongside autopicks). Engineering follow-ups in DECISIONS.md →
"Mock-draft session — open items": the lobby→active client flip on start, and an autopick empty-ranking
fallback (pre-launch hardening). · **Prompt 09 — draft closeout (lobby→active flip + autopick
totality), COMPLETE ✅** the two mock-draft follow-ups closed: the client lobby→active flip on the
`draft.status` change (partial-safe reducer + authoritative state fetch), and a total autopick
best-available fallback via `orderDraftPool` when the queue/ranking is empty (the empty-ranking stall is
gone). · **Draft surface — verified end-to-end on the live Render deploy ✅**
Both follow-ups above are now **CLOSED** (DECISIONS.md → "Mock-draft session — open items"): the
**lobby→active flip** and **live pick + autopick streaming** were confirmed across **two authed clients
with no reload**. Root fix = the browser Realtime client now authorizes with the **user JWT** (`setAuth`)
before subscribing — the anon socket received zero RLS-gated `postgres_changes`; autopick totality came
via the pure `orderDraftPool` (queue → `default_rank` NULLS LAST → `playerId`). · **Prompt 10 —
set-lineup flow (XI picker + lock-on-play binding), COMPLETE ✅** (605 tests, +~110). New pure
`packages/lineup` (`@app/lineup`): `validateLineup(squad, proposedXI, lockState, period, now)` →
`ok | a typed LineupError family` (`illegal-formation` / `incomplete-xi` / `not-your-player` /
`locked-player-moved` / `wrong-period`), with the Theme-B bounds sourced from `@app/shared`
(`FORMATION_BOUNDS` / `STARTING_XI_SIZE`, never re-derived) and **lock-respecting** on each locked slot's
frozen `is_starter` (a played player can't be moved into or out of the XI); behind a thin `LineupStore`
port (Memory + Prisma impls) + a store-backed `setLineup` controller — the decision core is
**purity-proven** (`purity.test.ts`, comment-stripped). The **player lock is consumed, not
reimplemented**: it is ingestion's `lineup_slot.locked_at` (Prompt 05a), read through the SSR loader. The
ONLY write path is the gated **`POST /api/lineup`**, mirroring `POST /api/draft/pick` (resolve session →
assert it owns the lineup, **401/403 before** the controller, scope `self`), with a **triple-defended
server-authoritative lock re-check**: `validateLineup` on the authoritative lock state → `saveLineup`
writes only `WHERE locked_at IS NULL` and **aborts the whole commit if a locked row would change** → the
`enforce_lineup_lock` trigger is the DB backstop (the Prompt-01 latch is **not regressed**).
`wrong-period` gates on `period.closes_at`, and **future windows are legal by construction** (the
validator's `PeriodWindow` carries no `opens_at`) — so pre-setting upcoming windows is allowed, and an
unplayed, never-locked starter is still backstopped by period close. The **set-lineup screen**
(`app/lineup/`, mapped to `design_reference/setlineup/*` + `shell/*`) renders the formation pitch +
bench, swaps start↔bench with live legality feedback (the SAME `validateLineup`), freezes locked players,
and pre-sets per-period windows; lineup data reaches the browser only via the SSR Prisma loader (**no
browser-direct `lineup_slot` read / no Realtime on the table — Theme F needs no new authenticated SELECT
policy**). Opus adversarial review (6 lenses → verify): **0 blockers, 2 majors fixed pre-merge**
(cross-position swaps so the formation can actually change + the live illegal-formation feedback is
reachable; the FAAB-drop orphan-slot reconcile seam TODO'd). **Launch gates (pre-June-11):** (a) wire the
per-player **kickoff indicator** (`kickoffByPlayer` `TODO(confirm)` — `fifa_match.kickoff_at` via
`player.teamId`); (b) the **runtime lock-freeze + late-edit rejection** is deferred (no drafted roster /
live `locked_at` yet) → fold into the GOAT-trial ingestion smoke test. **Carried TODOs:** FAAB add/drop
reconcile (`saveLineup` orphan-slot DELETE + forbid dropping a locked-slot player), a dedicated
`FormationPicker` / `reshape()`, and a commissioner `admin` lineup scope. **Next: the live "vs the field"
screen** (ARCHITECTURE → Real-time layer) — points-so-far alongside how much is still to come,
all-play-all — then the commissioner/admin surface + the group→playoff transition. All themes remain
LOCKED; build is downstream of decisions. · **Prompt 13 — vsfield RLS migration self-test fix (launch-gating deploy blocker), COMPLETE ✅ (deploy reported green).** Migration `20260606170000_rls_realtime_vsfield`'s embedded self-test set the JWT claim to a **non-uuid label** (`'rls_selftest_user_in'` / `'…_out'`); on Supabase the real `auth.uid()` casts `request.jwt.claim.sub` to `uuid`, so the helper `vsfield_caller_shares_league_with_manager` **`22P02`'d on every clean apply** — `prisma migrate deploy` failed at #10 and blocked the Render deploy on WC-eve. **Fix = valid-uuid literals** (`00000000-…-0001/0002`, canonical-lowercase so `manager.user_id (text) = auth.uid()::text` round-trips), mirroring the Theme F precedent (`20260605170000_enable_rls_public_tables`). Helper / SELECT policies / `supabase_realtime` adds **byte-for-byte unchanged**; cleanup (sentinel-rollback) already present; member-can / non-member-cannot assertion intact (#10 `finished=true` ⇒ both branches green). Proven by a Supabase-faithful Docker repro (uuid-returning `auth.uid()`) that reproduced the Render error byte-for-byte, then applied all 10 clean with zero residue. **Production gate cleared (operator-confirmed):** failed record resolved on the live Supabase DB (`migrate resolve --rolled-back`), branch `fix/vsfield-migration-selftest` merged to `main` (FF, 2 files: the 8/2 migration hunk + RUNBOOK), Render deploy ran #10 clean → green (first green deploy carrying the vsfield RLS migration). Recovery documented in **RUNBOOK d.1**. ⚠️ *Confirm-to-close (not yet observed): worker process up + sign-in→vs-the-field smoke — build/migrate is green; worker + smoke pending operator confirm.* · **Prompt 15 — pre-prod security follow-ups (function hardening), COMPLETE ✅** Migration `20260606180000` on `main` (clean merge `c8f404d`) + Render deploy **GREEN** (embedded self-test passed on live → `EXECUTE` revoke + both `search_path` pins **catalog-verified**): `REVOKE EXECUTE` on `mirror_auth_user_to_app_user()` from `PUBLIC`/`anon`/`authenticated` (`SECURITY DEFINER` kept — trigger still fires) + pin `enforce_lineup_lock` `search_path=''` (`INVOKER`, body unchanged), guarded by a fail-safe catalog-only self-test. Net-new vs prior `main` = only the lineup-lock pin (the mirror revoke + `''` pin were already in `20260606010000`). Behavioral confirms fold into go-live: first allowlisted signup → `app_user` row (trigger fires); first kickoff → locked-player swap rejected (no live locks pre-tournament). Leaked-password protection (HaveIBeenPwned): pending operator dashboard toggle. (Prompt-13 worker+vsfield smoke + Prompt-14 close-out remain separate.) · **Prompt 16 — landing hub (auth-aware root replacing the scaffold), COMPLETE ✅** (690 tests, +5). The Prompt-01 scaffold `/` (`apps/web/app/page.tsx`) is replaced by an **auth-aware server component** that reuses `getSessionManager()` **unchanged** (the Prompt-07 `getUser()`-backed edge) and defers the render to a pure **`selectLandingView(outcome)`** (`apps/web/src/landing/`, IO-free, `@app/auth` type-only import, exhaustive `switch` with a `never` guard so a 5th outcome kind is a compile error). Four states → four renders: **no-session → signin** (single "Sign in" CTA → `/sign-in`, closing the front-door gap); **ok → hub** ("Signed in as {name}" + nav cards to `/draft` / `/lineup` / `/vsfield` + a **POST** sign-out `<form action="/auth/sign-out">`, closing the post-login stranding gap — members land here via the callback's default `/` redirect, `safeNextPath(null)` left unchanged); **no-manager → unlinked** — a **distinct** "not linked to a manager yet — contact the commissioner" state, **NOT** routed to `/auth/denied` (the Prompt-07 provisioning seam; the `handlePick` / `handleVsField` 403-collapse is deliberately split out here); **not-allowlisted → denied** (defensive — the callback already signs these out). The build flips `/` from static `○` to **dynamic `ƒ`** (per-request SSR) — a cached static root would have leaked one user's hub to all, so the dynamic render is a correctness requirement, verified. `pnpm -w typecheck && lint && format:check && test` (690) + `pnpm --filter web build` all exit 0; no out-of-scope churn (no redirect/callback change, no feature-page / API / middleware / auth-core edits, no admin surface, no security-closeout churn, no new routes/env). **Numbering:** this is **Prompt 16**, deconflicted from **Prompt 15 = the pre-prod security follow-ups closeout** already on main (unrelated). **Next follow-up (flagged, not done):** the shared cross-nav strip on the three authenticated layouts so members move between draft / lineup / vsfield without bouncing through `/`. ⚠️ *Merge: implemented + verified on `feat/landing-hub` @ `dd0aed3`; merge to main pending base-reconcile onto `9accb1f` + working-tree cleanup (see DECISIONS → "Landing hub & route-map ground truth").* · **Prompt 17 — shared cross-nav strip on the authenticated screens, COMPLETE ✅** (697 tests, +7). `/draft`, `/lineup`, `/vsfield` had no cross-navigation — reaching one from another meant bouncing through `/`. Closed by a single presentational shared component, `apps/web/app/shell/CrossNav.tsx` (client component for `usePathname()`), mounted once per route-scoped layout — **no shared layout existed** (each screen has its own `app/<route>/layout.tsx`; the route-group refactor to a literal shared layout was rejected as out-of-scope churn). Markup lives in one place — no triplication. Links the other two feature screens + home (`/`); the current screen is indicated via a pure **`selectActiveNav(pathname)`** (`apps/web/src/shell/`, mirroring `selectLandingView` — exact home match; feature routes match exact / trailing-slash / nested sub-path but not a prefix-sibling like `/draftroom`; → `.tab.is-active` + `aria-current="page"`). Sign-out reuses the Prompt-16 hub form verbatim (`<form action="/auth/sign-out" method="post">` → existing 303 handler; native POST, JS-free even inside the client component). Zero new CSS (reuses ds.css `.tabs`/`.tab`/`.btn-ghost`). No new auth / routes / env / middleware; screens stay `getSessionManager()`-gated as before; build keeps `/draft` `/lineup` `/vsfield` dynamic `ƒ`. 7 unit tests (`crossNav.test.ts` — config shape + path-match edges). Full gate + `pnpm --filter web build` exit 0; no out-of-scope churn (hub `/` untouched; no feature-page / API / admin / auth-core / security-closeout edits). **Last navigation follow-up — nothing further flagged from this theme.** Merged to main @ `14a0811` (off current main `52e1416`, ff, no force-push). · **Prompt 19 — landing visual re-skin (apply the XI design system to `/`), COMPLETE ✅** (708 tests, +7). The Prompt-16 four-state `/` is re-skinned to the delivered design as a **purely visual** change: `selectLandingView()`, the four-outcome branch, the session read, and the route set are **byte-for-byte unchanged** (proven by `diff` of `Home()` vs HEAD + a source-contract smoke). The handoff package (`design_handoff_landing`) was **NOT actually vendored** as the prompt claimed (its `ds.css` is byte-identical to the already-vendored `apps/web/app/{draft,lineup,vsfield}/ds.css`; only `landing.css` + `XI Landing.html` were new) — the operator supplied it from Downloads; vendored to `apps/web/app/_landing/`. **The delivered design is a full 10-section public marketing+login page, not a four-state hub re-skin** (the prompt's premise was off): resolved with the operator → the logged-out **`signin`** state renders the full XI marketing landing (`_landing/MarketingLanding.tsx`, ported 1:1 from `XI Landing.html`: nav · hero · brand-trophy band · 4 mechanics rows · scoring 5-col grid (categories only, **no point values** — honesty) · draft-board + flag-pitch showcase · how-it-plays · 6 explore cards · CTA · footer); **`hub`/`unlinked`/`denied`** get branded `ds.css` panels (no design pixel-truth exists for them). The prototype's **two inline self-serve email forms become a "Sign in" CTA → `/sign-in`** (honoring "no self-serve join flow"); the prototype Tweaks panel + `<script>` are dropped (scroll-reveal left static — `.lp-reveal` is fully visible at rest, so the page stays a server component, no client boundary). **CSS is per-route, NOT global** (operator decision — global `ds.css` would double-reset with the existing Tailwind `globals.css` + double-load on the feature routes): `ds.css` + `landing.css` imported in `page.tsx`, scoped under a `.lp` wrapper; root `layout.tsx` untouched (no conflict with Prompt 18's metadata edit). **Two sanctioned `landing.css` adaptations:** `body.lp` → `.lp` with **`overflow-x: clip`** (not `hidden` — would make `.lp` a scroll container and break the sticky nav) + **`color: var(--text-primary)`** (the root `<body>`'s Tailwind `text-slate-900` *class* beats `ds.css`'s `body{color}` *element* rule on `/`, so color-less headings would inherit dark-on-dark). `<Brand/>` (`BrandMark`) sits in every state's header; sign-out stays a **POST** form; **unlinked stays distinct from denied** (only `denied` links `/auth/denied`). Amends **ARCHITECTURE §1 "Tailwind"** (plain CSS consumed as-delivered; see DECISIONS). Opus adversarial review (5 lenses → verify): **0 blockers / 0 majors / 0 confirmed-serious**; folded in the a11y polish it surfaced (decorative trophy/icons `aria-hidden` so the brand link reads "XI WC Fantasy League", not "XI XI …"). Gates: `pnpm -w typecheck && lint && format:check && test` (708) + `pnpm --filter web build` all exit 0; **`/` stays `ƒ`**; `/sign-in` + `/auth/denied` unchanged (`○`). **Deferred follow-up (flagged):** skin `/sign-in` (+ `/auth/denied`) — the login design (`Join.html`) was **not in this handoff bundle**, so not improvised. ⚠️ *Branch `feat/landing-design` (`aa295bd`) **stacks on Prompt 18** (`feat/brand-pwa`) — it imports `Brand.tsx` (landed in 18, not yet on `main`); **merge order: 18 → main, then 19.** Held for clearance; not pushed; no force-push.* · **Prompt 20 — App Shell: `ds.css` global foundation + shell chrome, COMPLETE ✅** (723 tests, +15). The first/foundational screen of the design sprint; every later skin sits on it. **(1) `ds.css` promoted to the single GLOBAL stylesheet:** imported once in root `layout.tsx` **after** `globals.css` (wins cascade ties); canonical byte-identical copy at `app/styles/ds.css` (md5 `66d4bbbc`); the 4 per-route copies stay (double-load harmless; de-dup is post-sprint). Tailwind / `globals.css` / Preflight **coexist** (not retired). **(2) Collision A (body surface) fixed:** root `<body>` dropped Tailwind `bg-slate-50 text-slate-900` (CLASS selectors that out-specified `ds.css`'s `body{…}` ELEMENT rule, pinning the app light); the ds **dark** surface now applies (dark = the `:root` default, no `data-theme` needed); `min-h-screen antialiased` kept. **(3) App Shell chrome:** `app/shell/AppShell.tsx` (+ `shell.css`) — the design's **GlobalTopbar** ported from `App Shell.html` + `shell/*`, a **pure server component** (no client JS: explicit `active` prop instead of CrossNav's `usePathname()`; plain `<a>` nav; POST `<form>` sign-out). `<Brand/>` per **BRAND.md §5** = `BrandBadge` trophy chip + "XI" + `WC Fantasy League` secondary line (brain file overrides the prototype's league-name-as-primary). Nav lists **only the 4 built routes**; the prototype's 14-screen IA + "More"/bell/avatar/mobile-tab-bar/sheets/commissioner (all targeting **unbuilt** screens) are flagged `TODO(confirm)` seams. **(4) Mounting** = per-feature-layout (no route group — the dual-state `/` can't be wrapped unconditionally): `/draft` `/lineup` `/vsfield` layouts swap `<CrossNav/>`→`<AppShell active=…>`; the **hub state only** of `page.tsx` is wrapped (`active="home"`, dropping its `.lp-nav`). `selectLandingView()` + `Home()`'s branch + the `signin`/`unlinked`/`denied` states are **byte-for-byte** (they keep landing chrome). **CrossNav absorbed** — `app/shell/CrossNav.tsx` deleted; the pure `crossNav.ts` helper + test stay/reused. Amends **ARCHITECTURE §1** + **supersedes Prompt 19's "per-route, not global."** **Opus adversarial review (5 lenses → verify): 3 of 10 confirmed, all folded** — (a) a naive height port clipped the fixed-height `/draft` surface (~3300px, browser-reproduced) → restored the design's `.sh-app{height:100%}` + `.sh-content{flex:1;min-height:0;overflow-y:auto}` dual model, **browser-verified** for both fixed-height (/draft) and natural-scroll (/lineup,/vsfield,hub); (b) global dark body made `/sign-in`+`/auth/denied` gray status/helper text illegible (~2:1) → minimal `text-slate-400/300` legibility repair (still Tailwind; full skin stays Prompt 21); (c) duplicate `banner` landmark on `/lineup` → `aria-label="Global"` on the shell `<header>`. Gates: `pnpm -w typecheck && lint && format:check && test` (723) + `pnpm --filter @app/web build` all exit 0; **`/` stays `ƒ`**; `/sign-in`+`/auth/denied` stay `○`; no out-of-scope churn (no Tailwind retirement, no feature-body re-skin, no `selectLandingView`/auth/route/env/middleware/`.gap-*`/admin edits). **Next: Prompt 21 — sign-in / Join skin** (`/sign-in` + `/auth/denied` from `Join.html` + `auth/*`, migrating them off Tailwind). ⚠️ *Branch `feat/app-shell`, off post-19 `main`; held for clearance; not pushed; no force-push.* · **Prompt 21 — sign-in / Join skin (`/sign-in` + `/auth/denied` off Tailwind onto ds), COMPLETE ✅** (735 tests, +12). Second screen of the design sprint: both auth routes migrate off bare Tailwind onto the ds **split-shell** per the repo's canonical `design/design_reference/Join.html` + `auth/*` + BRAND.md §5. **(1) Route-scoped CSS:** new `apps/web/app/_auth/auth.css` ports only the `au-*` rules from `Join.html`'s `<style>`; tokens + `.btn`/`.spinner` come from the **global** ds.css (the `shell.css` model — **no per-route ds.css copy, ds.css not forked**). **(2) Shared chrome:** new `apps/web/app/_auth/AuthChrome.tsx` (parallels `_landing/chrome.tsx`) = `AuthScreen` (split shell + brand panel + form-column slot + foot) + the design's icons + `PrivateTag`; **no `"use client"`** so the client `/sign-in` and server `/auth/denied` both render it. **(3) Brand per BRAND.md §5:** reused Prompt-18 `LockupStacked` (trophy · "XI" · tagline, **not redrawn**) + the `{league} · {season}` row (`WC Fantasy League` — same placeholder the shell uses, no new source/no `SHELL_LEAGUE_NAME` — + `World Cup 2026`) + `PrivateTag` + static value-props; the data-bound `RosterAvatars`/`InviteBanner` are **dropped, not faked** (no manager/invite fetch on these routes); the design's pre-rebrand `AuthLogo` "W" chip is replaced by the XI lockup. **Gold (BRAND.md §1) only in the trophy PNG**; "XI" = `--text-primary`, CTA/links/dots cobalt `--accent`, `PrivateTag` slate `--locked` — no gold leak. **(4) `/sign-in` presentation-only:** Supabase `signInWithOtp` (→ `/auth/callback`), env-gated `signInWithOAuth(google)`+`GOOGLE_ENABLED`, and `createClient` **byte-for-byte preserved**; the one-string `message` became two presentation flags (`error`+`sent`) so the request form + "check your email" confirmation are the two designed views; **no next/safeNextPath introduced** (stays in `auth/callback`). `/auth/denied` keeps its **dual-cause** copy (allowlist OR expired) + back-to-sign-in. **Early-warning seam clean:** the root `denied`/`unlinked` `selectLandingView` states are **independent `page.tsx` components** (already ds via Prompt 19), NOT shared with the `/auth/denied` route → **no `page.tsx` edit**. **Shell-free boundary holds** (no `AppShell`, no auth `layout.tsx`); both stay `○`, `/` stays `ƒ`. Responsive split→stacked collapse (`@media ≤1100px`) ported verbatim; **browser-verified** (desktop split + mobile stack + the denied au-center view). **Opus adversarial review (4 lenses → per-finding verify): 1 of 1 confirmed, folded** — the reused `LockupStacked` double-announced "XI" (trophy `alt="XI"` + visible wordmark) → wrapped the lockup in `aria-hidden` (decorative splash; the `_landing/chrome.tsx` precedent), accessible brand name now from the visible league name + each view's `<h1>` (`Brand.tsx` untouched — shared primitive); **browser-DOM-verified** (aria-hidden, still centered, single `<h1>`). New pure-Node source-contract smoke `apps/web/src/auth/authPages.test.ts` (+12, mirrors `landingPage.test.ts`). Gates: `pnpm -w typecheck && lint && format:check && test` (735) + `pnpm --filter @app/web build` all exit 0; route shapes preserved (`○ /sign-in`, `○ /auth/denied`, `ƒ /`); no out-of-scope churn. **Both `TODO(confirm)`s resolved (Chat clearance, folded as CONFIRMED):** (a) the auth foot stays **prose, never a link** — no commissioner-contact route exists app-wide; do not invent a `mailto`/`href="#"` (a real one is a later prompt); (b) `/auth/denied`'s broadened `<h1>` "We can't sign you in" **stays** (recorded **design-deviation** — the route is dual-cause allowlist-OR-expired, so the design's allowlist-only `DeniedView` copy would mislead; cause-specific copy = a future logic prompt). The `message → error+sent` split is endorsed as presentation. **Next: Prompt 22 — first feature-body re-skin (Draft, from the `design_reference/` Draft screen).** ✅ **CLEARED (Chat); `feat/auth-skin` (off post-20 `main`) pushed to origin (no force-push); Sergio runs the FF merge to `main` + owns the Render deploy + live-verify (Code does NOT merge/deploy).** · **Prompt 22 — Draft body re-skin (`/draft` to the `design_reference/` Draft screen), COMPLETE ✅ + MERGED** (745 tests, +10). The **first feature body** of the design sprint; the ds-aligned skinned set is now **landing (19) → shell (20) → auth (21) → Draft (22)**. A 6-agent adversarial fidelity audit confirmed the `/draft` body was **already byte-faithful** to `design_reference/Draft Room.html` on the global ds.css (the Prompt 08/09 port), so the re-skin reduced to **brand de-dup**: after Prompt 20 wrapped `/draft` in `AppShell` (which owns the trophy · "XI" · league lockup), the body's `.dr-top` doubled it with its own `.dr-logo` "W" + "Snake Draft" wordmark → dropped; `.dr-top` → a de-branded `.dr-status` strip (phase line + connection pill + presence), **no brand mark in the body** (BRAND.md §1/§5). The `.dr-*` layout stays route-scoped in `apps/web/app/draft/draft.css` (the `shell.css`/`_auth/auth.css` convention) on the **global** ds.css — **ds.css not forked**; the per-route `draft/ds.css` copy is left for the post-sprint de-dup; Tailwind/`globals.css`/Preflight retained globally (Lineup + Vs-the-Field still consume them). draft.css stays fully tokenized (zero hex) → cobalt/red/slate only, **no gold leak**. **Presentation only** — `packages/draft`, the gated `POST /api/draft/pick`, `handlePick`, the worker tick, the Realtime subscription, the **server-synced countdown** (`pick_deadline_at`, never the client clock), and the deadline logic are **untouched**; the Prompt-20 fixed-height/internal-scroll model holds; `/draft` stays `ƒ`. **Deferred-not-built (flagged):** the lobby start/sim controls + "Draft starts in" countdown and the Summary projected/grade/value-pick + `proj` sublines are **data-shape seams** (absent from `DraftRoomState` — need loader/payload fields, an early-warning STOP); the autopick queue **editor** (add/drag-reorder/remove + the "Idle → autopick" hint; the queue stays a read-only display) and the board **auto-scroll-to-current-pick** (ref+effect) are out-of-scope follow-ups. New pure-Node source-contract smoke `apps/web/src/draft/draftRoom.test.ts` (+10, mirrors `landingPage`/`appShell`; draft suite **59 green**); a `selectDraftView` extraction was rejected as disproportionate logic churn (the 49 existing pure draft tests already pin the behaviors). Gates: `pnpm -w typecheck && lint && format:check && test` (745) + `pnpm --filter @app/web build` all exit 0; independent adversarial diff review = clean PASS. **Live visual fidelity + the one `// TODO(confirm)` (the de-branded strip label) are the operator gate** on the Render deploy (`/draft` needs auth+DB+Realtime, not in-session renderable), per merge→verify-live. **Merged to `main` at `d9800e7`** (`b135cac` P20 → `7e5d801` P21 → `d9800e7` P22); this thread-close docs record was added separately (it didn't ride the cherry-picked draft commit). **Next: Prompt 23 — Lineup re-skin (from the `design_reference/` Set Lineup screen).** · **Prompt 23 — Lineup body re-skin (`/lineup` to the `design_reference/` Set Lineup screen), COMPLETE ✅ + MERGED** (757 tests, +12). The **second feature body** of the design sprint. An adversarial fidelity audit confirmed `/lineup` was **already a faithful `.sl-*` port** of `design_reference/Set Lineup.html` on the global ds.css (no raw Tailwind, gold-free) and — **unlike `/draft` and `/vsfield`** — it **never carried a body brand chip** (`sl-topbar` is a plain de-branded screen-title strip: `<h1>Set lineup` + manager name + PeriodTabs), so the Prompt-22-style brand de-dup was **N/A** (nothing to remove). The re-skin reduced to **two presentation-only reconciliations**: **(1) pitch markings** — ported the design's full field markings (penalty boxes + goal areas + halfway line + centre circle, the `.sl-pl-*` set) + mowed-stripe turf, replacing the old inner-frame + single `::before` circle, so the primary surface reads as a pitch; **(2) legend over-claim** — the binary `lineup_slot.locked_at` (+ `LockTag`) is just "Locked", not "Locked · played", so relabelled to "Movable — still swappable" / "Locked — has played, frozen". Files: `SetLineupClient.tsx` + `components.tsx` + `lineup.css` + new pure-Node smoke `setLineup.test.ts` (+12, mirrors `draftRoom`). **Presentation only** — `packages/lineup` (`validateLineup`), the gated `POST /api/lineup`, the server-side lock re-check, `lineup_slot` persistence, `requireManager` **untouched**; `/lineup` stays `ƒ`. **Flagged-not-built (data-shape / live-feed seams):** `FormationPicker`, flag-kit jerseys + `<Flag>` chips, live `ScorePill`/`PlayerScoreSheet`, the 3-state lock·playing split, next-lock countdown, per-player kickoff chip, connection banners, autosave. **Merged to `main` at `43490aa`** (off post-22 `71c96d8`). **Next: Prompt 24 — Vs-the-Field re-skin (ran in parallel, same `71c96d8` base).** · **Prompt 24 — Vs-the-Field body re-skin (`/vsfield` to the `design_reference/` Vs the Field screen), COMPLETE ✅ + MERGED** (770 tests, +13). The **third feature body and FINAL screen of the design sprint** (ran in parallel with Prompt 23 — a sibling branch off the same `71c96d8`, touching disjoint files). A fidelity audit confirmed `/vsfield` was **already a faithful `.vf-*` port** of `design_reference/Vs the Field.html` on the global ds.css (gold-free), so — like Prompt 22 — the re-skin reduced to **two presentation-only changes**: **(1) brand de-dup** — drop the body `.vf-logo` "W" badge now that the App Shell topbar (Prompt 20) owns the trophy · "XI" · league lockup; `.vf-top` → a de-branded `.vf-status` stack (screen label "The Field" + the live period line) + tabs + Updated/ConnPill (mirrors Prompt 22's `.dr-status`). **`.vf-logo` was vsfield-LOCAL** — BRAND §5 calls it "the shared chip," but in the implemented codebase it was defined+used ONLY in `apps/web/app/vsfield/*` (exactly like `.dr-logo` was route-scoped to draft), so removing the def + usage is **not** a shared-file edit. **(2) pitch markings** — the original port dropped the design's faint centre circle (`.vf-pitch::after`) + halfway line (`.vf-pitch-{v,h}::before`); ported verbatim (white `rgba .05`, no gold) — the same gap/fix as Prompt 23. **Avatars stay initials** (BRAND §6 — no parrot). The **natural-scroll height model is preserved** (`.vf-app` keeps `min-height:100%`; `shell.css` classes `/vsfield` as a natural-scroll screen where `.sh-content` owns the single scrollbar — NOT forced to the design's `height:100%`, which would fight the shell). The **live indicator is red `#FF4D4D`** — design-canonical (broadcast/match-LIVE pulse, not green; see DECISIONS → "Lineup + Vs-the-Field skins"). Files: `VsFieldClient.tsx` + `vsfield.css` + new pure-Node smoke `vsFieldSkin.test.ts` (+13). **Presentation only** — `packages/vsfield` (`buildVsField`), the Prompt-04 pairwise helper, `loadVsField`, the authed read `GET /api/vsfield` (**401, no 403** — league-scoped), the JWT-authed Realtime subscription + polling fallback, the RLS/publication migration, `requireManager` **untouched**; `/vsfield` stays `ƒ`. **Flagged-not-built (out-of-scope seams):** the per-player H2H XI list (needs player names + `score_player_match`), the scoring FeedPanel (event-level rows), the clickable match-card filter. **Merged to `main`** (`fee577f` via merge `37fd7c6`). 🎉 **DESIGN SPRINT COMPLETE** — the five screens (landing · auth · Draft · Lineup · Vs-the-Field) re-skinned to `design_reference/`, all sitting on the Prompt-20 App Shell foundation: **landing (19) → App Shell (20) → auth (21) → Draft (22) → Lineup (23) → Vs-the-Field (24)** (`main` = 68 files / 770 tests). The only remaining sprint work is the **operator gate**: live authenticated visual fidelity on the Render deploy (the feature bodies need auth+DB+Realtime, not in-session renderable), folded into the go-live / GOAT-trial verify-live. · **Post-provision fix — `resolveSessionManager` hardening (`1b28e3b`), COMPLETE ✅** `resolveSessionManager` now **throws `AmbiguousManagerLinkError`** when the Supabase uid resolves to manager A but the linked `app_user` email resolves to manager B (was **silently returning the uid match** — a split link could bind a session to the wrong manager). · **Post-provision fix — match-schedule mapper shape (`6ca5829`), COMPLETE ✅** `mapMatchRow` + `derivePeriodLabel` reconciled with the GOAT API's **nested** matches response (`home_team`/`away_team`/`stage`/`group` are objects, not flat strings). Verified against live **2026 schedule data: 104 fixtures, 103 carrying a `period_id`** (3 group MDs + 5 KO rounds); the 1 excluded fixture is the **3rd-place match** (no 9th period bucket). · **Post-provision fix — scoring mappers shape (`f3db93a`, branch `fix/scoring-mapper-shape`), COMPLETE ✅** (782 tests) `mapEvent`/`mapShot`/`mapTeamStat`/`mapStatLine` reconciled with the documented GOAT shapes. **Critical:** `mapEvent` read flat `player_id` but the API **nests** `player`/`assist_player`/`player_in`/`player_out` (the **same root cause** as the match mapper); also `mapShot` `minute`→`time_minute`, `mapTeamStat` `possession`→`possession_pct`, and `referee` typed as an **object, not a string**. New **`FeedShapeMismatchError`** gives **per-item isolation** — one bad row is logged + skipped, never halts the batch. Shapes verified against live **2022** match data (match 1030, Qatar v Ecuador). · **Prompt 25 — FAAB batch engine (pure resolver + bid route + cron body), COMPLETE ✅ + MERGED** (901 tests). The first of two Waivers prompts (the **engine**; the `/waivers` UI is Prompt 26) — implements the LOCKED DECISIONS §D blind-FAAB → free-agency algorithm with **no UI**. **New `packages/faab` (`@app/faab`)** mirrors the draft/lineup pure-core + injected-IO shape: a **PURE, IO-free batch resolver** (`resolveFaabBatch`) encoding all 8 §D steps — void+refund a bid whose add target already kicked off, highest-bid-first player-by-player, tie → rolling waiver order, **move-to-bottom ONLY when the tiebreak is actually used** (winning on amount alone never moves you), own-bids resolve highest-first-skip (emergent from the global-highest loop against the manager's updated budget/roster/ownership), $0 bids legal, every won claim is add/drop + a budget debit, contiguous 1..N waiver order — proven clock/Prisma/Supabase-free by a **70-case purity matrix** (7 modules × 10 forbidden patterns). **Gated `POST/PATCH/DELETE /api/faab/bid`** (submit / edit / cancel) via the framework-agnostic `handleBid` on the `/api/draft/pick` template: `requireManager` → `assertCanActAsManager({scope:"self"})` → **401 (no session) / 403 (not your manager) BEFORE any DB write** (verified structurally with an exploding-Proxy store), then submission validation — `amount ≥ 0`, **over-commit rejection** (`amount ≤ budget − other pending bids`), add unowned league-wide + not-kicked-off, drop owned + `≠ add` + required-when-full + **drop-lock validation** (can't drop a player locked by play in an active matchday), 2/5/5/3 roster legality. **Real cron body** in `apps/worker/src/jobs/faabBatch.ts` (replaces the live-but-inert placeholder): opens a `FaabBatch`, reads pending bids + managers + roster + add-target kickoffs, calls the pure resolver, then writes ALL outcomes in **ONE atomic `$transaction`** (the no-double-spend / valid-drop guard) — assign winners (add/drop + debit), refund voids, mark losers, apply the move-to-bottom reorder as the schema-mandated **TWO-PHASE reassignment** (negate → final 1..N, preserving the contiguous `manager_waiver_order_uq` invariant), stamp each bid's `batchId` + terminal status (`won`/`lost`/`voided_refunded`), write the batch `complete`. **Idempotent re-run** (a `done` batch is not re-processed: guarded `updateMany WHERE status='pending'` + no-pending-bids → no batch row). **Lineup-slot release behind the `@app/lineup` domain boundary:** a won drop's UNLOCKED `lineup_slot` rows are released (so a dropped starter stops scoring) via exported `@app/lineup/prisma` `releaseDroppedPlayerSlots(tx,…)` inside the same batch transaction — **faab never touches `lineup_slot`**; the lock-on-play check (`findLockedSlotPlayerIds`) lives there too. **Cross-player equal-amount ordering = waiver order** (by the leading bidder's `waiverOrderPosition`, not player id / insertion). `addTargetKickoffAt` resolves through **`fifa_match.kickoff_at` via `player.team_id`** — the SAME schedule field lock-on-play derives `lineup_slot.locked_at` from (no second clock). **Deferred / flagged:** the `faab_bid.priority` column migration is deferred to **Prompt 26** (pending-claim reorder NOT exposed; the engine resolves own bids by **amount**, with `priority` reserved as the intra-manager equal-amount tiebreak for a future migration); a free-agency **FA `Acquire` route does NOT exist** (flagged sibling concern, not built here); the playoff FAAB reset + waiver carry-forward belong to the separate group→playoff transition prompt (the engine only READS current budget/order). `faab_bid` RLS already present (Prompt-05 invariants migration; no gap). Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (901) + `pnpm --filter @app/web build` + worker build all exit 0; no churn to scoring / recompute / standings / draft / ingestion / auth signatures. **Merged to `main` @ `2145700`.** **Next: Prompt 26 — Waivers UI (`/waivers` to the `design_reference/` Waivers screen, on this engine).** · **Prompt 33 — draft-pool emoji flags (`<Flag>` + `src/draft/flag.ts`), COMPLETE ✅** (1074 tests). Pure `src/draft/flag.ts` resolver (ISO alpha-3 / FIFA alpha-3 / English name → ISO alpha-2 → Unicode regional-indicator pair); sole `<Flag>` React render surface; `countryFlag` wired to pool rows + queue + nation-filter chips. **Prompt 34 — draft nation binding, COMPLETE ✅.** DraftPlayer.country now sourced from FifaTeam.name via player.team join in `loadDraftRoom.ts` (was the never-written `player.country` scalar). · **Prompt 35 — flag-resolver gap closure + collapsible nation filter, COMPLETE ✅** (1085 tests). All 48 distinct FifaTeam.name values in the 2026 dataset now resolve to flags (zero placeholders): home nations (England/Scotland/Wales/Northern Ireland → 🇬🇧 interim), Curaçao, DR Congo, Côte d'Ivoire, Bosnia, Cabo Verde (Intl Node-25 name mismatch), and 11 FIFA formal names. Collapsible nation chip grid added (default collapsed, active-selection header with ✕ clear). CLIENT-ONLY (`flag.ts` / `flag.test.ts` / `components.tsx` / `draft.css`). · **Prompt 36 — home-nation inline SVG flags, COMPLETE ✅** (1089 tests). England = St George's Cross (#CE1124 on white), Scotland = Saltire (#005EB8, white diagonals) — inline SVG, universal, no dep, no asset files, no emoji tag sequences. All four home-nation → GB fallbacks removed from `src/draft/flag.ts` (England/Scotland/Wales/N. Ireland → null); `Flag.tsx` intercepts via exported `isHomeNation()` predicate before the emoji path. 46 other nations unchanged (emoji mechanism). CLIENT-ONLY (`flag.ts` / `flag.test.ts` / `flagWiring.test.ts` / `Flag.tsx`). · **Prompt 37 — dashboard home foundation + pre-draft + draft phases, COMPLETE ✅** (1122 tests, +33). The Prompt-16/19 nav-card hub is replaced by a phase-aware `<Dashboard>` component on `/`. Phase derived from `draft.status` via the new pure `selectDashboardPhase` selector (`src/dashboard/`; `never` exhaustiveness guard; 5 unit tests). `loadDashboard` (`app/_dashboard/loadDashboard.ts`) reuses `loadDraftRoom` DIRECTLY — no second draft source, no re-derivation. `DashboardData { phase, draft }` feeds `PrimaryBanner` (phase-coloured `--phc` CSS var) + the `modulesFor`/`renderModule` module router in `Dashboard.tsx`. Four modules shipped: pre-draft → `LeagueInfoModule` + `ReadinessModule`; draft → `DraftFormingModule` + `RecentPicksModule`. All data from `DraftRoomState`; no additional reads. Route-scoped `dashboard.css` on global `ds.css`; zero hex; no gold; `--phc` for phase colour (the `shell.css` convention). `page.tsx` `Hub` now `async`; calls `loadDashboard(managerId)`; renders `<Dashboard data={data} />`; all non-hub branches + `selectLandingView` + force-dynamic are **byte-for-byte unchanged**. **Two open data-shape seams (STOP-flagged in source):** (a) `draft.scheduled_start_at` does not exist — pre-draft countdown deferred, renders "waiting for commissioner"; (b) per-manager `is_ready` does not exist server-side — readiness grid deferred, all dots off. Both are candidate future migrations. **Post-draft phase = minimal interim** (group-phase module set is Prompt 38; playoff/complete deferred). 30-assertion source-contract smoke `dashboard.test.ts`. `landingPage.test.ts` hub assertion updated to check `<Dashboard>` delegation (FEATURES array gone). `feat/dashboard-home-foundation` @ `5b6f5ec`, **merged to `main`**. · **Prompt 38 — dashboard group phase, COMPLETE ✅** (1175 tests, +53). Extended the P37 foundation with the full group phase: pure `selectTournamentPhase(matches[])` (IO-free; `round null = group, non-null = knockout`; no migration) composed with `selectDashboardPhase` via a private `"post-draft"` intermediate; `DashboardPhase` widened to the six-member union (`pre-draft | draft | pre-kickoff | group | playoff | complete`); `loadDashboard` queries `fifa_match` (SELECT only) and calls `loadVsField` READ-ONLY for the group phase. Group modules shipped: `RecordModule`, `StandingsModule`, `MatchdayModule` (all sourced from `VsFieldView`). `PrimaryBanner` extended for all six phases. Playoff and complete remain honest interims (`// STOP(P38)`) pending Guillotine and recap. Gates: 1175 tests ✓, typecheck ✓, lint ✓, format:check ✓, build ✓. **Merged to `main`.** **On horizon: Guillotine bracket unblocks dashboard playoff/complete.** · **Prompt 39 — self-service display-name rename (`/settings` profile + `POST /api/manager/display-name`), COMPLETE ✅** (1213 tests, +38). The App Shell's Settings `TODO(confirm)` seam is now a real minimal route. **New files:** pure helper `apps/web/src/manager/displayName.ts` (`validateDisplayName`: trim + collapse whitespace, reject empty / >40 chars, no charset restriction), framework-agnostic handler `src/manager/handleDisplayNameRename.ts` (mirrors `handleDraftPick` — `resolveManager` injected, `NameTakenError` domain error for the unique-violation, 401/403/400/409/200 status map), thin route `app/api/manager/display-name/route.ts` (maps Prisma `P2002` → `NameTakenError`), migration `20260610120000_manager_display_name_unique` (raw-SQL: `CREATE UNIQUE INDEX "manager_league_id_lower_display_name_key" ON "manager" ("league_id", lower("display_name"))` — case-insensitive per-league uniqueness), `/settings` route (`app/settings/layout.tsx` + `page.tsx` + `settings.css` + `src/settings/SettingsClient.tsx` client island), `src/settings/settings.test.ts` source-contract smoke. **Modified:** `src/shell/crossNav.ts` (`"settings"` added to `NavId` + `NAV_ITEMS`), `AppShell.tsx` (gear glyph), `schema.prisma` (`/// Prompt 39:` comment on `displayName`), `crossNav.test.ts` (updated nav-item list + `selectActiveNav("/settings")`). **Writes ONLY `manager.display_name`** — `app_user.display_name` (vestigial) untouched. **Realtime propagation caveat:** a rename appears to other clients on next server render/navigation; no publication change (not time-sensitive, avoids the RLS-publication trap). **Scoring/engine/draft/lineup/vsfield/worker untouched.** No existing-duplicate failure found on current empty dev DB; operator must verify before production migration (`SELECT league_id, lower(display_name), array_agg(id) FROM manager GROUP BY 1,2 HAVING count(*)>1`). **Live-deploy-only inferences to confirm:** Settings link in the AppShell nav, rename toast on 200, cross-client name propagation on next page load. `feat/profile-rename` off current `main`; **merge held for Chat clearance; no force-push.** · **Prompt 40 — pick'em pool data layer + engine (backend only; NO UI), COMPLETE ✅** (1283 tests, +70). The first of two pool prompts (the **engine + data**; the `/pool` UI + knockout bracket + leaderboard screen + nav + Realtime client are **Prompt 41**). A per-match **1X2 pick'em** (option A, NOT an advancement bracket) scored **separately** from the player engine: +1 per correct pick. **New `packages/pool` (`@app/pool`) is strictly pure** (mirrors `standing.ts` — no IO/clock/DB, grep-proven by `purity.test.ts`): `derivePoolResult` · `scorePick` · `weightForPeriod` (flat 1; escalating-knockout weight is a seam keyed on `period.label`) · `buildPoolLeaderboard` (`{played, correct, points}`, `points desc → managerId asc`) · `isPickLocked` · `validatePickSubmission`. **The load-bearing correction (Chat-caught during grounding):** the prompt's `round == null → group` discriminator is **FALSE** — `mapMatchRow` writes `round` = the matchday number for group (NON-NULL) / raw round name for knockout, and `groupId`/`stageId` are never populated; the literal contract would misclassify all 72 group matches as knockout (DRAW rejected, nothing scores). Fixed by discriminating on **`period.kind`** (`group_md`/`knockout_round`/null), resolved by the IO loader from `fifa_match.periodId → period.kind` — the same signal locking/recompute trust; `periodKind==null → null` (honest unscored, no round-regex guess); the stale `fifa_match.round` schema comment was corrected. **Schema:** `pool_pick` (`PoolPrediction {HOME DRAW AWAY}`; `UNIQUE(manager_id, match_id)`; indexes `(league_id,match_id)`+`match_id`) + migration `20260610130000_pool_pick`. **RLS mirrors `faab_bid`** (auth.uid→manager→league): **league-scoped SELECT** (whole-field reads — `standing_select_league_member` shape, no SECURITY DEFINER since the row carries `league_id`), **own-`manager_id` INSERT/UPDATE**, no DELETE; `pool_pick` added to the `supabase_realtime` publication now (the Realtime-RLS trap — P41's subscription would otherwise be silently empty). **Anti-copying lives in the read QUERY, not RLS** (no clock in RLS): own picks always + others' only for `kickoffAt <= now`. **Theme-F embedded self-test** (cross-league isolation + own-row write) **verified against a UUID-returning `auth.uid()` shim** on a throwaway Postgres — proven the uuid cast is live (non-uuid `sub` → `22P02`) and the predicate discriminates a leaky policy, closing both false-green traps (text-shim cast + owner-bypass); migration applies clean with **zero drift**. **Server path** (`apps/web/src/pool/` + `app/api/pool/pick`, the `/api/faab/bid` template): store port (Memory+Prisma doubles) + framework-agnostic `handleSubmitPick`/`handleReadPicks` → 401/403 (scope "self") BEFORE any write → pure validate → upsert; server `now` authoritative. **Nothing else touched** — no change to scoring / recompute / standings / draft / lineup / vsfield / faab / ingestion / auth signatures. Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1283, +70: pool engine 33 + purity 27 + handler 10) + `apps/web` typecheck all exit 0. **Branch `feat/pool-engine` off `main`; brain files ride with the feature (no separate docs commit); merge HELD for Chat clearance; no force-push.** **Next: Prompt 41 — Pool UI** (pick surface + knockout bracket layout + leaderboard + nav + the Realtime subscription: `realtime.setAuth(token)` before subscribe, gated on `INITIAL_SESSION`, re-subscribe on `TOKEN_REFRESHED`). · **Prompt 41a — Web Push transport + subscription + preferences + Settings UI (INERT sender), COMPLETE ✅** (1415 tests, +132). The first of two notification prompts (the **transport + preference model + UI**; the three triggers are **41b**). Notifications = **Web Push over the PWA** — **NO new vendor** (Render + Supabase only), **NO Realtime** (push is server→device, sidestepping the `postgres_changes`/RLS-publication path entirely — stated explicitly in the migration + DECISIONS). Landed **inert**: `dispatchToManager` is built + unit-tested but **called by NOTHING** (same as the faab/period-close crons landed plumbing-first). **New `packages/notify` (`@app/notify`)** mirrors the faab/draft pure-core + injected-IO-port shape (purity-proven, 77 assertions): pure `buildPushPayload`/`buildTestPayload` + `validatePreferenceInput` + the delivery policy `dispatchToManager` + the `NotifyStore` port + `MemoryNotifyStore` double on the `.` surface; the IO adapters are separate subpaths — `@app/notify/send` (`sendPush`, the web-push/VAPID wrapper) + `@app/notify/prisma` (`createPrismaNotifyStore`). **`dispatchToManager` order = preference gate → subscriptions (early-out WITHOUT claiming if none, so a later subscribe still notifies) → `claimLedger` (idempotency) → fan-out + prune 404/410**; claim-BEFORE-send = **at-most-once** (a transient send failure does NOT re-fire next poll — the chosen trade vs. spamming a lock screen). **Migration `20260610140000_notifications`** (3 additive tables): `push_subscription` (endpoint UNIQUE; SELF-ONLY SELECT/INSERT/DELETE RLS), `notification_preference` (PK manager_id, three bools DEFAULT true, lazily upserted-with-defaults on first read; SELF-ONLY SELECT/INSERT/UPDATE), `notification_sent` (**UNIQUE(manager_id, kind, subject_id)** = the load-bearing idempotency ledger for 41b's pollers; **RLS ENABLED + ZERO policies = default-deny** = "service-role write only, no client read"; history-read policy left a `TODO(confirm)` seam). RLS mirrors the `pool_pick`/`faab_bid` `auth.uid()→manager` idiom but **self-only** (not league-scoped). **Theme-F embedded self-test** (self-only isolation on the two policied tables + structural default-deny on `notification_sent`) **verified against a UUID-returning `auth.uid()` shim on a throwaway Docker Postgres** (catches the `(auth.uid())::text` cast trap), **negative-control-proven** (the harness raises on a forced violation), applies clean with **zero drift**. **Service worker** = plain `apps/web/public/sw.js` (served at `/sw.js`; `push` → `showNotification` from the JSON payload + `notificationclick` → focus/open; **no fetch interception / no caching** — boring + reliable; manifest + Prompt-18 metadata untouched). **Gated routes** (the `handleDisplayNameRename` template — framework-agnostic handler + thin route + injected `resolveManager`, 401/403/400/200): `POST /api/notifications/{subscribe,unsubscribe,preferences,test}`; `/test` sends `buildTestPayload()` via the store's send **bypassing the ledger** (transport probe). **Browser enrolment** (`apps/web/src/notifications/pushClient.ts`) is the injectable `enableBrowserPush(env)` (permission → register `/sw.js` → subscribe with the VAPID key as bytes → POST `/subscribe`) so the permission→subscribe path is **unit-tested in Node with fakes** (no DOM) + `urlBase64ToUint8Array`. **Settings UI** fills the AppShell **Notifications `TODO(confirm)` seam** (touched only that seam): a `NotificationsClient` island (three toggles seeded from `getPreference` + "Enable browser notifications" + "Send test"), ds.css toggles (cobalt accent, no gold). **VAPID env** on **web + worker** (`NEXT_PUBLIC_VAPID_PUBLIC_KEY` build-time+server, `VAPID_PRIVATE_KEY` + `VAPID_SUBJECT` server, all `sync:false`); operator key-gen = `npx web-push generate-vapid-keys` (NOT committed); render.yaml comments note the worker copy is **inert until 41b**. **Out of scope (seams intact):** the three triggers (41b), any worker hot-path edit, email, notification-history UI, any publication/RLS-broadcast edit. Tests (+30): notify pure (preferences 6 + payload 5) + dispatch 8 + purity 77-assert + handlers 14 + pushClient 6 + notifications source-contract smoke + renderBlueprint VAPID + settings smoke updated. Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1415) + `pnpm --filter @app/web build` all exit 0; no out-of-scope churn (scoring/recompute/draft/lineup/vsfield/ingestion/Realtime untouched). **Live-deploy-only inferences to confirm:** the real OS permission prompt, actual delivery to a device, installed-PWA push on iOS (installed-PWA only) / Android (in-browser or PWA). **Branch `feat/notify-transport` off `main`; merge HELD for Chat clearance; no force-push.** **Next: Prompt 41b — wire the three triggers (draft-turn / player-not-starting / match-starting) to `dispatchToManager` in the worker.** · **Prompt 41b — the three triggers wired (sender now live), COMPLETE ✅ (merge HELD).** Each trigger = a **PURE selector** (`apps/worker/src/notify/selectors.ts`, IO-free, 14 tests) + an **IO dispatch** through the unchanged `dispatchToManager` (`triggers.ts`, 8 tests); idempotency is the `notification_sent(manager,kind,subject_id)` UNIQUE ledger **inside dispatch** (selectors stay stateless, re-emit each tick, ledger collapses to one send). **`draft_turn` `${draftId}:${pickNo}`** — piggybacks the existing 2s draft ticker via a new injected **`afterTick`** hook on `startDraftTicker` (so `draft.ts` imports no `@app/notify`); reuses `DraftStore.loadDraft` + the **shared** store the ticker drives → sees the post-autopick pointer; catches turns advanced by human pick **or** autopick without touching the controller; one per turn, **no on-deck**, drops out on complete. **`player_not_starting` `${matchId}:${playerId}`** — hooks the pre-match `match_lineups` pull: `ingestLineups` now **returns the official-XI starter BDL ids** it already fetched (no 2nd feed call), the worker compares vs the match-period's fantasy **is_starter** slots and alerts owners of any starter **not in the XI + still unlocked** (`locked_at` null) — the high-value, time-sensitive one (swap window before the reserve's kickoff). **`match_starting` `${matchId}`** — each 60s scheduler tick, **owners-only** (whole roster, either team) for fixtures kicking off within **`NOTIFY_MATCH_LEAD_MIN`** (default 15; `[now,now+lead]`, past-kickoff excluded). New **worker-local trigger-read port** (`notify/store.ts` + `prismaStore.ts` + `memoryStore.ts`) holds the two reads `@app/ingest` shouldn't (keeps lock derivation IO-free); roster ownership **global** (one-league assumption, noted in adapter). Each dispatch **isolated** (try/catch) so a notify failure never starves the autopick/ingestion loops. **Hook points:** `index.ts` `afterTick`→`dispatchDraftTurns`; `scheduler.ts` pre_match→`dispatchPlayersNotStarting`, tick-tail→`dispatchMatchStarting`; `wiring.ts` constructs the notify+trigger+shared-draft stores. **Added `@app/notify` to the worker `package.json`** (the 41a "worker already depends" note was VAPID-env-only, not a dep). **Grep-clean:** `packages/draft` core + `packages/ingest/lock.ts` IO-free; Supabase/clock/web-push confined to the worker IO layer. **SCORING.md untouched** (no scoring change). Test delta **+24** (1415→1439): selectors 14, triggers 8, ingest `ingestLineups`-returns-XI 1, draft `afterTick` net +1. Gates: `pnpm typecheck` (all packages) + `lint` + `format:check` + `test` (1439) + `pnpm --filter @app/web build` all exit 0. **Live-only inference:** actual device delivery (no push service in CI) — confirm on deploy. **Branch `feat/notify-triggers` off `main`-state (`bafd86c`, post-41a-merge); merge HELD for Chat clearance; no force-push.** **Deferred follow-up (flagged):** desktop Web Push registration/delivery is unverified — deferred; mobile (installed PWA, iOS confirmed) delivers; server-side send is sound (proven by real mobile delivery) — the desktop gap is most likely an unregistered desktop subscription / local browser-SW quirk, not a server fault. This was the **last notifications prompt** — nothing further flagged. · **Prompt 42 — `/pool` pick'em UI (picks + leaderboard; NO Realtime), COMPLETE ✅ + MERGED** (1462 tests, +23). The user-facing surface on the P40 engine: a `/pool` route with a **Picks** tab (group → matchday lists, 3-way Home/Draw/Away; knockout → the fixed R32→Final **bracket skeleton** in knockout phase only, 2-way advancer, empty rounds = honest **TBD**, no fabricated matchups) + a **Leaderboard** tab (**all league members**, non-pickers padded 0/0/0, ranked **points desc → name → id**). Reads on load/refetch — **form-driven CRUD** through `POST /api/pool/pick` then `router.refresh()`; **NO Realtime, NO polling** (Realtime client deferred to **P43**). New pool-owned files only (`apps/web/{app,src}/pool/`): pure `poolView.ts` (`selectPoolPicksView` + `buildPoolLeaderboardView` left-joining membership over the **untouched** `buildPoolLeaderboard` + `isFixtureLocked`; 15 tests) + IO `loadPool.ts` + `PoolClient.tsx`/`components.tsx`/scoped `pool.css` (tokens only, zero hex, lock-on-play steel pill, `<Flag>` reuse, `.dtable` board) + 8 source-contract smokes. The group↔knockout split + result derivation key off **`period.kind`** (`round` only feeds the reused P38 `selectTournamentPhase`); the reveal gate (own-always / others-post-kickoff) stays owned by the **P40 store's `readVisiblePicks`** — the UI never bypasses it (the leaderboard's separate all-league read is safe: aggregates only completed matches). **Engine + RLS untouched; SCORING.md untouched.** Gates: `pnpm -w typecheck && lint && format:check && test` (1462) all exit 0. **Merged to `main` @ `527ab1a`.** **Pending:** the "Pool" **nav entry** (deferred — `/pool` reachable by direct URL only; "pool" isn't a `NavId` yet, so the layout passes a non-member `active` and nothing falsely highlights) + **Realtime (P43)**. · **Prompt 43 — `/pool` live updates (clock-reveal + leaderboard poll; NO Realtime), COMPLETE ✅ + MERGED** (1492 tests — the green merged-state count of record; the per-prompt deltas were muddled by the worktree race below, so 1492 is the number of record). Makes `/pool` feel live with **no `pool_pick` subscription** — two **on-read** mechanisms, no schema/migration, no stored score table, no `postgres_changes`. New pure `apps/web/src/pool/poolLive.ts` (IO-free, injected timers/visibility/clock — mirrors draft `resilience.ts` + vsfield `liveController.ts`, fake-timer unit-tested in Node): **`nextRevealInstant`** (soonest STRICTLY-future kickoff, mirrors the server gate `kickoffAt <= now`; coalesces shared instants) + **`startRevealClock`** (one timer → on fire `router.refresh()` the gated loader → self-reschedules; mount-once effect, fixtures via ref so no double-schedule) + **`startLeaderboardPoll`/`handleLeaderboardVisible`** (60s visibility-gated poll: immediate on tab-activate + on `visibilitychange→visible`, paused on the Picks tab / when hidden). `PoolClient.tsx` wires both effects; the **reveal-leak guard** is reaffirmed under the new refetch paths (others' picks come ONLY from the gated `fixture.others`; no Realtime client — a `postgres_changes` frame would bypass the clock-gated read and leak pre-kickoff picks, and revealable ⇒ locked ⇒ nothing live to stream). The **deferred P42 nav entry was wired separately** (`feat/pool-nav` → `main`, the P17 cross-nav pattern). Engine + RLS + SCORING.md untouched; `/pool` stays `ƒ`; dormant P40 publication left in place. Gates: `pnpm -w typecheck && lint && format:check && test` all exit 0. **Merged to `main`** (see the entangled-merge note below). · **Prompt 44 — draft positional caps lifted (shape-unconstrained to 15), COMPLETE ✅ + MERGED.** The snake draft no longer enforces per-position ceilings — `@app/draft`'s `isPositionLegal` / `isSquadComplete` are now **total-based** (new **`squadTotal(counts)`** helper; gate purely on `squadTotal < SQUAD_SIZE` / `>= 15`), so a manager may draft any shape up to the 15-man total. **Lineup/formation bounds are UNCHANGED** (exactly 1 GK, min 3 DEF / 2 MID / 1 FWD — DECISIONS Theme B), so managers must still self-assemble a fieldable XI; an over-drafted squad can be **locked out of a legal lineup by choice**. `PositionFullError` is **retained but defensive/unreachable** in the snake flow (squad-full now coincides with draft completion at 15×N). Touched `packages/draft/src/roster.ts` + the autopick/controller/roster tests. **Merged to `main`.** · **Docs/process note (P43 + P44 merge):** P43 and P44 entered `main` via a **single entangled merge (`5239b46`)** caused by a shared-working-tree branch race (a concurrent session switched the checked-out branch mid-prompt, so the P43 commit briefly rode the draft branch); both changesets are independent and were **reviewed and cleared post-hoc**. Going forward, parallel prompts run in **isolated `git worktree`s** to prevent the race. This brain-file catch-up (`docs/brain-p42-44`) records the deferred P42/P43/P44 deltas in one docs commit. · **Prompt 45 — period-close cron now live, COMPLETE ✅** (1509 tests, +17). The inert `wc-fantasy-period-close` hourly cron placeholder (P12) is replaced with real freeze logic. Pure IO-free `selectPeriodsToFreeze` + `selectAnomalyPeriods` in `packages/recompute/src/freeze.ts` (the existing period-domain package): a period is freezable when every fixture is `completed` AND `now >= max(kickoffAt among fixtures) + league.resultFreezeHours`; threshold uses `kickoffAt` as the FT proxy (`fifa_match` carries no settled-at timestamp — Chat-confirmed, keeper comment in source); `frozen_at` stamped as `now` (the actual freeze instant, not the threshold — Chat-confirmed). Anomaly periods (any `postponed`/`abandoned` fixture) are skipped and logged as warnings for manual commissioner override — never frozen. Idempotent: the DB query excludes `frozenAt IS NOT NULL` periods; the pure fn is additionally defensive. The thin cron body (`periodClose.ts`) reads the **runtime** `league.resultFreezeHours` DB row (not a seed default or env var), loads unfrozen periods + their fixtures, calls the pure fn, and stamps `frozen_at = now` in one atomic `$transaction`. Shape mirrors FAAB split exactly: pure logic in the domain package, thin IO in the cron. All DoD gates exit 0; cron schedule untouched; `feat/period-close` @ `96caf15`, merge HELD for Chat clearance. · **Prompt 46 — player avatars (generated; flag + initials + position color), COMPLETE ✅** (1547 tests, +55). A single shared presentational `<PlayerAvatar>` (`apps/web/components/PlayerAvatar.tsx`) gives every player card a deterministic generated avatar — **no real photos, no network fetch, no new dependency**. Given `{ displayName, firstName?, lastName?, country, position, size? }` it renders: **(1) position-color disc** via `player-avatar pos-${position}` CSS classes (reuses `--pos-gk/def/mid/fwd` tokens — cobalt/emerald/coral/slate; GK = slate with white text; no gold); **(2) player initials** via the new pure `playerInitials` helper (`src/draft/playerInitials.ts` — priority: `firstName+lastName` → display-name whitespace split → first 2 chars of single word; 8-case unit-tested); **(3) country flag badge** (`.pa-flag`, bottom-right corner, `overflow:visible` on `.player-avatar` to allow badge protrusion) via the P35/36 resolver — **consumed, not reinvented** (`toIso2`+`flagEmoji`+`isHomeNation`): home nations (England/Scotland) render an inline SVG at badge scale (`.pa-flag-svg` CSS-sized per `avatar-sm/md/lg`), all others render the emoji string, null/unknown country → neutral no-badge fallback, **never a broken image** (no `<img>` tag anywhere). **`.flag-emoji` promoted from route-scoped `draft/draft.css` to the global `app/styles/ds.css`** (shared across all avatar and flag surfaces); all 5 byte-identical `ds.css` copies synced. **Wired into 4 surfaces** (presentation-only, zero engine changes): `/draft` pool rows + queue items + roster pcard (`components.tsx`, replacing the per-surface `<CountryFlag country=…>` + `<Pos>` combination); `/lineup` `PitchToken` + `BenchRow` (`lineup/components.tsx`); board ticker/cell (`dr-tick`/`dr-cell`) kept unchanged — too compact. **Tests:** `playerInitials.test.ts` (8 unit) + `playerAvatarWiring.test.ts` (18 source-contract smoke: component structure, no hardcoded UI hex, pa-flag, SVG home nations, draft/lineup wiring) + updated `flagWiring.test.ts` (CountryFlag pool-row assertion → board-cell; `.flag-emoji` check → `ds.css`). No migration, no schema change, no scoring/engine/ingest/worker/Realtime change. `feat/player-avatars` @ `e4e13b9`, merge HELD for Chat clearance. · **Decisions amendment thread (Theme B + Theme D), LOCKED ✅ (docs-only).** Two rule changes locked this thread, appended to `DECISIONS.md` in the amendment-trail style (the superseded text is retained, not deleted): **(Theme B) in-matchday substitutions** supersede the lock-on-play bidirectional freeze — sub-IN preserved (unplayed only), sub-OUT now permitted as a played-starter-out / unplayed-bench-in swap that **forfeits the outgoing slot's banked period points via `is_starter=false`** (the incoming sub scores that slot); cap = bench size (4 group / 2 playoff), formation bounds unchanged, no auto-subs; **(Theme D) a per-matchday acquisition window** (one blind-bid `resolveFaabBatch` before each scoring period + $0 free agency until the period's first kickoff + a **hard league-wide lock** at that kickoff) supersedes the daily two-tier FAAB cycle and the per-player-kickoff acquisition deadline. `SCORING.md` gets one cross-reference line (Core principle #6) — **no point values change**. **`ARCHITECTURE.md` updates are DEFERRED to the two implementation threads** (`TODO(theme-B-impl)` / `TODO(theme-D-impl)`): the scoring engine (`resolveFaabBatch`) is unchanged; the knock-ons are cadence/scheduling (daily cron → per-period batch trigger) and a **directional `lineup_slot` lock latch** (block promote-IN, permit validated demote-OUT). Docs-only; no code, no tests, no merge. · **Prompt 47 — FAAB per-matchday cadence (Theme-D amendment, implementation), COMPLETE ✅** (1570 tests). Swapped the FAAB cadence from the retired daily 06:00 cron to **one blind-bid batch per scoring period, cleared before that period's first kickoff** — the clearing algorithm (`@app/faab` `resolveFaabBatch` + its 70-case purity matrix + `resolve.ts`) is **byte-unchanged**; only *when* it runs moved. **New pure cadence selectors** (`apps/worker/src/faab/selectors.ts`, mirrors the notify-selectors pattern): `selectPeriodsToClear` (periods whose `effectiveBatchAt = waiver_batch_at ?? first_kickoff − lead` has passed AND `batch_cleared_at IS NULL`), `acquisitionWindowState` (sealed-bid → free-agency → locked), `effectiveBatchAt`. **Worker-tick driver** (`dispatch.ts` + a `FaabCadenceStore` port + Memory/Prisma adapters): runs `runFaabBatch` once per due period and stamps `period.batch_cleared_at` — **idempotent via the latch** (not a flag), per-period try/catch isolation, wired into `scheduler.ts` after the recompute sweep. **Retired the daily cron**: deleted `apps/worker/src/jobs/faabBatch.ts` + the `job:faab` script + the `wc-fantasy-faab-batch` render.yaml block (⚠️ **Render-side: Sergio must delete/suspend the live cron service** — render.yaml removal doesn't auto-delete it; flagged in the retired-block comment + the renderBlueprint test updated to 4 services / 1 cron). **Acquisition cutoff → the period's first kickoff (league-wide)**: `@app/faab` `validateBidSubmission` gate renamed `addTargetKickoffAt → acquisitionCutoffAt` and `PlayerFacts.kickoffAt → periodFirstKickoffAt` (the Prisma `getPlayerFacts` resolves the add target's fixture → its `period_id` → MIN(kickoff) in that period); the **batch keeps the per-player kickoff** for the resolver's **defensive void-refund** (now unreachable in normal flow). Theme-B sub-IN eligibility untouched. **Schema**: `period.waiver_batch_at` (commissioner-configurable per period; default `first_kickoff − FAAB_BATCH_LEAD_MIN`, **chosen 360 min / 6 h**, `// TODO(confirm): batch lead`) + `period.batch_cleared_at` (latch); migration `20260610150000_period_faab_cadence` — additive nullable columns, **no RLS self-test** (`period` carries no RLS; the `draft_timer_enabled` column-add pattern). **`// TODO(confirm)`: the $0 first-come FA grant surface does NOT exist** (a $0 bid is a sealed bid cleared by the batch — Prompt 25); built the pure window predicate as the ready building block, did not guess at the unbuilt grant path. The **1-cycle waiver hold was never code** (free agents = the owned-complement in `loadWaivers`), so its removal is a no-op + documented; mid-window drops are held to the next batch **by construction** (no mid-period immediate grab exists). Tests +23 (selectors 14 + dispatch 4 + validate +2 + handleBid/blueprint renames). Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1570) all exit 0; `resolve.ts` / `resolve.test.ts` / `purity.test.ts` confirmed **byte-unchanged**. **ARCHITECTURE.md §3 updated** (the deferred Theme-D scheduler + acquisition-window model; §2 topology, §4 faab_batch + period columns, §8 jobs). `feat/faab-per-matchday` off `main` (`f48623c`); **migration applied on deploy** (`prisma migrate deploy`); merge HELD for Chat clearance; no force-push. · **Prompt 48 — instant $0 free-agency grant (Theme-D amendment; stacked on 47), COMPLETE ✅** (1592 tests). Builds the **$0 FA pickup**: between a period's batch-clear and its first kickoff, any manager grabs an unclaimed player for **$0, applied immediately** — no bidding, no waiver order. **`resolveFaabBatch` + its 70-case purity matrix byte-unchanged.** **Shared window predicate** moved worker→`@app/faab/window.ts` (`acquisitionWindowState` + `PeriodWindowView`; `apps/web` can't import `apps/worker`); worker `selectors.ts` re-exports it (P47 surface preserved), tests moved to `window.test.ts`. **Gated route `POST /api/faab/free-agent`** on the bid-route template: extracted shared **`faabGate`** (`apps/web/src/faab/gate.ts`, now used by both handlers) → **401/403 before any write** → `handleFaGrant` runs the **free-agency window** gate + **snapshot eligibility** + the **shared drop/roster rules** (`validateFaGrant` + the new pure `checkDropAndRoster` extracted from `validateBidSubmission`, both purity-guarded in `validate.ts`) → atomic **first-come claim**. **FA eligibility = the BATCH-CLEAR SNAPSHOT, not live-unowned** — a player dropped during the window is held to the next period's batch (anti-snipe). **Mechanism chosen: NO snapshot table** — the single immutable predicate `NOT EXISTS roster_player WHERE player=X AND (dropped_at IS NULL OR dropped_at >= period.batch_cleared_at)` (holds batch winners/droppees, mid-window FA drops, and the claimed-then-dropped race; equivalent to an `fa_open` marker but derived from history + the existing `batch_cleared_at` — no extra write, no snapshot-idempotency problem). **First-come concurrency** = the existing **`roster_player_active_ownership_uq`** partial unique (`WHERE dropped_at IS NULL`): two parallel grabs → one INSERT wins, the loser gets a clean **`fa-conflict`** (409, tx rolled back) + an in-tx eligibility re-check catches the claimed-then-dropped race the unique alone misses. **$0 — budget unchanged, no waiver-order mutation.** Drop-lock reused; dropped-player UNLOCKED `lineup_slot` release via the `@app/lineup` boundary (same as the batch). **1-cycle waiver hold removal = no-op** (never was code). **NO new schema** (history-derived eligibility + the existing unique). New `@app/faab` `FaGrantStore` (store port + Memory double + Prisma adapter `createPrismaFaGrantStore`) + `FaGrantError` family (`fa-window-closed` / `fa-not-eligible` + reused drop/roster). Tests +22 net (window 5 + validateFaGrant 10 + handleFaGrant 11 − moved selectors 4); the `handleBid` gate-extraction refactor stays green. Gates: `pnpm db:generate && -w typecheck && lint && format:check && test` (1592) all exit 0; `resolve.ts` / `purity.test.ts` confirmed **byte-unchanged**. **ARCHITECTURE.md §3** updated (FA grant surface + snapshot rule). `feat/faab-fa-grant` stacked off `feat/faab-per-matchday` (**47+48 merge as one unit**); merge HELD for Chat clearance; no force-push. · **Prompt 49 — waivers screen shows the REAL per-period batch time (display-only), COMPLETE ✅** (1610 tests). The "next batch" element no longer renders the daily/illustrative placeholder; it shows the **actual instant the worker fires**, in `league.timezone` (Intl `timeZoneName:"short"` → reads ET/EDT). **One source of truth:** `effectiveBatchAt` + `PeriodCadenceView` + a new `DEFAULT_FAAB_BATCH_LEAD_MIN` constant **extracted worker→`@app/faab/batchTime.ts`** (same `apps/web` ∌ `apps/worker` boundary that moved `acquisitionWindowState`); worker `selectors.ts` **re-exports** them (P47 surface + `selectPeriodsToClear` unchanged), worker config default now anchors to the shared constant, the `effectiveBatchAt` unit tests **moved** to `batchTime.test.ts`. **Web render (server-side):** `loadWaivers` picks the current period (open→soonest-pending, the `loadVsField` selection) + its MIN-kickoff fixture, derives the phase via `acquisitionWindowState` + the time via `effectiveBatchAt`, and maps to copy through the new **pure `buildBatchWindowView`** (`waiversLogic.ts`): **sealed-bid** → "Waivers process at {t}" + the existing live countdown; **free-agency** → "Free agency open — locks at {firstKickoff}"; **locked** → "Waivers locked for {label}". New `<BatchBar>` reuses the design's `wv-batchbar` + ported `wv-batchbar-r/-cd` CSS + the existing `fmtCountdown` (data wiring, not a re-skin); `batchLocalTime`/`faabBatchLocalTime` placeholder dropped. **`resolveFaabBatch` / `resolve.ts` / the purity matrix byte-unchanged** (display-only; out of scope). Pure mapping unit-tested for all 3 phases in a **non-UTC** league tz. Gates: `-w typecheck && lint && format:check && test` (1610) all exit 0. **ARCHITECTURE.md §4** updated (effectiveBatchAt now shared in `@app/faab`). `feat/waivers-batch-time-ui` off `main` (47+48+49 merged); merge HELD for Chat clearance, then live-deploy verify; no force-push. · **Prompt 51 — lineup + waivers polish (4 parts, display/filter only), COMPLETE ✅ (merge HELD)** Four independent commits on `feat/lineup-waivers-polish` off `main`@`4dd6cd5`; the FAAB engine / scoring / resolver / purity matrix are **byte-untouched**. **(1)** the set-lineup matchday selector now sorts by a new canonical `@app/shared` source (`periodOrderRank` / `comparePeriodLabels` / `sortByPeriodOrder`: group MD# then the knockout bracket R32→R16→QF→SF→Final, reusing `KNOCKOUT_ROUNDS`) — fixes the alphabetical-fallback bug (`opens_at ASC, label ASC` → "Final, QF, R16, R32, SF" when `opens_at` is null). **(2)** the draft pool's collapsible country filter is factored into a shared `<NationFilter>` (apps/web/components) and mounted on the waivers FA picker (`claimableFreeAgents` gains a `nation` filter + `freeAgentNations` chip list), reusing the sole `<Flag>`/`toIso2` surface. **(3)** country flags render on every waivers player (FA picker / drop picker / edit panel / claims / results) via a `NationFlag` over the same `<Flag>`; the loader now derives `nation` from the **fifa_team join** (player.country is unwritten — matches draft/lineup) so flags resolve; kit chips stay background-image-free. **(4)** every squad player's fixture **date + time** (all 15) shows on the pitch token + bench row as the lock/sub deadline, formatted in the league tz — this **closes the Prompt-10 launch-gate seam** (`kickoffByPlayer` `TODO(confirm)` — `fifa_match.kickoff_at` via `player.teamId`): `formatInLeagueTz` lifted to `@app/shared` (waivers reuses it), pure `resolveKickoffByPlayer`/`kickoffByTeam` in `src/lineup/view.ts` (team→period fixture; resolve-miss → "TBD"), `loadLineup` fills it per period + threads `league.timezone`. Gate `typecheck && lint && format:check && test` (1638) all exit 0 (+28). Merge HELD for Chat clearance; no force-push. · **Prompt 52 — draft complete-state shows the full read-only board, COMPLETE ✅ (merge HELD).** After the draft completes, `/draft` now shows the full snake board (`<Board>` + `<RosterPanel>` squad recap in the rail) instead of only the personal squad overlay. Presentation-only — `loadDraftRoom` already hydrates all picks + managers; `buildBoard` is pure + already gates `isCurrent` on `status=active`; no loader, Realtime, or data-shape change. Mobile reuses the `.dr.show-board` CSS toggle for a "Board / Your squad" tab pair. +5 tests (1643). `feat/draft-complete-board`; merge HELD for Chat clearance; no force-push. · **Prompt 53 — per-player opponent label on the lineup screen, COMPLETE ✅ (merge HELD).** Each squad player now shows their fixture opponent — "vs 🇫🇷 France" (home) / "@ 🇫🇷 France" (away) — directly below the kickoff date+time on both the pitch token and the bench row. Display-only; engine, resolver, recompute, purity matrix, and Realtime contract are **byte-untouched**. Implementation: `resolveOpponentByPlayer` (new pure function in `src/lineup/view.ts`) mirrors `resolveKickoffByPlayer`'s earliest-kickoff tie-break so kickoff and opponent always reference the same `fifa_match` row and can never diverge; the `fifaMatch` Prisma query in `loadLineup` is extended once to also select `homeTeam.name` / `awayTeam.name` (one read, two outputs); `OpponentInfo { opponentName, opponentNation, isHome }` added to `src/lineup/types.ts` + `opponentByPlayer` field to `PeriodLineup`; `<OpponentTag>` in `app/lineup/components.tsx` renders "vs/@ + Flag + name" via the sole `<Flag>`/`toIso2` surface (no new flag IP). Null when the player's team has no fixture this period or either side is TBD (knockout bracket not yet determined) — renders "TBD" with no flag, only when `kickoffAt` is also non-null (no double-TBD). +4 tests (1647). `feat/lineup-opponent-label`; merge HELD for Chat clearance; no force-push. · **Prompt 54 — period-select fix: `opensAt`-NULL sort → `selectCurrentPeriod` with per-screen isCurrent latch (waivers + vsfield), COMPLETE ✅ (merge HELD).** Root cause: `period.opensAt` is never populated by the provisioning CLI — `ORDER BY opensAt ASC, label ASC` degrades to alphabetical, so "Final" (F) sorts before "Group MD1" (G) and both `/waivers` and `/vsfield` resolved to the Final period on opening day. **Fix:** new pure `selectCurrentPeriod<T>(periods, isCurrent: (p: T) => boolean)` in `@app/shared/periodOrder.ts` (same module as P51's `sortByPeriodOrder`) sorts in JS by `matches[0].kickoffAt` then applies a per-screen `isCurrent` predicate. **Per-screen latches differ:** waivers `= p => p.batchClearedAt === null` (batch hasn't run yet); vsfield `= p => now < matches.at(-1).kickoffAt + 120min` (at least one match still live). The split is required because `batchClearedAt` is stamped **~6h BEFORE first kickoff** (`effectiveBatchAt = firstKickoff − 360min`) — using it as the vsfield latch would drop the live wave while MD1 matches are being played, binding the Realtime subscription and lineup/score reads to MD2 instead. `period.status` never transitions (always `"pending"` from provision to freeze); the `status === "open"` fast-path is retained future-safe. `loadWaivers` + `loadVsField` both updated; `selectCurrentWaiverPeriod` removed from `waiversLogic.ts` (hoisted). vsfield period query extended to fetch all matches (removed `take: 1`) so `matches.at(-1)` gives the last kickoff. `resolveFaabBatch` / `resolve.ts` / purity matrix byte-unchanged. `// TODO(confirm)` for overlapping group waves (sequential periods only). +7 tests (1658 total). `feat/waivers-period-select-fix`; merge HELD for Chat clearance; no force-push. · **Prompt 54 — set-lineup formation picker + roster-fillability filter (LAUNCH-BLOCKER), COMPLETE ✅.** Repro: MR. ZETTA's squad (1 GK / 3 DEF / 7 MID / 4 FWD — legal under the Prompt-44 total-only draft cap) could not field a legal XI. The set-lineup default was a hardcoded 4-DEF shape (`DEFAULT_FORMATION = {GK1,DEF4,MID4,FWD2}` in `apps/web/src/lineup/view.ts`); with only 3 DEF the 4th DEF slot never filled → 10 starters, 5 bench → "starting XI must have exactly 11 players (got 10)". His squad IS fieldable (3-4-3 / 3-5-2) but there was **no formation control** in the live UI (the carried `FormationPicker`/`reshape()` TODO) and the default was blind. **Closes that carried TODO.** Fix = pure helpers added to `view.ts`: `GROUP_FORMATIONS` (the DECISIONS Theme-B standard 7), `formationFillable` (GK≥1 + roster supply ≥ shape per pos — the gap `validateLineup` never covered: it checks a proposed XI, never roster supply), `formationLockLegal` (live port of the design's `formationLegal`), `offeredFormations` = fillable ∩ lock-legal, `reshapeToFormation` (keeps locked starters, promotes only MOVABLE reserves — never a played bench man), `formationKeyOf`, `defaultFormationKey` (canonical 4-3-3 if fillable, else first fillable in canonical order). `defaultStarterIds` now builds from the fillable default (a 3-DEF squad opens on **3-4-3, savable immediately**); a persisted lineup still loads its OWN saved shape (never overridden). New `<FormationPicker>` (segmented `.tabs`, offered shapes only) wired in `SetLineupClient` → `reshapeToFormation` → the **existing** swap + `evaluateProposal` + `submitLineup` flow. **NO new write path; `@app/lineup` validator + `POST /api/lineup` byte-untouched; no engine/RLS/Realtime change.** +14 tests (`formation.test.ts`; 1672 total), all gates green. `feat/lineup-formation-fillable`; merge HELD for Chat clearance; no force-push. · **Prompt 54-fix — render the FormationPicker as a REAL control + prove it (the P54 lesson), COMPLETE ✅.** Correction to the above: P54's picker *logic* (the pure helpers, 14 units) was written but the branch was **HELD/never merged**, so the deployed `main` never had the picker at all — a manager (Los Dragones) saw only the read-only `LockHero` FORMATION label and tapping it did nothing. Root cause = held branch + the control was only **source-contract-smoke**-tested (a smoke can't prove a control renders/taps — the lesson). This prompt re-bases on current `main`, and: (1) renders `<FormationPicker>` **directly under the FORMATION panel** (moved from inside `.sl-pitchcol`), populated by `offeredFormations` (fillable ∩ lock-legal); (2) when `offered.length <= 1`, renders a **static indicator** (no dead single-tab control) — chosen over a disabled tab; (3) updates the help copy to name the picker. **Replaced the 3 contract smokes with a real RTL/jsdom interaction test** (`apps/web/app/lineup/FormationPicker.test.tsx`, mounts the actual `SetLineupClient`): all offered shapes render, selecting a non-active one reshapes + re-validates (Save stays enabled), unfillable shapes absent, single-shape → static. Required NEW test infra (the repo was Node-only): `jsdom` (root), `@testing-library/react` + `@testing-library/dom` (apps/web, shares React 19), `oxc: { jsx: "automatic" }` + `.tsx` include globs in `vitest.config.ts`, per-file `// @vitest-environment jsdom`. Verified with a headless-browser screenshot of the real component (esbuild bundle + Playwright). Still NO new write path; validator + route byte-untouched. 1672 tests (123 files), all gates green. `feat/lineup-formation-picker-render` off `main`; merge HELD for Chat clearance; no force-push. · **Prompt — FA-grant Waivers UI ($0 free-agency surfaced), COMPLETE ✅ (merge HELD).** Prompt 48 shipped + tested `POST /api/faab/free-agent` but **no UI ever called it** — the Waivers tab rendered only the sealed-bid claim form, so in a period's free-agency window the only action was a sealed bid that wouldn't clear until the next batch (verified live: MD1 in free-agency, 1077 snapshot-eligible). **Lesson: route-level tests ≠ a working user path** (same class as P54's FormationPicker). Wired (UI-only): new `FreeAgentPanel` (instant Add/drop, reuses the composer's `droppableRoster` + `claimableFreeAgents` + `KitChip`/`NationFlag`/`Pos`) + `WaiversClient.handleGrant` → `POST /api/faab/free-agent` → `router.refresh()`; phase switch on the SAME `acquisitionWindowState` the BatchBar uses (sealed-bid → sealed form unchanged; free-agency → FA list; locked → Add disabled). Loader now offers the **snapshot-eligible** pool via new `listFaIneligiblePlayerIds` (`@app/faab/prisma`), the FA predicate factored into ONE `snapshotOwnershipWhere` shared with `getFaTargetFacts` (list + grant can't drift; a stale list only → `fa-conflict` 409, surfaced inline). HARD STOPS held: `resolve.ts` / purity matrix / scoring + the route / `validateFaGrant` / `claimFreeAgent` byte-untouched; no schema change. Closed the missing **end-to-end** gap: an RTL/jsdom test mounts the real `WaiversClient`, drives Add → asserts the `/api/faab/free-agent` POST + refresh + the `fa-conflict` path; added a `@/` alias to `vitest.config.ts` so jsdom mounts resolve app-router `@/` imports. **1680 tests (124 files), all gates green** (typecheck/lint/format/test; baseline was 1672). `feat/faab-fa-ui` off `main`; merge HELD for Chat clearance; no force-push. · **Prompt — Commissioner-override CLI (`commish:roster` / `commish:lineup`), COMPLETE ✅ (stacked on feat/faab-fa-ui, merge HELD).** Render-Shell tool letting the commissioner repair "our-fault" moves the missing FA UI blocked (operator side of the same incident). `apps/worker/src/commish/` — dry-run by default, `--apply` to execute. THE DESIGN = the bypass/enforce line: `commish:roster` bypasses the FA/waiver **window** (calls `claimFreeAgent` directly; reuses `validateFaGrant` with window/snapshot/drop-lock neutralized) but KEEPS roster cap + valid-drop + active-ownership unique + slot-release ($0, no budget/waiver-order change); `commish:lineup` bypasses ONLY the lineup edit-window lock (`relaxPeriodLock` → `validateLineup` window no-op) but KEEPS formation/position legality + ownership + 11-XI + the lock-on-play latch (+ DB trigger, unbypassable). Both ALWAYS enforce: commissioner gate (`is_commissioner` or smrios07@gmail.com), a required `--reason`, idempotent skip, and a structured `commish-override {json}` audit line per applied action. Per-player **kickoff guard ON by default** (blocks an already-played add; explicit `--allow-post-kickoff` honors it + logs LOUDLY — the post-kickoff integrity caveat). Names resolved team→managerId (manager.displayName) + player→id (displayName/first/last), ambiguity is an ERROR with candidates printed. STOP SEAMS held: engine/route/schema untouched, **no migration** (audit = stdout); `claimFreeAgent` + `@app/lineup` service reused, validation never re-derived. Pure core + injected-deps orchestrators unit-tested (resolver/ambiguity, gate refusal, kickoff default-block vs allow, cap kept under window-bypass, formation legality kept under lock-bypass, dry-run applies nothing); IO `cli.ts` tsc-covered like the provision CLI. Added `@app/lineup` dep to apps/worker. **1712 tests (126 files), all gates green.** `feat/faab-fa-ui` 3rd commit; merge HELD for Chat clearance; no force-push.
**Commish follow-up — period-pin + lock-on-play carve-out, COMPLETE ✅ (3 of 4; gate held #4).** Applying the MD1 "our-fault" repairs surfaced four fixes. **Shipped:** (1)+(2) `commish:roster --period "MD1"` pins BOTH the FA snapshot (`claimFreeAgent`→`resolveAddPeriodWindow` pinned branch reads that period's `batch_cleared_at`) and the kickoff guard (`getAddMatch` filters fixtures to that period) — fixes an already-played-MD1 add wrongly rejected as `fa-conflict` / falsely reading "not yet kicked off" because its next fixture is the still-sealed MD2; optional + commissioner-only, unpinned byte-unchanged, recorded in plan + audit. (3) `commish:lineup --allow-locked-slot` relaxes the lock-on-play latch in all three places (`validateLineup` phase-4 empty `lockState` + `saveLineup` re-check + the **DB trigger** via a tx-local `app.commish_override` GUC) while KEEPING formation/ownership/XI/gate/reason; ONE migration `20260611120000_lock_on_play_commish_override` (embedded self-test + throwaway-Postgres verified; `search_path` stays pinned). **HELD:** #4 (remove the FAAB FWD cap) — the DECISIONS gate found it is the *still-valid* FAAB `2/5/5/3` cap, NOT the retired *draft* cap (Prompt 44 was draft-only); it needs a separate commissioner decision first, so `checkDropAndRoster`/`resolve.ts` are byte-unchanged. STOP SEAMS held (engine/purity/scoring untouched; the trigger migration is the only schema touch). **1719 tests (126 files), all gates green.** `feat/commish-fixes`; merge HELD for Chat clearance; no force-push.

**Prompt 52 — Player box-score modal + Set-Lineup score breakdown, COMPLETE ✅ (1844 tests, 133 files). Post-deploy visual confirmed on deploy 2026-06-12 (modal open/render + ScorePill tap flow observed live on Set Lineup and /vsfield).**
Three pieces shipped together on `feat/player-box-score`:

1. **Pure view-model package `@app/player-box`** (`packages/player-box/`). `buildPlayerBox(input)` maps
   injected DB rows → `PlayerBoxView` (header / `BoxState` / `SectionView[]` / `TrackedStatRow[]` /
   season). `CATEGORY_META` maps all 27 `SCORE_CATEGORIES` keys to SCORING.md §1→§8 sections +
   display tags. Nation derived from `player.nation` (which the loader sources from the
   `player→team→fifa_team.name` join — `player.country` is never written by ingestion and must not be
   used). Season total is always `null` from the pure function; the API route injects the real aggregate.
   59 exhaustive tests + purity guard; zero IO.

2. **`GET /api/player-box?periodId=&playerId=`** (`apps/web/app/api/player-box/`). Authed server read:
   `getSessionManager()` → 401/403; Prisma owner-bypass (`import "server-only"`); no new RLS migration.
   Four parallel Prisma reads (`score_player_match`, `stat_player_match`, `fifa_match`, season aggregate).
   Server resolves match from `(playerId, periodId)` — the client only passes `activeId` (periodId), not
   a match ID. Pure `handlePlayerBox` handler tested independently (auth postures + 404 + 400).

3. **`ScorePill` + `PlayerScoreSheet` + Set-Lineup wiring** (`apps/web/app/lineup/`).
   - `ScorePill`: rendered on PLAYED/LOCKED tokens; `role="button"` + `stopPropagation`; `isLive` dot
     when `slotKind === "locked"`.
   - `PlayerScoreSheet`: surface-agnostic `"use client"` modal; fetches on open; renders `StatePill`
     + `SectionBlock`/`LineRow` + tracked stats + season total; `.sl-scoremodal` card styled above the
     forfeit overlay scrim. Accepts optional `forfeitProps` — renders `ForfeitSection` ("Bench & forfeit"
     button + confirm copy) at modal bottom for played starters only.
   - `BenchRow` locked rows are now clickable (not `disabled`): routes `!movable → onScore`.
   - `SetLineupClient` holds `scorePlayerId` state; `onScore` callback; period change resets to null.
     `onForfeitFromModal` handles eligibility gate + modal-close → toast or `ForfeitConfirmSheet`.
     `forfeitPropsForModal` memo wires played-starter classification → modal prop.
   - **No new RLS policy** — `score_player_match`/`stat_player_match` are owner-bypass server reads.
   - `ForfeitConfirm.test.tsx` updated: tap → modal (not forfeit confirm), forfeit via modal button.

**Tap-routing fix (fix/lineup-boxscore-tap):** Live defect on MD1 — tapping a played/locked player
opened the forfeit confirm instead of the score modal. Fixed by routing `PitchToken.handleClick` for
`isPlayedStarter` to `onScore` (same as `isLocked`); forfeit moved inside the modal as deliberate
secondary action. `ForfeitSection` component renders the existing confirm copy + button in modal.
Post-deploy visual confirmed on deploy 2026-06-12.

**Seams (next thread — Prompt 53):** Vs-the-Field wiring (`PlayerScoreSheet` is surface-agnostic);
scoring feed panel / mini-pitch coloring; live score refresh polling.

`fix/lineup-boxscore-tap`, merge HELD for Chat review.

**Vs-the-Field Direction-A reskin (`feat/vsfield-reskin`) — built, gates green, merge HELD.**
Ports `design/design_handoff_vs_the_field/vsfield2` Direction A ("split cockpit") onto `/vsfield`:
prominent leaderboard left rail, 2-fact CompareBand (Fact 3 deferred — Theme F, no per-player points in
the SSR snapshot), dual flag-kit jersey XI pitches (new pure `kitOf.ts`, iso2-keyed via the shared flag
mapper), net-new `.ma-*` mobile layout (leaderboard-first, local You/Opp toggle), `--kit-outline` global
token (all five ds.css copies synced). Realtime/loader/gate byte-identical; `VsFieldClient` =
layout + `effSel` state only. **1891 tests** (baseline 1874); typecheck/lint/format/test/`next build`
all green. Visual proof: local production build (`next build` + `next start`), desktop 1280 + mobile
375, dark + light, all 7 plan states (screenshots in `.screenshots-vsfield-reskin/`, untracked).
Theme-F-safe (test-enforced: no per-player score renders on any token; no `score_player_match` read in
the loader).

**Full WC2026 nation→kit gradient map — SHIPPED ✅** (`feat/vsfield-nation-kits`). `kitOf.ts` now
covers all **30 in-scope nations** (8 original + 22 from the approved Claude Design jersey-gradients
handoff), ISO2-keyed via the shared `toIso2`, Scotland resolved by name (saltire). Croatia upgraded
from a plain red/white/blue tricolor to a conic **šahovnica checkerboard** dot to break the NED↔CRO
collision. `var(--surface-4)` fallback retained for any nation outside the set. Prod feed strings
verified against `fifa_team` rows (Côte d'Ivoire / Czechia / South Korea / Türkiye match the resolved
keys); zero resolution misses → no shared-mapper edit and no kit-local alias table. (Merge/deploy by
operator.)

**Horizon (vsfield reskin follow-ups):**
- **/lineup jersey introduction** — production lineup has NO jerseys today (PlayerAvatar discs);
  adopting kits there is a feature build using the now-synced global `--kit-outline`.
- **AppShell mobile topbar wraps at 375px** — pre-existing global shell issue (visible in the mobile
  proof shots), separate fix, not vsfield-scoped.
- **Confirm-to-close (post-deploy):** on live Render `/vsfield`, verify ConnPill = Live and
  lock-on-play kit dimming render — the local harness cannot authenticate Supabase, so the proof shots
  show the gated `LOADING` state by design.
- **ds.css byte-identity now machine-enforced** across all four per-route copies (draft/vsfield/
  _landing/lineup vs `styles/ds.css`) by the extended appShell test.

## Mobile bottom-nav + overflow containment (Prompt 40 — `feat/mobile-bottom-nav`, HOLD for clearance)

- **The top-strip-widens-the-document bug is closed.** Root cause: the 8 top-nav items' intrinsic width
  set the document scroll width, causing every authenticated page to be wider than the viewport; the whole
  page slid sideways when the active tab auto-scrolled into view. Fixed by: (1) `.sh-topnav-scroll`
  scroll container (`overflow-x:auto; min-width:0; flex:1`) + `min-width:0` on `.sh-app`/`.sh-topbar`
  (real fix); (2) `html, body { overflow-x:hidden; max-width:100% }` in `ds.css` (document backstop);
  (3) `MoreSheet` `useEffect` calls `scrollIntoView({ inline:'nearest' })` on the active item, which
  is captured by the scroll container not the document.
- **Mobile bottom tab bar shipped** (phones < 640 px): Dashboard · Set lineup · Vs the field · Pool ·
  More. More sheet: Scoring · Waivers · Draft room · Settings · identity · sign-out. Pure CSS 640 px swap
  — no matchMedia, no hydration fork. Bar clears iOS home indicator via `env(safe-area-inset-bottom)` +
  `viewport-fit=cover` (real-device confirmation gated on deploy).
- **Playwright proof:** 75/75 assertions passed at 320/375/390/414 px (phone) + 768 px (desktop) across
  5 routes. Screenshots in `apps/web/screenshots/`. iOS safe-area/touch-ergonomics gated on real-device
  deploy pass.
- **Per-page mobile fit closed — mobile sweep complete (Prompt 41).** The two pages whose content was
  built wider than the viewport (and so *clipped* by the global backstop, not reflowed) now fit:
  `/lineup` (the formation pitch column was pinned ~384 px by a bare-`1fr` grid track → `minmax(0,1fr)`
  + token scaling 78→62→52 px; full XI in formation, tappable at 320 px) and `/pool` (auto-layout
  leaderboard forced ~670 px by long names/emails → `table-layout:fixed`, MANAGER ellipsis-truncates,
  PLAYED/CORRECT/POINTS fully visible, YOU row intact). Fixes live in per-route `lineup.css`/`pool.css`;
  `ds.css` byte-identity untouched. **Proof (`verify-page-fit.mjs`) asserts element bounds** — every
  `getBoundingClientRect()` within `[-1, clientWidth+1]` at 320/375/390/414 (8/8), not document
  `scrollWidth` (which the backstop satisfies by clipping); screenshots eyeballed. Merge HELD for Chat.

## Dashboard/Pool tournament-phase discriminator → `period.kind` (Prompt 44 — `fix/dashboard-phase-discriminator`, HOLD for clearance)

- **Bug (verified against the live DB):** `selectTournamentPhase` decided group-vs-knockout on
  `fifa_match.round` (`round === null → group`, `round !== null → knockout`). The live BALLDONTLIE feed
  populates `round` with the **matchday number** for group games — MD1 rows carry `round = "1"` with
  `in_progress`/`completed` status — so the selector's playoff branch fired and the dashboard rendered
  the **knockout interim while the tournament was still in the group stage**. Pool reuses the same
  selector, so it raised its knockout-bracket skeleton early too.
- **Fix:** `selectTournamentPhase` now keys on `period.kind` (`group_md`/`knockout_round`) and detects
  the Final by `period.label === "Final"`; `TournamentMatchSummary` is now `{ status, periodKind,
  periodLabel }`. `loadDashboard` + `loadPool` join `fifa_match.periodId → period.kind`/`period.label`
  and feed the mapped shape — `round` dropped from `loadDashboard`'s select and Pool's `MATCH_SELECT`;
  neither surface touches `fifa_match.round` any more. `loadDashboard`'s earliest-group-kickoff filter
  moved from `round === null` to `period.kind === "group_md"`. `periodKind === null` (period not linked)
  advances no phase. No migration; pure-selector + two thin loaders. This closes the one holdout that
  still trusted `round` — the rest of the system (locking / recompute / period-close / Pool engine)
  already keyed on `period.kind` (DECISIONS → Pool).
- **Tests (delta +4):** rewrote `selectTournamentPhase.test.ts` onto the `period.kind` shape (new `m()`
  helper; all precedence + exhaustiveness cases re-expressed) and **added** a Prompt-44 live-MD1
  regression `describe` proving `group_md` in_progress/completed + scheduled knockouts → `"group"` (not
  `"playoff"`), plus matchday-style-label, lone-scheduled-knockout, and unlinked-period cases.
  `selectDashboardCompose.test.ts` helper type + cases translated. `dashboard.test.ts` P38
  source-contract assertions updated (`periodKind === "knockout_round"`, `periodLabel === "Final"`,
  `period: { select: { kind: true, label: true } }`). `poolContracts.test.ts` green unchanged.
- **Gates:** `pnpm -w typecheck && pnpm lint && pnpm format:check && pnpm test` all green. **1949 tests
  passed** (140 files; baseline 1945, +4). Live-deploy proof (group dashboard on `/`, no knockout
  bracket on `/pool`) gated on deploy; merge HELD for Chat clearance.

**Prompt 45 — Quiniela polish (rename + ET kickoff times + Completed archive), COMPLETE ✅** (1955
tests). Three display/sort changes on the `/pool` surface; no engine/RLS/schema/migration/API-contract
change. **(1) "Pool" → "Quiniela" (display copy only):** the `NAV_ITEMS` + `BOTTOM_TAB_ITEMS` pool
labels (desktop strip + mobile bottom bar), the Picks `tablist` `aria-label`, and a new
`app/pool/layout.tsx` `metadata.title` ("XI · Quiniela"); route `/pool`, `NavId` value `"pool"`,
`pl-*` classes, `pool.css`, `@app/pool`, `/api/pool/pick`, `pool_pick`, and all identifiers untouched.
**(2) ET kickoff times:** the deterministic `PoolClient.tsx` formatter switches `timeZone:
"UTC"`→`"America/New_York"` and the suffix `" UTC"`→`" ET"` (correct EDT offset through the
tournament); the explicit fixed zone preserves SSR≡client output (no hydration mismatch). **(3)
Completed archive:** `selectPoolPicksView(fixtures, phase, now)` partitions `group_md` fixtures — a
completed match whose kickoff is ≥24h past (`ARCHIVE_AFTER_MS`) leaves its matchday and collects in a
new `PoolPicksView.completed` bucket (kickoff-desc), emptied matchdays dropped; `bracket`/`unscheduled`
unchanged. `loadPool` threads the same server `now` used by the reveal read. `PoolClient` renders
`completed` as a collapsed native `<details>` titled "Completed" at the bottom of the Picks tab (SSR-safe;
nothing when empty). +6 archive unit cases. Gates: typecheck/lint/format/test (1955) + `pnpm --filter
web build` (`/pool` ƒ 5.81 kB) all exit 0. **Merged to `main` @ `122b893`.** **Live-deploy smoke to
confirm:** Quiniela label (nav + mobile bar + browser tab), kickoff text reads "ET" at the right offset,
a completed >24h match appears in the bottom Completed `<details>`.

**Prompts 54 + 55 — Tabbed player card (Points | Stats), COMPLETE ✅.** The shared
`PlayerScoreSheet` gained a segmented **Points | Stats** tab strip (`.pc-seg*` from the
2026-06-13 `ds.css` design batch). The Points tab re-houses the existing `breakdown_json` breakdown
(unchanged scoring path). The Stats tab shows a position-aware tournament-stats tile grid
(`PC_TILEKEYS`/`PC_LINEKEYS` from the design) fetched from a new player-scoped
`GET /api/player-tournament-stats?playerId=` endpoint. Stats are fetched **eager + parallel** with
the existing player-box call on sheet open. The card is live on **/vsfield** (both own and opponents'
players) and **/lineup** (own XI, via the existing score-pill tap); the shared `PlayerScoreSheet` in
`apps/web/components/` is unchanged in interface — only the tab strip and Stats body were added.
**Next:** Free Agents / Waivers card integration — SHIPPED in Prompt 56 (below); the
open-the-card-vs-add-the-player interaction is resolved as a dedicated trailing control.

### 2026-06-14 — FA / Waivers player card (feat/fa-player-card, merge HELD)
The tabbed Points | Stats player card now opens on `/waivers` (the surface the tabbed-card prompt
deferred). A dedicated trailing `.wv-comp-fa-info` control on each free-agent picker row opens a
VIEW-ONLY `FaPlayerCardSheet` on the design's standalone `.pc-scrim`/`.pc-sheet` chrome: Points = a
real-data overview (Season points + Acquisition cutoff; no fabricated "projected"), Stats = the shared
`PlayerStatsTab` body (eager fetch, quiet error). The row's select-for-acquisition tap is unchanged;
acquisition stays on the existing right-panel select→submit. Shared `FaPickRow` de-dups the picker row
across `BidComposer` + `FreeAgentPanel`; `PlayerStatsTab` + `usePlayerTournamentStats` extracted so
`PlayerScoreSheet` and the new card share one Stats body. New styles in `waivers.css` only; ds.css +
schema untouched; no migration; no scoring/recompute/ingestion change. +18 tests (2005). Gates:
typecheck/lint/format/test + `pnpm --filter @app/web build` (`/waivers` ƒ 8.21 kB) exit 0. **Live-Render
screenshot on `/waivers` owed before merge:** the trailing control opens the tabbed card; Points shows
real season/cutoff data; Stats shows position tiles + completed-match log (correct opponents/flags/
scorelines — a `duels`-bearing line may read `—`/0 on live WC2026 data, expected); the
select→drop→submit acquisition still works unchanged. · **vsfield self/field detail XI
(`feat/vsfield-self-xi`), COMPLETE — merge HELD.** The self/field cockpit (`YouVsField`, rendered when
`effSel` resolves to field/self on both desktop `.da-body` and mobile `.ma-scroll`) now renders the
viewer's **own detailed jersey XI** (the existing `XIPitch`/`XIToken`, fed with `me.starters`) in place
of the abstract dot-node `PitchMini` + `XILegend` hero pitch — per-player points chips + tap-to-open
`PlayerScoreSheet` reuse the already-wired `setBoxPlayer` (threaded via new `onOpenPlayer`/`dimLive`
props on both call-sites). Still-to-come/playing/played side column preserved; new `.v2-agg-xi` frame +
mobile stacking; `PitchMini`/`XILegend` + dead `.vf-pitch*` CSS removed (zero call-sites). Pure
presentation — no loader/engine/RLS/endpoint change; points already in the snapshot (Prompt 41),
`pointsPath.test.ts` green, H2H untouched. +5 RTL tests (2034, 150 files); typecheck/lint/format/test +
`/vsfield` ƒ next build all green. **Screenshot proof not capturable in the headless worktree** (route is
auth-gated ƒ; needs a Supabase session + seeded mixed-state fixtures) — behavioral proof via the RTL
mount instead; live screenshot owed before merge.

### 2026-06-14 — Feed match-scoped pagination fix (feat/feed-match-ids, merge HELD)
The five match-scoped feed helpers (`matchLineups`/`matchEvents`/`playerMatchStats`/`teamMatchStats`/
`matchShots`) now filter **server-side** via the bracketed `match_ids[]=<id>` array param at `per_page=100`,
so a single fixture resolves in **one page** instead of walking the entire tournament dataset (~1,800 req /
~3 min per single-match peek that monopolized the rate budget and stalled live polling via the re-entrancy
guard). Root cause: the GOAT FIFA **paginated** endpoints silently ignore the scalar `match_id` we sent (it
is recognized only on the non-paginated `/odds/player_props`) → the server returned an unfiltered firehose —
also the deeper cause of the 2026-06-12 cross-match lock leak. `matchScoped` reuses the same `toQuery` array
path rosters uses for `team_ids[]`/`player_ids[]`; the client-side `match_id` re-filter is **retained** as
belt-and-suspenders; `playerProps` (scalar `match_id`, the one endpoint that honours it) +
`FIFAMatchLineupEntry`/`res.data` mapping (c9e8990) untouched. +7 tests (2041 passed | 3 skipped, 150 files)
asserting `match_ids[]=<id>` (not a bare `match_id=`) + `per_page=100` per helper, the belt-and-suspenders
cross-fixture drop, and a rosters array-scoping regression. Gates: `pnpm -w typecheck` / `pnpm lint` /
`pnpm format:check` / `pnpm test` all exit 0. Two commits (fix+tests `38a3a56`; brain files `[skip render]`).
Merge HELD pending clearance.

### 2026-06-15 — Group→playoff transition + playoff lineup mode (Phases 1+3, merged to main)

**feat/playoff-transition** (87a7e1a→b7dc9d3, 7 commits) + **feat/playoff-lineup-mode** (706351d + 3952e62), ff-merged to `main`. 2115 tests ✓. **Schema:** new `playoff_entry` table (`id/league_id/manager_id/seed/status/eliminated_round/eliminated_at`, `PlayoffEntryStatus = alive|eliminated|champion`, `@@unique([league_id, manager_id])`, `@@index([league_id, status])`, FKs CASCADE, RLS league-scoped SELECT, added to `supabase_realtime` publication). **Pure transition derivation** in `packages/recompute/src/transition.ts`: `cutScheduleFor` (front-loaded `⌊÷5⌋+rem` distribution, field ≥ 6; supersedes the design's illustrative preset), `selectPlayoffField`, `carryForwardWaiverOrder`. **`commish:transition` CLI:** dry-run default (prints field+seeds, cut schedule, plan, `standings: FINAL ✓/⚠` gate); `--apply` runs 6-step `$transaction` (status claim → `cut_count` writes → `playoff_entry` rows → roster release → $100 FAAB reset → two-phase waiver carry-forward); D6 standings-frozen guard + `--allow-incomplete-standings` override. **FAAB roster-cap split:** `rosterCapForLeagueStatus(league.status)` in `@app/shared` (15 group / 9 playoff) enforced at both FAAB validators — submission + batch resolver (`league.status` axis, NOT `period.kind`). **Playoff lineup mode:** `validateLineup` branches on `period.kind === "knockout_round"` → `PLAYOFF_ROSTER` (cap ≤9, 7 starters 1 GK + 6 outfield); `FORMATIONS_PO` 2-2-2/2-3-1/3-2-1 emerges from `playoffBounds()`, not hard-coded; drift-guard test pins the emergence. Period-driven Set-Lineup tab (`loadLineup` reads `period.kind`, `LockHero` shows "Playoff XI · 7 starters"). **Key invariant (D5):** 9-cap is a gate, not a driver — a 15-man advancer who never drops is blocked from setting their R32 lineup and forfeits (by design). **Open seams (trim-down phase, next):** manager-facing release-to-9 flow; commish force-trim CLI; non-advancer playoff FAAB gate; trim window structure. See DECISIONS.md → Group→playoff transition block + ARCHITECTURE.md §20.

**On horizon:** Phases 1+3 merged. **Next = the trim-down phase** (manager release-to-9 flow + non-advancer FAAB gate + commish force-trim), then the guillotine round-loop (Phase 2), then the theater screen + dashboard playoff/complete.
