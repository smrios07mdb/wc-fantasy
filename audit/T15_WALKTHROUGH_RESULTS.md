# T15 — Live mobile walkthrough — RESULTS

- **Run date:** 2026-07-03 (Fri) ~14:10 ET
- **Executed by:** Claude, driving the **iPhone Mirroring** window on Sergio's Mac against the **live Render production** deploy, logged in as the commissioner ("You").
- **Checklist:** `audit/T15_LIVE_WALKTHROUGH.md` (67 steps) · companion punch-list `audit/AUDIT_T15_mobile_ux.md`.
- **Scope:** OBSERVE-ONLY. Navigate / scroll / open+close sheets / focus inputs / screenshot. No Save/Submit/Confirm taps; nothing mutating in `/commish`; no release/drop/forfeit; no permission prompts.

## Verification medium

Mirroring uses the Mac keyboard/pointer, not touch. Every verdict is tagged:

- **mirror-valid** — layout, clipping, occlusion, reachability, copy, colors, states, Safari zoom-on-focus. Trustworthy through the mirror.
- **mirror-degraded** — tap-target ergonomics, scroll/overscroll physics, swipe-dismiss feel, safe-area/home-indicator, keyboard-overlap while typing. Recorded but **capped at PROVISIONAL** → needs a finger pass on-device.

**Verdict keys:** PASS · FAIL · PARTIAL · PROVISIONAL · SKIPPED-STATE-UNAVAILABLE · OPERATOR-REQUIRED · BLOCKED.

## Device & environment

- **Device:** iPhone Pro-class (Dynamic Island), portrait, Safari, mirrored. **Native CSS width ≈ 390–393px.**
- **360px caveat (applies to every clip step):** the mirror renders at the device's native ~390px. Where a step asks for 360px, a **FAIL at ~390px is a hard confirmation** (360 is narrower → worse); a **PASS/fit at ~390px is only PARTIAL for the 360 claim** — true 360 needs an SE/mini-class device or Safari RDM 360×780. Flagged per step.
- **Screenshots:** ⚠ disk-persistence is **disabled** in this session (`save_to_disk` no-op). The `screenshots/` folder could not be populated with PNGs. Each step's observation is written to be self-contained; the human follow-up pass should re-capture FAIL screens per the checklist's "whole screen, not a crop" rule.

## State at run time (Step §0 assessment)

- **Phase:** Playoff / knockout — **R32 survival**, reduced roster (7 starters + 2 bench), FAAB carries over (no reset). Dashboard hero: "● KNOCKOUTS LIVE / You're alive / R32 · 10→8 survive · 2 cut this round". "You" = rank 4 of 10, 79 pts, margin +33 clear of the cut.
- **Live match in play right now?** **No.** `/vsfield` shows R32 as the **LIVE period** (Updated 02:xx PM · ● LIVE) but both this-period fixtures render **FT** (South Africa–Canada, Brazil), "1 still to come" (upcoming, not kicked off). So the round is live-scored but **no match has a running clock at this instant**.
- **Consequence:** in-play-only steps (live minute #38, live lock-flip #17, server-reject on fresh kickoff #15, live re-sort wave #12, blade-drop moment #31, ET/pens #39, FA-window flip #21) → **SKIPPED-STATE-UNAVAILABLE** today (re-run during a live match per Appendix A). LIVE-badge / socket-honesty / elimination-framing checks remain runnable because the round itself is live.

---

## Results

_Table filled section by section as the run proceeds (incremental save). Screenshot column notes in-session capture; disk-persist disabled._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 1 · Cold-load font flash ⚠ | Global | PROVISIONAL | mirror-degraded | Settled brand type renders correctly (grotesk headings, mono numerics — "10 left"). Could not force a cache-cold cellular load nor sample the sub-second FOUT via discrete mirror frames. F-P2 @import-chain FOUT/reflow neither confirmed nor refuted → needs on-device screen-recording on a cold cellular load. |
| 2 · Dark overscroll + first paint ⚠ | Global | PROVISIONAL (leans PASS) | mirror-degraded | Scrolled to top & bottom boundaries and behind the status bar / Dynamic Island; background stayed the app's near-black throughout — **no white bands or flashes** observed. True rubber-band stretch color + cold first-paint need a finger drag + cold reload on-device. |
| 3 · Landscape notch | /vsfield | OPERATOR-REQUIRED | mirror-degraded | Cannot rotate the physical device through iPhone Mirroring. Needs on-device landscape rotation on /vsfield + More sheet to verify safe-area-x (F-P3). |
| 4 · PWA standalone | Global | PASS (partial) | mirror-valid (layout) | Verified in the **installed home-screen PWA**: chromeless standalone presentation, readable dark status bar, app tab bar clears the home indicator. Dark splash + XI icon crispness on cold launch = operator follow-up. |
| 5 · Keyboard vs bottom bar ⚠ | /settings, /waivers | PROVISIONAL (deferred) | mirror-degraded | The iOS software keyboard generally does not appear under hardware-keyboard mirroring, so keyboard-overlap can't be reproduced faithfully. Focus behavior folded into §4/§8 input tests; the position:fixed tab-bar-float quirk needs an on-device finger pass. |
| 6 · 404 page ⚠ | /zzz | **FAIL (confirmed)** | mirror-valid | `/zzz` → stock Next.js **"404 · This page could not be found"**: pure **white**, unbranded, **no app nav, no in-app way back**. Not color-scheme-aware (white even in the dark app → jarring; covers the dark-appearance case too). Confirms **F-P1 (no not-found.tsx)**. |
| 7 · Tab-switch freeze (slow net) ⚠ | Global | PROVISIONAL | mirror-degraded | Can't throttle to Low Data Mode / weak signal or time sub-second transitions via mirror. The "old screen frozen, no loading.tsx" (F-P2) needs an on-device slow-network pass. **Compounded by** the unresponsive bottom-nav (see NEW FINDING N1) which itself reads as a freeze. |
| 8 · Forced server error | Global | SKIPPED-STATE-UNAVAILABLE | n/a | Staging-only per the step ("never break prod"). Not exercised on production. |
| 9 · Matchday Points clipped ⚠ | /standings | PARTIAL (not reproduced) | mirror-valid | Matchday tab, MD3, native ~390–430px: columns are #·Manager·W–L–D·PTS; **PTS values (157,139,124,122,120,118,100,94,82) render fully, right-aligned, not clipped**, with generous whitespace before PTS. F-P0 clip does not manifest at this width. **360 narrow-end unverified** (device is Pro-class, wide end) → needs SE/mini or Safari RDM 360×780. Only manager *names* ellipsize (expected). |
| 10 · Cumulative chevron/trend clipped ⚠ | /standings | PARTIAL (not reproduced) | mirror-valid | Cumulative tab: the **expand chevron ⌄ is present on every row, invites the tap, and works** — tapping Cheles expands the per-MD breakdown (MD1 11–0·158 / MD2 8–3·154 / MD3 10–1·139 / Season 29–4·451) and the chevron inverts. So F-P1 "chevron fully gone" does **not** reproduce at native width. The **+/- trend column shows only "–" for all rows** (no ▲/▼) — ambiguous: no movement baseline vs. glyph not rendering (worth a data-state check). 360 narrow-end unverified. |
| 11 · Season grid scroll affordance | /standings | PARTIAL | mirror-valid (mechanics) | Season tab is a MANAGER·MD1·MD2·MD3·**TOTAL** grid; **TOTAL (the key column) is clipped at the right edge** and only a horizontal drag reveals it. Scroll works, but the **only affordance is the cut-off column** — no scrollbar, fade, or arrow. Naive-user discoverability (F-P2) = **operator (needs a fresh person)**. Design read: affordance is weak, and it's the most important column that's hidden. |
| 12 · Live re-sort cue | /standings | SKIPPED-STATE-UNAVAILABLE | n/a | No in-play scoring wave right now (no live match; /standings here is group-stage historical). Re-run during a live match to watch ranks change (F-P2). |
| 13 · Stale "provisional cut" copy | /standings | **FAIL (confirmed)** | mirror-valid | Post-transition (R32 knockouts LIVE) the Cumulative ContextBand still reads **"PROVISIONAL CUT · Top 10 qualify · fixed at the group→playoff transition."** Qualification is locked and we're already past it, so "PROVISIONAL" is stale/misleading. Confirms **F-P2**. (Count is right: "Top 10" matches the R32 field of 10; the *label* is the stale element.) |
| 14 · SaveBar buried under tab bar ⚠ | /lineup | OPERATOR-REQUIRED | mirror-valid (structural) | Lineup is "UP TO DATE" (no pending edit → no bottom SaveBar). Live R32 playoff lineup shows **1 movable · 8 locked** — I deliberately did **not** stage an edit on live-scoring production data to summon the SaveBar. **Structural evidence supports F-P1:** the pitch already overflows the viewport (had to drag to reveal the defender row; GK + bench remain below the fold) and the app bottom-nav is fixed — a bottom-anchored SaveBar on edit would land at/behind the nav. Confirm by staging a swap during an open (pre-lock) window and observing "Save lineup" + the legality line vs. the nav. |
| 15 · Server-reject error invisible ⚠ | /lineup | SKIPPED-STATE-UNAVAILABLE | n/a | Needs a live window + a kickoff that locks a mid-edit player, then Save. No match in play. Re-run during a live match (Appendix A). |
| 16 · Frozen period presentation | /lineup | OPERATOR-REQUIRED | n/a | Requires a commissioner **freeze** (mutating /commish action, out of bounds for this pass). Sergio to freeze in a quiet window, observe whether players still read swappable / Save enabled, then unfreeze. |
| 17 · Live lock flip | /lineup | SKIPPED-STATE-UNAVAILABLE | n/a | Needs to sit on /lineup across a real kickoff to watch a token flip movable→locked without reload. No match in play. |
| 18 · Token micro-labels @360 | /lineup | PASS | mirror-valid (daylight=operator) | Reduced-roster playoff pitch (2-3-1, 7 starters). Token micro-labels (name / kickoff / "vs Opp") are **contained within each token**; long names like **"Idrissa Gana Gueye"** wrap to 2 lines with no collision or overflow; ~9–11px but legible on-screen at native width. Kickoff labels carry a zone tag ("EDT") — see §9. Outdoor/daylight readability = operator. 360 narrow-end not directly tested. |
| 19 · Eight period tabs @360 | /lineup | PARTIAL (PASS-leaning) | mirror-valid (layout) | The 8-period strip (MD1/MD2/MD3 final · R32 · R16/QF/SF/Final upcoming) **wraps into 2 rows** above the pitch, consuming vertical space. Chips are two-line (~40px tall) → tap targets acceptable (not the ~30px worst case). Fast-switch mis-tap precision is mirror-degraded → finger pass. |
| 20 · Score-sheet scroll-chain + close ⚠ | /lineup→§4/§6 | BLOCKED (mapping) | mirror-degraded | Deferred from /lineup; on /waivers the FA-card couldn't be opened once the tap-mapping broke (see SESSION NOTE). Retest on a match-page score sheet (§6) after the mirror restart. |
| 21 · FA window flip | /waivers | PARTIAL | mirror-valid (read) | FA pool **is present** but **acquisition is LOCKED**: header "WAIVERS LOCKED FOR R32" + "FREE AGENTS · INSTANT PICKUP · The acquisition window is locked for this period." State is **clearly communicated, not broken** — shows FAAB banner, budget ($99/$99, $0 pending), rolling waiver order (You #10), and a browsable/searchable pool (Tielemans, El Aynaoui, Niakhaté, Diatta, Eustaquio, Bensebaini render cleanly — nation chip, pts, pos badge, opponent). It does NOT state *when* FA reopens. Exact flip/gap timing = needs-live-verify (right after a batch clears). **Bonus:** the FAAB banner copy is the CORRECT "carries over … not wiped or replenished" — the stale "FAAB resets" copy is NOT present (consistent with the T15-A audit reversal). |
| 22 · Composer vs keyboard ⚠ | /waivers | BLOCKED (mapping) | — | Composer not opened before the tap-mapping broke; acquisition-locked may also disable bidding. Retest after restart. |
| 23 · URL-bar vs 90vh ⚠ | /waivers | BLOCKED (mapping) | — | Needs the bid composer open. Retest after restart (Safari tab has the URL bar, so this is testable). |
| 24 · Search jank | /waivers | BLOCKED (mapping) | — | Could not reliably focus/type in "Search free agents…" once the mapping broke. Retest after restart. |
| 25 · Scroll-chain in composer lists ⚠ | /waivers | PARTIAL | mirror-degraded | The FA **pool list scrolls** fine. Background-scroll containment on the *composer's* inner lists/drop-picker (F-P2) not tested (composer not opened). Retest after restart; scroll physics are mirror-degraded regardless → finger pass. |
| 26 · Release-list confusion | /waivers | OPERATOR-REQUIRED | n/a | Needs-live-verify during a trim-down (ask a manager with played players whether the shorter release list reads as "locked" vs "missing"). |
| 27 · Composer under bottom nav ⚠ | /waivers | BLOCKED (mapping) | — | Needs composer open to check the Place-bid row vs. nav + tap-through scrim (F-P1 z-index). Retest after restart. |
| 28 · FA player card under nav ⚠ | /waivers | BLOCKED (mapping) | — | Tapping the player ⓘ / row did not register once the mapping broke. Retest after restart (F-P1 z-index). |
| 29 · NationFilter ergonomics | /waivers | BLOCKED (mapping) | — | Could not expand "Nations ▸" / interact with chips once the mapping broke. Retest after restart (F-P2). |

---

## NEW FINDINGS (defects not covered by any checklist step)

- **N1 · Bottom-nav taps are unreliably registered (primary navigation) — HIGH.** Screen+state: any authenticated route, bottom nav (Dashboard / Set lineup / Vs the field / Quiniela / More), PWA + Safari. Repeated taps on "Set lineup" did **not** register (navigation didn't fire, active-tab highlight didn't move) across multiple attempts. **Operator (Sergio) confirms on real hardware unprompted: "the bottom icons are highly unresponsive most of the time."** This is real-device behavior, not a mirror artifact — the whole-app primary navigation is intermittently dead. Distinct from the tap-target *size* findings (steps 19/34/64): this is responsiveness/hit-testing, not just target dimensions (possible causes to check downstream: an overlay/scrim swallowing taps — cf. F-P1 z-index steps 27/28, a too-small/misaligned hit area, or a slow/blocked tap handler). Recommend elevating: primary nav that misses "most of the time" is arguably P1. Workaround used for this audit: navigate by typing routes in the Safari URL bar.

---

## SESSION NOTE — why this run was cut short

Mid-run, the operator's **external monitor was disconnected** (going laptop-only). The computer-use tool had calibrated its screen→tap coordinate mapping to the original multi-display geometry; after the disconnect the **vertical axis collapsed** — every tap/hover landed on the top macOS menu bar instead of the phone (menus kept opening). Screenshots, scrolling, zoom, and keyboard all kept working; only pointer clicks were unreliable. Force-quitting/reopening iPhone Mirroring did **not** fully resolve it (a cursor *move* round-tripped correctly once, but a subsequent *click* still hit the menu bar). Conclusion: **finish in a fresh Cowork session started on the single laptop screen** so the pointer mapping calibrates correctly from the start. This file is the durable record; steps 1–19 + partial 20–29 are banked below.

## Summary counts (steps 1–29 attempted this run; 30–67 not yet started)

- **PASS:** 2 — steps 4, 18
- **FAIL (confirmed defect):** 2 — steps 6, 13
- **PARTIAL / not-reproduced-at-native-width:** 6 — steps 9, 10, 11, 19, 21, 25
- **PROVISIONAL (mirror-degraded):** 4 — steps 1, 2, 5, 7
- **OPERATOR-REQUIRED:** 4 — steps 3, 14, 16, 26
- **SKIPPED-STATE-UNAVAILABLE:** 4 — steps 8, 12, 15, 17
- **BLOCKED (tap-mapping failure — retest next session):** 7 — steps 20, 22, 23, 24, 27, 28, 29
- **NOT YET RUN:** 38 — steps 30–67 (§5–§11) + Appendix A live-state steps

**Confirmed defects so far (real hardware):** step 6 (404 = unbranded white Next.js default, no nav/way-back, not dark-mode aware) · step 13 (stale "PROVISIONAL CUT" copy persists post-transition) · **N1 (bottom-nav taps unreliable — operator-confirmed "highly unresponsive most of the time").**

## Human follow-up queue

**Retest first thing next session (BLOCKED by tap-mapping):** 20 (score-sheet scroll-chain + ✕, via a match page), 22 (composer vs keyboard), 23 (composer vs URL-bar/90vh), 24 (FA search jank), 27 (composer under nav / tap-through), 28 (FA card under nav), 29 (NationFilter ergonomics). Note: FA acquisition is currently **locked for R32**, so the bid *composer* may be unavailable until a window opens — the pool/card/filter/search UI is still testable.

**OPERATOR-REQUIRED (Sergio, on-device):** 3 (landscape-notch rotation), 14 (stage a swap during an open window to expose the SaveBar vs. nav), 16 (freeze a period from /commish, observe /lineup, unfreeze), 26 (release-list wording during a trim-down), 45 (type-to-confirm keyboard auto-caps — real iOS keyboard only; never tap confirm), 54/55 (push enable + mid-enable failure), 61 (draft set-clock inert-success submit).

**PROVISIONAL → on-device finger pass (mirror-degraded):** 1 (cold-load FOUT — needs cold cellular + screen-record), 2 (overscroll rubber-band color / cold first-paint), 5 (keyboard vs bottom bar), 7 (tab-switch on slow network).

**PARTIAL → true 360px pass (device is Pro-class ~390–430px):** 9, 10, 11 (and all §5–§7 clip P0/P1s once run) need an SE/mini or Safari RDM 360×780 to confirm the narrow-end clips.

**SKIPPED-STATE-UNAVAILABLE → re-run during a live match:** 12, 15, 17 (and the whole Appendix A set: live minute, live lock-flip, blade-drop, ET/pens, FA-window flip).

---

# SESSION 2 (RESUMED) — 2026-07-03 (Fri) ~16:05 ET

- **Executed by:** Claude, driving iPhone Mirroring on Sergio's Mac (single laptop screen — no external monitor this time), live Render production, commissioner login.
- **Setup / calibration:** Requested iPhone Mirroring (full tier). Menu bar frontmost = "iPhone Mirroring". Tap calibration verified clean: `mouse_move(249,568)` → `cursor_position(249,568)` exact. **The tap-mapping bug from session 1 did NOT recur** — every tap this session landed accurately. On connect the phone showed "iPhone in Use — Lock your iPhone to connect"; a single Connect click re-established the mirror.
- **Device:** iPhone Pro-class (Dynamic Island), portrait, Safari, native width ≈ 390–393px. Same 360px caveat as session 1 (FAIL at ~390 = hard confirm; fit at ~390 only PARTIAL for the 360 claim).
- **Mirror scroll note:** wheel-scroll through the mirror is very sluggish (large `scroll_amount` batches needed to traverse long pages); scroll/overscroll **physics remain mirror-degraded** (finger pass still required for pull-to-refresh / rubber-band). Software-keyboard overlap still can't be reproduced (hardware keyboard routes text to focused iOS inputs, but the iOS soft keyboard doesn't render).

## Results — §4 retest (was BLOCKED) + new waivers observations

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 20 · Score-sheet scroll-chain + close ⚠ (FA-card Stats leg) | /waivers FA card | PARTIAL (no leak seen) | mirror-degraded | Opened an FA player card (Tielemans ⓘ) → **Stats** tab. Card auto-grows to fit the per-match breakdown (4 MATCHES / 2 G / 0 A / 7 KP / 59 PTS; MD1 Egypt +13, MD2 Iran +10, MD3 NZ +11, R32 Senegal +25 · 129'). Content fit with **no internal scroll needed**; wheel-scrolling inside the card did **not** leak to the background (FA list behind the scrim stayed put). ✕ close stays top-right, always reachable. True finger drag-past-end / pull-to-refresh physics still mirror-degraded. **Primary match-page score-sheet leg → tested under §6 (step 20b).** |
| 22 · Composer vs keyboard ⚠ | /waivers | OPERATOR-REQUIRED (bid composer unavailable) | mirror-degraded | R32 acquisition is **LOCKED** → the **FAAB bid composer with an amount field does not exist in this state**. The only add flow available is **FREE AGENTS · INSTANT PICKUP** (select FA → "DROP TO MAKE ROOM · SQUAD FULL 9/9" drop-picker → confirm) — **no bid-amount input**, so the "focus amount then search / keyboard over lower half" test can't be run now. Re-run during an open FAAB window (operator). Keyboard-overlap is mirror-degraded regardless. |
| 23 · URL-bar vs 90vh ⚠ | /waivers | PARTIAL → OPERATOR (bid composer unavailable) | mirror-valid (layout) | No FAAB bid composer to open (locked, see 22). For the available **instant-pickup** composer, its action area is occluded by the bottom nav in BOTH URL-bar states (see step 27) — so the 90vh→dvh action-row-cut concern is effectively realized by the nav occlusion rather than the URL bar. True bid-composer 90vh test → operator, open window. |
| 24 · Search jank | /waivers | PARTIAL (functional, no drops seen) | mirror-degraded | Focused "Search free agents…", typed "Diatta" → all 6 chars registered, field showed a caret + blue focus ring, and the pool filtered live to the single match (Krépin Diatta · Senegal · 55 · MID). iOS field-accessory bar (^ ⌄ ✓) appeared = field genuinely focused. **No dropped keystrokes**, but `type` sends text in a batch (not a faithful fast-keypress stress test) → true rapid-typing jank on the full live pool still needs an on-device finger pass. (First attempt mis-focused because the field sat under the fold; on a precise click it worked.) |
| 25 · Scroll-chain in composer lists ⚠ | /waivers | PARTIAL | mirror-degraded | FA **pool list scrolls** and the **FA-card Stats** inner content did not leak to background (step 20). The instant-pickup **drop-picker's** own inner-scroll containment couldn't be exercised because the drop-picker is occluded by the nav (step 27) and the bid composer is unavailable. Scroll physics mirror-degraded → finger pass. |
| 27 · Composer under bottom nav ⚠ | /waivers | **FAIL (confirmed — instant-pickup composer)** | mirror-valid | With the INSTANT-PICKUP "ADDING [player]" composer open, the **"DROP TO MAKE ROOM · SQUAD FULL 9/9" drop-picker is cut off exactly at the fixed app bottom-nav line** — neither scrolling the pool nor the composer region reveals the drop cards/confirm control; the lower composer sits **behind the tab bar**. Confirms **F-P1 (composer content under bottom nav)** for the add flow that IS available at R32. (Tap-through-scrim sub-check deliberately not run — with N1 nav-unreliability a non-response would be ambiguous, and a response would navigate away.) The true FAAB bid composer's Place-bid row → operator, open window. |
| 28 · FA player card under nav ⚠ | /waivers | PASS (not reproduced @≈390px) | mirror-valid | Opened FA cards from the pool (ⓘ). The card is a **compact centered popover with a dimmed scrim**, floating mid-screen (≈y360–555 on Points; grows to ≈y715 on Stats) with a **clear gap above the bottom nav** — the tab bar does **not** paint over the card. ✕ close reachable top-right throughout. The expected F-P1 "nav over card bottom" does **not** manifest at native ~390px. Minor: the card **header name ellipsizes prematurely** ("Youri Tielem…") though there's whitespace. 360 narrow-end unverified. |
| 29 · NationFilter ergonomics | /waivers | PARTIAL | mirror-valid (mis-taps=degraded) | "Nations ▾" expands a wrapping chip grid (All + ~30 nations). Chips are pill-shaped, flag + full name, **legible** (even "Bosnia & Herzegovina" on its own row); height ≈30px (borderline, F-P2). Selecting Belgium filtered the pool correctly and **re-flowed the grid**. **Two deviations from the step's expectation:** (a) there is **no ✕-clear on the active chip and no count/clear control on "Nations ▾"** — you clear by tapping "All" or re-tapping the chip, not the ✕ the step describes; (b) on re-flow a chip (**"Canada"**) **clips at the right viewport edge** ("Canad…") instead of wrapping. Expanding the grid pushes the pick-list down but it stays reachable. Per-chip mis-tap rate mirror-degraded → finger pass. |

### NEW FINDINGS (session 2, §4)

- **N2 · Raw email addresses leak as team names (PII / polish) — MEDIUM.** On `/waivers`, managers who never set a team name are rendered by their **raw email** in shared, all-league lists: **Rolling Waiver Order #4 = "ahitaon@gmail.com"**, and **Team Budgets** shows "ahitaon@gmail.com $100" and "yader.rosales@gmail.com $87". Every league member sees these. Should fall back to a display name / "Manager 4", not expose email addresses. (Also appears anywhere else the team-name field is rendered — worth a global check: standings, playoffs board, vsfield.)
- **N3 · Opponent renders as raw internal ID "Team 288" — MEDIUM (data/copy).** In the FA pool, **Nico Elvedi (Switzerland, 49 pts)** shows fixture "**vs Team 288**" — a raw team identifier leaking into copy instead of a nation name or the "TBD" used by other unresolved knockout fixtures (e.g., Niakhaté/Diatta show "TBD"). Looks like an unresolved-opponent code path that forgot to map/placeholder the ID.
- **Positive note (not a defect):** Team Budgets renders **eliminated teams with a strikethrough** ("yader.rosales@gmail.com $87", "Los que todo lo hacen mal $72") — a clear, tasteful "out" treatment. Worth preserving; contrast with the missing explicit-eliminated state flagged for `/playoffs` (step 63).
- **Confirms step 21 messaging (mirror-valid):** locked state reads cleanly — "WAIVERS LOCKED FOR R32" + "FREE AGENTS · INSTANT PICKUP · The acquisition window is locked for this period." FAAB banner is the CORRECT "carries over … not wiped or replenished." Budget $99/$99, $0 pending, You #10.

## Results — §5 /vsfield + /pool (steps 30–36)

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 30 · Season tab table clipped ⚠ | /vsfield | PARTIAL (much milder than F-P0) | mirror-valid | The Season tab is a **"Power record"** table: # · MANAGER · RECORD · WIN%. On initial render the **WIN% column is marginally clipped** at the right viewport edge (the "%" / last glyph cut: "88%"→"88"). BUT the table **does scroll horizontally** and a small scroll reveals WIN% fully; there are **no "Points / by-period" columns** in this view. So the feared F-P0 ("rightmost columns cut with NO in-table scroll") does **not** reproduce at ~390px — the clip is cosmetic and scroll-recoverable. 360 narrow-end could clip more of the number → flag. (Contrast: the /pool Leaderboard's 5-col table fits with no clip.) |
| 31 · Elimination legibility | /vsfield | SKIPPED-STATE-UNAVAILABLE | n/a | Needs a live blade-drop / cut moment (field shrinking). No elimination happening this instant (R32 live-scored but no cut settling). Re-run across a round settling (Appendix A). |
| 32 · Period strip auto-scroll | /vsfield | PARTIAL (not reproduced @R32) | mirror-valid | At R32 the period strip is MD1 · MD2 · MD3 · R32 · LIVE — only 4 periods, all fit, **R32 (active) is visible** at the right, not parked off-screen. The "strip parked at MD1, active round off-screen right" clip needs a **deep bracket (SF/Final, 8 periods)** to manifest → re-run late in the bracket. |
| 33 · Sheet height vs URL bar | /lineup (score sheet) | PASS-leaning | mirror-valid | The player score-sheet modal (tested on /lineup, step 20b) keeps its header + ✕ pinned at the top and the whole card is visible; the compact FA card (step 28) likewise cleared the chrome. Whole card incl. ✕ visible with the URL bar in its normal state. Exact URL-bar-fully-expanded pixel check is minor / finger-pass. |
| 34 · Pool pick buttons ⚠ | /pool | PARTIAL / PROVISIONAL | mirror-degraded | Knockout pick'em: each match card has **[Team A] [Team B] [🔒 LOCKED]** pill buttons, **~34px tall** — matches the flagged F-P1 target-size concern. All R32 matches are **LOCKED** (past kickoff), so the one-handed **miss-rate can't be exercised** on active buttons through the mirror. Confirm size-driven mis-taps on an unlocked upcoming match on-device. |
| 35 · Contrast outdoors | /pool, /vsfield, /playoffs | OPERATOR-REQUIRED | n/a | Bright-light readability of tertiary micro-labels / cut-line labels — physical daylight test only (pairs with step 67). |
| 36 · Pool leaderboard staleness | /pool | PARTIAL (cue absent) | mirror-valid (for the absence) | The Quiniela **Leaderboard** (# · MANAGER · PLAYED · CORRECT · POINTS) has **no "updated Xs ago" / freshness cue** — header is just "STANDINGS · +1 per correct result · ranked by points". So stale data would look live (confirms the F-P3 concern). The true "background 2 min → return" timing test is mirror-degraded. Table itself fits cleanly at ~390px, names ellipsize. |

## Results — §6 /games/[matchId] (steps 37–44)

_Match pages reached by tapping fixtures: R32 knockout = **South Africa 0–1 Canada** (via /pool); group = **Mexico 2–0 South Africa (MD1)** (via /vsfield → MD1 → fixture)._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 37 · Five-tab clip ⚠ | /games (group MD1) | PARTIAL (not reproduced @≈390px) | mirror-valid | Opened a **completed group-stage match** (Mexico 2–0 South Africa, MD1) — it has the full **5 tabs: Lineups · Statistics · Events · Ratings · Standings**. At native ~390px **all five fit and "Standings" is fully visible + reachable** (renders a clean Group A table: # TEAM P W D L GD GF PTS). Margin is tight (~14px past "Standings"), so the F-P0 clip does **not** reproduce at 393px but **360px would very likely clip Standings** → flag for a true-360 pass. (Knockout matches show only 4 tabs — no group "Standings" — and fit with room.) |
| 38 · Live minute | /games | SKIPPED-STATE-UNAVAILABLE | n/a | No live match in play. Re-run during a live match to check for a running clock / HT indicator vs. a bare "Live" word (Appendix A). |
| 39 · ET / pens | /games (knockout) | OPERATOR-REQUIRED (no pens match found) | mirror-valid (sample) | Sampled R32 knockouts were **not** penalty-decided: South Africa 0–1 Canada (Eustáquio 90+2', normal time); Brazil 2–1 Japan; the one ET match seen in a player breakdown (Belgium 3–2 Senegal, 129') was decided in extra time, not a shootout. Need to catch a **penalty-decided** knockout to check the "1–1 · Full-time with no shootout info / score-anomaly banner" defect (Appendix A). |
| 40 · Event name overlap | /games (knockout) | PASS (not reproduced @≈390px) | mirror-valid | Events tab is a **center-timeline**: minute chip centered, player names flanking (SA left / Canada right). Goal (Eustáquio 90+2') + subs (Tshepang Moremi▲/Thapelo Maseko▼ 86', Iqraam Rayners▲/Evidence Makgopa▼ 86', Alphonso Davies▲ 75') — **no name-vs-minute collisions** at ~390px; long names fit. 360 caveat for the longest pairings. |
| 41 · Pitch token size | /games (Lineups) | PARTIAL / PROVISIONAL | mirror-degraded | Lineups pitch shirt tokens are **small (~24px visible icon)** — **below the ~40px comfort threshold** the step asks for. Names + fantasy chips (e.g., Eustáquio +23 highlighted) are legible. Actual tap comfort on an SE-class device is mirror-degraded → finger pass; the visible token size already reads as under-target. |
| 42 · Muted fantasy chip contrast | /games (Lineups) | PARTIAL / PROVISIONAL | mirror-degraded (daylight=operator) | Low-value / appearance-type chips (e.g., +2, +3) render **dimmer/greyer** than the highlighted scorer chip (+23 blue). On-screen they're legible; true muted-chip contrast at arm's length / daylight is an operator call (pairs with 35/67). |
| 43 · Deep-link Back | /games | PARTIAL | mirror-valid | Every match page has a "‹ Back" button; internal navigation Back returns to the referrer (/pool or /vsfield). The **cold deep-link** case (open a match URL from Notes/Messages with no in-app history) can't be faithfully reproduced here → operator; the bottom nav provides a fallback way out regardless. |
| 44 · Ratings podium | /games (Ratings) | PASS (not reproduced @≈390px) | mirror-valid | "Highest-rated players" top-3 podium (Eustáquio 8.7 🇨🇦 +23, Ronwen Williams 8.2 🇿🇦 +19, Aubrey Modiba 7.8 🇿🇦 +12) — three cards fit side-by-side at ~390px; nation label "South Africa" fits inside its card; player names wrap cleanly. 360 caveat for very long nation names (e.g., "Bosnia & Herzegovina"). |

### NEW FINDINGS (sessions 2, §5–§6)

- **N4 · Data-provider name "balldontlie" leaks into user-facing score copy — LOW.** On a player score sheet (/lineup → player → Points), the Performance Rating line reads "rating 8.2 **(balldontlie)** → +3". The upstream stats provider's package/name ("balldontlie", i.e. balldontlie.io) is exposed in fantasy-breakdown copy — reads as a dev artifact; should be dropped or replaced with a clean attribution.
- **N2 is pervasive (upgrade rationale):** the raw-email-as-team-name leak (session 2 §4) also appears on **/vsfield** ("This period" standings: ahitaon@gmail.com rank 3) and **/pool** (league picks: "yader.rosales@gmail.com Canada"; Leaderboard rows 8–9). It surfaces anywhere the team-name field renders, i.e. across every shared leaderboard — reinforces MEDIUM/PII.
- **Time-zone finding (formalized under step 58):** `/games` renders kickoff in **UTC with no zone label** — "Sun 28 Jun · 19:00 · R32" and "Thu 11 Jun · 19:00 · MD1" — while `/pool` shows the **same** SA–Canada kickoff as "Jun 28 at 3:00 PM **ET**". 19:00 UTC = 15:00 EDT, i.e. the flagged **~4h offset, no zone label** on `/games`. See step 58.
- **Positive (not a defect):** the group match page adds a **"YOUR XI · N in this match"** strip surfacing your own players + live rating + fantasy pts (e.g., Raúl Rangel 7.3 +13, Raúl Jiménez 7.9 +12) — a genuinely useful touch.

## Results — §7 /commish (steps 45–50) — NAVIGATE + READ ONLY

_Safety held: no Save/Freeze/Cut/Apply tapped, no type-to-confirm entered, nothing mutating. **AUDIT ENTRIES stayed 0** the whole pass (re-verified by scrolling to the very bottom of the page — the "Audit log · Append-only history" reads "No commissioner actions recorded yet."). Tabs present: **Playoff cuts · Stat corrections · Roster & lineup repair** — PLUS a **4th "Game operations" tab that is off-screen** (proven to exist by the Playoff-cuts "Blocked: freeze the round in Game operations first" message; see step 48). Badges: IRREVERSIBLE·LOGGED / RE-SCORES+LOGGED / SAFE ONLY·LOGGED. Header: "Commissioner mode — every action is logged to the audit trail" + a "View as…" button._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 45 · Type-to-confirm keyboard ⚠ | /commish | OPERATOR-REQUIRED | n/a | The freeze/cut type-to-confirm flow is exactly the forbidden mutating path — **not opened**. The Playoff-cuts ladder shows a per-round freeze/cut model (R32 10−2→8 "next up · not frozen"; R16/QF/SF/Final "not frozen"), so the confirm exists. Real-device test (type "Freeze"/"Cut" with the stock auto-capitalizing keyboard, observe whether the button ever arms — **without completing**) stays with the operator. |
| 46 · Borderless inputs ⚠ | /commish | PASS (not reproduced) | mirror-valid | The Stat-corrections **"Select a match…"** and Roster-repair **"Select a manager…"** selects both render with a **clear visible border** (rounded outline + chevron), not bare text patches — the `--border` token is defined/rendering. F-P2 borderless-input concern does **not** reproduce on these controls. (Downstream penalty/rating inputs appear only after selecting a match+player — a mutating cascade — so not exercised.) |
| 47 · Bottom clearance | /commish | PASS | mirror-valid | Checked on the **longest tab (Game operations, 8 period cards MD1→Final)** scrolled to the end: the last card (**Final · Knockout · pending · [Freeze…]**) and its button sit **above** the bottom nav (~20px margin — clears, though not generous). The Audit log tab also ends above the nav. (Audit-log populated-state re-check still worthwhile since it's currently empty.) |
| 48 · Tab strip overflow ⚠ | /commish | PARTIAL (F-P2 discoverability) | mirror-valid | The tab strip has **4 tabs** but at ~390px only ~2.5 show: "Playoff cuts · Stat corrections · Roster & lineu[p clipped]". The 4th **"Game operations"** tab is **off-screen right** and it's **load-bearing** — the Playoff-cuts panel blocks the R32 cut with **"Blocked: round R32 is not frozen — freeze the round in Game operations first,"** so freezing (which gates every cut) lives only in that tab. **It IS reachable** via a deliberate **horizontal swipe on the strip** (trackpad two-finger swipe / a `scroll right` with enough travel worked; small nudges and a click-drag did NOT, which is why it looked unreachable at first). BUT there is **no scroll affordance** (no arrow/fade/peeking chevron) — so the audit's F-P2 "is it obvious the strip scrolls?" is essentially **confirmed: not obvious.** A first-time commish on mobile could easily miss the freeze gate. Reachable = yes; discoverable = no. |
| 49 · Cut-preview with real names | /commish | PARTIAL (read-only, deep-dive) | mirror-valid | The alive-field cut-preview is shown **read-only** under "Next cut: R32" (no tap needed — it renders below the Blocked banner; I did NOT trigger any apply). Full 10-row table Manager · R32 pts · Total, ranked, blade = bottom 2 (lowest R32 score): …9 **SI SE PUEDE… (62)**, 10 **Jager FC (26)**. **Most team names fit fully** (Cheles, Chevanton F.C, Los Dragones, Gunners FC, They Paid, FENIX FC, MR.ZETTA) — the audit's "ellipsize to ~5 chars" does **not** happen; only genuinely long strings truncate at ~11–13 chars ("SI SE PUEDE…") and the **email-as-name "ahitaon@gm…"** (that's the N2 leak, not generic ellipsis). **No CUT chips render in this not-frozen preview** (the blade is conveyed only by ranking + "lowest round score is eliminated"), so CUT-chip overflow can't be evaluated until a round is frozen → operator/live for that piece. |
| 50 · Ledger timestamps | /commish | SKIPPED-STATE-UNAVAILABLE | mirror-valid (state) | **Audit log is empty — "AUDIT ENTRIES 0 · No commissioner actions recorded yet."** No rows exist, so timestamp visibility-on-touch and league-local formatting can't be evaluated. Re-check after a real logged action exists. (System status also shows **FROZEN PERIODS 3** — i.e. the 3 completed group periods MD1–MD3 are frozen; R32+ read "not frozen" on the ladder, which reconciles.) |

**§7 addendum — Game operations tab (reached via swipe, read-only):** [RESTATEMENT GATE · LOGGED] "Period freeze — results finality." Per-period cards: MD1 frozen since 2026-06-18 (7 pending corrections held), MD2 since 2026-06-24, MD3 since 2026-06-28 (all `[Frozen — unfreeze]`); **R32 · Knockout · open · ● live · `[Freeze…]`**; R16/QF/SF/Final · Knockout · pending · `[Freeze…]`. Not one Freeze/unfreeze tapped. **Useful for step 16:** the copy states freezing "**does not lock lineups and does not pause live scoring**" — it only stops auto-restatement — so a frozen-period `/lineup` should still read swappable with scoring live (partial answer to step 16 without manufacturing a freeze). The `[Freeze…]`/`[Cut]` "…" buttons are the type-to-confirm entry points for step 45.

## Results — §8 auth + settings (steps 51–57)

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 51 · Sign-in fold | (login) | OPERATOR-REQUIRED | n/a | Requires the **logged-out** sign-in screen; I'm logged in as commissioner and the brief says stay logged in (logging out risks needing an email magic link to return). Check "Send magic link" above the fold on an SE-class device on-device. |
| 52 · Input zoom ⚠ | /settings | **FAIL (confirmed)** | mirror-valid | Focused the **Display name** field → **the whole viewport zooms in** (all content enlarges; the "Settings" title clips at the left edge to "…ettings"). Tapped **Done to blur → it STAYS zoomed** (title still clipped; bottom-nav "Dashboard" label now clipped to "shboard"). Classic iOS **<16px input → auto-zoom with no reset**. Confirms **F-P1 systemic input zoom** ("zooms on focus and stays zoomed after blur"). (iOS also offered an AutoFill "Sergio Rios" chip — cosmetic.) The bid-amount exception wasn't re-tested but the systemic pattern is confirmed on a representative input. |
| 53 · Magic-link interstitial | (email) | OPERATOR-REQUIRED | n/a | Needs the emailed magic link tapped on cellular; time the blank interstitial before landing. Email-dependent → operator. |
| 54 · iOS push, Safari tab | /settings | OPERATOR-REQUIRED (control observed) | mirror-valid (UI present) | The controls exist and render: **"Enable browser notifications"** (primary) + **"Send test"**. **Not tapped** — enabling triggers the OS permission prompt (forbidden) and "Send test" would fire a notification. Record the exact non-A2HS behavior/message, then A2HS + repeat, on-device. |
| 55 · Mid-enable failure ⚠ | /settings | OPERATOR-REQUIRED | n/a | Needs granting permission then flipping airplane mode to catch the "Enabling…" stuck-forever state. Permission-gated → operator. |
| 56 · Long email clip | (check-email) | OPERATOR-REQUIRED | n/a | Needs the logged-out check-email view with a long address at 360px. Sign-in-dependent → operator. |
| 57 · Expired link | (email) | OPERATOR-REQUIRED | n/a | Needs a >15-min-old magic link reused. Email-dependent → operator. |

**§8 audit-integrity note — one inadvertent state change (remediated).** While testing **step 52** (input-zoom) on `/settings`, the **"Player not starting" notification toggle** was inadvertently flipped **ON→OFF and auto-saved** (these toggles auto-save with no confirm; the persistent input-zoom had shifted the tap coordinate onto the toggle during a zoomed scroll/tap). **Caught immediately and remediated:** toggled it back **ON**, reloaded, and confirmed all three notification toggles (Draft turn · Player not starting · Match starting) read **ON** and persisted server-side. This is the **only** deviation from the observe-only mandate in the entire audit — no other setting, lineup, waiver, `/commish`, or draft state was ever mutated. Reported to Sergio in-session at the time.

## Results — §9 time-consistency (steps 58–59)

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 58 · Same kickoff, four screens ⚠ | /games vs /pool vs /lineup vs /waivers | **FAIL (confirmed)** | mirror-valid | **Same fixture, two representations:** South Africa–Canada (R32) shows **"Jun 28 at 3:00 PM ET" on /pool** but **"Sun 28 Jun · 19:00" on /games** (24-h, **no zone label**). 19:00 UTC = 15:00 EDT, i.e. exactly the flagged **~4h offset with `/games` rendering in UTC and no zone tag.** Group-phase confirms the pattern (Mexico–SA MD1 = "Thu 11 Jun · 19:00" on /games). **/lineup** labels player kickoffs correctly with **"EDT"** ("Wed, Jul 1 at 4:00 PM EDT vs Senegal"). **/pool hardcodes "ET"** on every card. **/waivers** uses **relative countdowns** ("75h 54m to cutoff" on the FA card / batch context — no absolute time, so no zone shown). Net: `/games` is the ~4h-off, zone-label-less outlier; `/pool` says "ET" regardless. Confirms **F-P1 + the F-P2 zone-label gaps.** |
| 59 · League tz | /settings, /commish | PARTIAL (no explicit setting) | mirror-valid | **No league-timezone value is exposed** in /settings (Public profile + Notifications only) or in /commish System status (managers/periods/frozen/audit only). The effective zone is **America/New_York** — inferred from the consistent **"ET"/"EDT"** labels on /pool and /lineup, and confirmed by the math (19:00 UTC = 3 PM EDT). So the §58 offset is anchored, but the tz itself isn't user-visible to confirm directly. |

## Results — §10 /draft (steps 60–62)

_Draft is **COMPLETE** ("Snake draft · DRAFT COMPLETE"). Header still carries a live **● SYNCED** pill + an editable **pick-clock input (placeholder "900" s) + "Set clock"** button + a **"1/12 online"** presence count — all present/enabled on a finished draft. Two tabs: **Board** (12-col draft grid) · **Your squad** (15-man roster). No mutating control tapped (never pressed "Set clock")._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 60 · Realtime leak | /draft | PROVISIONAL | mirror-degraded | On a **permanently-complete draft**, the page holds an **active realtime connection**: the **● SYNCED** pill stayed green and **"1/12 online"** presence persisted across a **~90s idle** (5:14→5:16) with no disconnect / "Reconnecting". Behaviorally confirms a websocket is kept open on a finished-draft view (the F-P2 "leak" concern). Frame-level proof (actual socket frames, battery/network churn) needs DevTools/desktop-pairing → not visible through the mirror. |
| 61 · Set-clock inert-success | /draft | OPERATOR-REQUIRED (precondition confirmed) | mirror-valid (UI) | The **set-clock control is present + interactive on a DRAFT COMPLETE draft**: focusing the numeric input raised the iOS number-pad accessory (steppers + Done) with a blue focus ring — it accepts input. The **submit** ("Set clock") is the forbidden mutating tap → **not pressed**. So the FAIL's *precondition* (a live set-clock affordance on a finished draft) is **confirmed**; whether submitting returns a silent "success" needs the operator to tap Set clock and watch the response. |
| 62 · Board header names @360 | /draft (Board) | PARTIAL (fits/truncates @≈390px) | mirror-valid | Surveyed **all 12 manager column headers**: You · Chevanton F.C · Gunners FC · Jager FC · MR.ZETTA · Cheles · **yader.rosales@gmail.com** · **ahitaon@gmail.com** · SI SE PUEDE FC · FENIX FC · **Los que todo lo hacen ma…** · They Paid. **Containment holds** — long names **clip/ellipsize at the column boundary** and do **not** bleed into the neighbor ("Los que todo lo hacen ma|" is hard-cut at its own right edge; player cells ellipsize cleanly). Two cosmetic snags: (a) header truncation is sometimes a **hard mid-word clip with no visible "…"**, and (b) the **email-fallback names fill the column edge-to-edge with ~no side padding**, crowding the divider (short names sit centered with padding). True 360px would clip earlier → flag. |

### NEW FINDINGS (session 2, §10)

- **N5 · Draft "Your squad" position counter styles an over-cap tile as satisfied — LOW.** Counters read **GK 2/2 · DEF 4/5 · MID 6/5 · FWD 3/3 (15/15 rostered)**. **MID is 6/5 — over its displayed cap** — yet the MID tile keeps the **green "requirement-met" border**, while DEF 4/5 (under cap) has none. An over-cap count is styled valid rather than flagged; the cap metadata (5) doesn't match what the draft actually allowed (6). Draft is complete/historical → low impact.
- **N6 · Email-address team-name fallback is exposed on the draft Board too (reinforces N2) — MEDIUM (PII).** Two of the twelve draft columns are headed by **raw personal emails** ("yader.rosales@gmail.com", "ahitaon@gmail.com") because those managers never set a team name — visible to every league member. Same fix as N2: fall back to a display name / "Manager N".
- **Corroboration of step 52 (input zoom):** focusing the /draft **pick-clock input** reproduced the **systemic input-zoom** — viewport zoomed on focus and **stayed zoomed after blur AND after a reload** (only a fresh URL navigation reset scale). Independent 2nd instance of the F-P1 input-zoom defect, on a different input than /settings.

## Results — §11 /playoffs (steps 63–67)

_"Guillotine" view. **R32 · ROUND 1 OF 5 is ● LIVE.** Hero: "LOWEST 2 GET THE CHOP · 10 standing · 8 advance", an **ON THE BLOCK** red box (SS = SI SE PUEDE FC, JF = Jager FC), and a **YOU band** — "✓ Surviving · 4/10 · +16 clear of the blade · 79 pts" — with the survive/rank accent in **cobalt**. Two view tabs: **Board** (round-nav R1–R5 + per-round ladder) · **Ladder** (streamlined live standings, no round switch). Full R1/R32 ladder: 1 Cheles 96 · 2 Chevanton F.C 94 · 3 ahitaon@gmail.com 85 · **4 You 79** · 5 They Paid 74 · 6 FENIX FC 71 · 7 Gunners FC 70 · 8 MR.ZETTA 69 — [blade: "⚑ Lowest 2 cut"] — 9 SI SE PUEDE FC 63 · 10 Jager FC 26 (rows 9–10 render maroon fill + red blade icon)._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 63 · Eliminated-manager band ⚠ | /playoffs | SKIPPED-STATE-UNAVAILABLE (exact) + proxy | mirror-valid (proxy) | The precise FAIL state — an **eliminated** manager's summary band while a **later** round is live — **isn't reproducible now**: R32/R1 is the **first** live round, no guillotine cut is final, and future rounds (R2=R16 … R5=Final) show a clean **"N survivors enter · set once the prior round locks"** placeholder (no eliminated bands). Logged in as "You" (surviving, rank 4) so can't *be* a cut band. **Proxy gathered:** in the ladder, cut teams DO get an **explicit red treatment** (maroon fill + red blade icon, not cobalt), and the top **YOU band uses cobalt for "✓ Surviving"**. The step's open question — does that cobalt band flip to an explicit red **Eliminated/Out** when the viewer is cut, or stay cobalt/blank — still needs a **cut-manager view** (operator: View-as / shoulder-surf a cut manager once a round locks; ~6 pm live window). |
| 64 · Board/Ladder + round-nav targets | /playoffs | PARTIAL | mirror-valid | Round-nav **R1/R2/R5/R3/R1 taps all hit first-try** at ~390px (buttons ~30px tall). The **Board/Ladder toggle** is reliable **when the tabs are fully visible at the top**, but it **tucks under the iOS status-bar tap zone once the page is scrolled** — Ladder taps there **failed repeatedly** (status bar catches the tap / scrolls-to-top) until I reset to the very top. Targets are usable at ~390px, but the non-sticky toggle's **status-bar collision on scroll is a real mis-tap trap** (supports F-P2-K2). True-360 would tighten the R1–R5 buttons further → flag. |
| 65 · Backgrounded socket honesty ⚠ | /playoffs, /vsfield | PARTIAL (foreground confirmed; background deferred) | mirror-degraded | **Foreground socket is demonstrably live + honest:** on "Vs the field · R32 · LIVE" the **"Updated HH:MM"** stamp self-advanced 05:32→05:33→05:34 while idle, and /draft's SYNCED/presence persisted (step 60). The **backgrounded** test (home-out 60–120s → return; watch whether the "LIVE" pill sits over a stale "Updated" stamp) **could not be completed**: backgrounding Safari via iPhone Mirroring (home swipe-up) didn't reliably trigger, and — per Sergio — **the match that would put live points in motion slipped 5:30 → 6:00 pm ET**, so the "frozen live points" scenario isn't live yet. **Deferred to the ~6 pm live window / on-device.** |
| 66 · Boundary-tie victims row | /playoffs | PARTIAL (no tie present) | mirror-valid | The cut boundary renders an **in-board avatar row** of the victims (SS, JF) just under the "⚑ Lowest 2 cut" blade — with **2 victims it fits comfortably** at ~390px, single line. But the **step's stress case (a whole *tied* set at the cut) isn't present** — blade scores are distinct (MR.ZETTA 69 → SI SE PUEDE 63 → Jager 26). So the "avatar row overflow / no-wrap" question is **unexercised**; the mechanism exists and is single-line. Re-check when a boundary tie exists (live). |
| 67 · Cut-zone contrast outdoors | /playoffs | PARTIAL / PROVISIONAL | mirror-degraded (daylight=operator) | Cut rows (9–10) use a **low-saturation maroon fill** on the near-black page + a small **red blade icon** (replacing the green ✓) + a muted **~9–11px "seed #" eyebrow**. On-screen it's distinguishable, but the **maroon-vs-black row contrast is subtle** and the eyebrow is small — plausible readability loss in bright daylight (the F-P2-I4 concern; pairs with step 35). Actual sunlight test = operator/on-device. |

### NEW FINDINGS (session 2, §11)

- **Positive (not a defect):** the **live "Vs the field" view carries an explicit "Updated HH:MM · LIVE" freshness stamp** that self-advances each minute — exactly the freshness cue the /pool Leaderboard (step 36) and the playoffs socket (step 65) were flagged for lacking. Good pattern to propagate to those surfaces.
- **Manager census (12 teams; R1 seed in parens):** You / Los Dragones (9) · Chevanton F.C (5) · Gunners FC (2) · Jager FC (7) · MR.ZETTA (4) · Cheles (1) · yader.rosales@gmail.com · ahitaon@gmail.com (8) · SI SE PUEDE FC (6) · FENIX FC (3) · Los que todo lo hacen mal · They Paid (10). **2 of 12 use email-fallback names** (N2/N6). 12 total → **10 entered R32** (2 eliminated pre-knockout).

## Appendix A — live-state steps (status)

A knockout **round is live (R32)** with an active socket, so this is **no longer a blanket SKIP** — but per Sergio the **match that puts new points in motion slipped 5:30 → 6:00 pm ET**, so the point-driven live states can't be seen until then.

- **Live minute / HT indicator (step 38):** R32 *period* is LIVE and some R32 fixtures are already **FT** (South Africa 0–1 Canada; Brazil 2–1 Japan). A **running-clock match page** needs the 6 pm kickoff → **pending 6 pm**.
- **Live lock-flip (roster/lineup lock at kickoff):** "1 still to come" (one of my players is in the delayed match) → capturable **at 6 pm**.
- **Blade-drop (cut line moves on live points):** ladder static pre-kickoff (blade between MR.ZETTA 69 and SI SE PUEDE 63) → **pending 6 pm**.
- **ET / penalties (step 39):** sampled R32s were normal/ET, none shootout-decided → still need a **penalty-decided** knockout.
- **FA-window flip:** acquisition LOCKED for R32 → needs a window transition.

**Recommended: a focused ~6 pm ET live pass** for live minute, lock-flip, blade-drop, the full step-65 backgrounded-socket test, and step 63's eliminated band once a cut is final.

## Summary counts — session 2 §10–§11 (steps 60–67)

- **On-device verdicts recorded:** 8/8 (60–67). PARTIAL ×4 (62, 64, 66, 67) · PROVISIONAL ×1 (60) · OPERATOR-REQUIRED, precondition-confirmed ×1 (61) · PARTIAL-foreground/deferred-background ×1 (65) · SKIPPED-STATE + proxy ×1 (63).
- **New findings:** N5 (over-cap tile styled valid, LOW) · N6 (email team-name on draft board, MEDIUM/PII, reinforces N2) · input-zoom (52) corroborated on /draft.
- **No new hard FAILs in §10–§11**, but no clean PASSes either — every step is device-width-caveated (390px vs 360px) or live-state-gated.

### Cumulative confirmed defects (whole audit, both sessions)
- **Step 6** — 404 = unbranded white Next.js default (no nav, not dark-aware). FAIL.
- **Step 13** — stale "PROVISIONAL CUT" copy persists post group→playoff transition. FAIL.
- **Step 52** — systemic input-zoom (zooms on focus, no reset after blur/reload); corroborated on /settings **and** /draft. FAIL.
- **Step 58** — same kickoff renders ~4h off with no zone label on /games (UTC) vs "ET" on /pool. FAIL.
- **N1** bottom-nav tap unreliability · **N2/N6** raw-email team names across waivers/vsfield/pool/draft-board (PII, MEDIUM) · **N4** "balldontlie" provider string in user-facing score copy (LOW) · **N5** over-cap draft tile styled valid (LOW).

## Human follow-up queue — additions (session 2, §10–§11 + Appendix A)

**OPERATOR-REQUIRED (Sergio, on-device):**
- **61** — tap "Set clock" on the COMPLETE draft once; confirm whether it returns a silent/false "success" (mutating — do once, note the toast).
- **63** — View-as (or shoulder-surf) a **cut** manager once a round locks; confirm the summary band shows an explicit **red Eliminated/Out**, not the cobalt "surviving" accent or a blank "–/N · – pts".
- **65** — on a live round, background Safari 60–120s, return; confirm "LIVE"/"Updated" stays honest (flips to Reconnecting or refreshes) rather than sitting stale.
- **67 / 35** — daylight readability of the maroon cut-zone fill + 9–11px eyebrow text.

**PENDING ~6 PM ET LIVE MATCH:** live minute / HT (38), lineup lock-flip at kickoff, blade-drop on live points, boundary-tie victims row if a tie forms (66), penalty-decided knockout (39).

**Fix candidates (defects):** N5 (over-cap tile styled valid) · N6/N2 (email-as-team-name PII across board/waivers/vsfield/pool) · confirmed input-zoom (52, corroborated on /draft) · kickoff-tz (58).

## LIVE-MATCH PASS — 6 pm ET kickoff (resolves the deferred live steps 38 / 39 / 65 + Appendix A)

_The delayed R32 real match kicked off ~6 pm ET: **Argentina 0–0 Cape Verde** ("Fri 3 Jul · 22:00 · R32"). Captured with the round **● LIVE** and fantasy points moving. Sergio backgrounded Safari to the iOS home screen, which enabled the clean step-65 resume test. These verdicts **upgrade the earlier SKIPPED/deferred entries** for 38, 39, 65._

| Step | Screen | Verdict | Medium | Observation |
|---|---|---|---|---|
| 38 · Live minute ⚠ | /games (live: Argentina 0–0 Cape Verde) | **FAIL (confirmed)** — was SKIPPED | mirror-valid | The in-play match shows a **bare "● Live" pill + the 0–0 score but NO running match minute / clock** in the header. The only time present is the fixed **kickoff** "Fri 3 Jul · 22:00 · R32". A user can't tell what minute it is from the app — exactly the flagged "bare 'Live' word, no live clock / HT" defect. |
| 39 · ET / pens ⚠ | /games (Netherlands 1–1 Morocco; Germany 1–1 Paraguay — FT) | **FAIL (anomaly present)** — was OPERATOR-REQ | mirror-valid | Drawn knockouts render "**1–1 · Full-time**" with **no AET / penalty-shootout / "advanced" indicator** (NED 1–1 MAR: Gakpo 72', Issa Diop 90+1'; GER 1–1 PAR). A real R32 knockout can't end level, so "1–1 Full-time" is the flagged score-anomaly. Caveat: this is a **guillotine-by-fantasy-points** format, so the real winner is arguably moot for standings — but the display anomaly the step targets is present. |
| 65 · Backgrounded socket honesty ⚠ | /vsfield (live) | **FAIL (confirmed, bounded ~20 s)** — was PARTIAL/deferred | mirror-valid | Safari backgrounded (home screen) during the live round, then reopened. **On resume the view showed the "● LIVE" pill over a STALE "Updated 06:01 PM" timestamp while the real time was ~06:25 PM (~24 min stale)**, with **no "Reconnecting/Delayed" state**. It then **silently refreshed to "Updated 06:26 PM" (current) after ~15–25 s**. So the Live pill sat over frozen data for ~20 s on resume before self-healing — the F-P2-K3 FAIL (bounded, not indefinite; no honest reconnecting cue). |

**Appendix A live-states — CAPTURED this pass:**
- **Live points / blade-drop:** the /playoffs ladder updates live and **re-sorts**. Vs the ~5:30 baseline: **SI SE PUEDE FC 63 → 64** (the cut line / blade itself), **Gunners FC 70 → 72 → 71**, **FENIX FC 71 → 72 → 71** (ranks 6–7 swapped mid-flight), and the YOU band recomputed **"+16 → +15 clear of the blade."** The "● LIVE" pill + "Updated HH:MM" stamp advance each minute in the foreground. **Blade-drop confirmed — the cut threshold moves in real time.**
- **Live match feed:** Argentina–Cape Verde "**Feeds 4 of the league's XIs · 7 started, 0 benched**"; per-player live fantasy chips render on the pitch (+1 / +2 / +3 / +8).
- **Step 58 corroboration (live instance):** the live-match kickoff renders **"22:00" (UTC, no zone)** — 22:00 UTC = 18:00 EDT = the 6 pm ET kickoff — another instance of the /games UTC-no-zone-label defect (step 58).

**Appendix A live-states — STILL not captured:**
- **Live lock-flip:** /lineup shows **"1 movable · 8 locked · UP TO DATE"**, but **none of my players are in the live Argentina match** (no YOUR-XI strip there), so the movable→locked flip wasn't triggered by this kickoff. Lock STATE documented (kickoff labels carry EDT); the live flip needs a kickoff of the match holding my 1 remaining player.
- **Step 63 · eliminated-manager band:** R32/R1 is still live and **no guillotine cut is final** (round not frozen), so no manager is yet in a finalized Eliminated state. Still needs a locked/frozen round — or View-as a cut manager.
- **FA-window flip:** acquisition still LOCKED for R32.

### Live-pass tally
- **Resolved from deferred → FAIL:** 38 (live minute), 39 (ET/pens anomaly), 65 (backgrounded socket). All three are **new confirmed defects** on top of the session-2 set (6, 13, 52, 58).
- **Appendix A:** blade-drop / live-points / live-feed **captured**; lock-flip, eliminated-band (63), FA-window-flip **still pending** the right micro-state (my-player kickoff / a frozen round).
- **Full confirmed-FAIL list (whole audit):** 6 (404 unbranded), 13 (stale provisional-cut), 38 (no live minute), 39 (drawn-knockout "Full-time" no ET/pens), 52 (input zoom), 58 (kickoff tz UTC/no-zone), 65 (backgrounded LIVE-pill over stale ~20 s). Plus findings N1, N2/N6 (email PII), N4 (balldontlie), N5 (over-cap tile).
