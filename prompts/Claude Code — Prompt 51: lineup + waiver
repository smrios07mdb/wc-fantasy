Claude Code — Prompt 51: lineup + waivers UI polish (4 parts, in order)

Run Opus 4.8, high. Branch off main (now 4dd6cd5, P50 merged): feat/lineup-waivers-polish. Four parts in the order below; commit each as its own conventional commit. Display/filter only — no engine, scoring, resolver, or FAAB-firing changes; resolve.ts/purity matrix untouched.
Read first: the lineup page (loader + components); the waivers screen (apps/web/app/waivers/loadWaivers.ts, apps/web/src/waivers/*); the draft pool filter (Prompt 31 — the collapsible country filter to reuse); the flag resolver (Prompts 35/36 — the 48-country flag component on draft/roster/lineup, incl. the hand-authored England/Scotland SVGs); period (kind, label); fifa_match (kickoff_at, period_id, home/away team); player.country + player.team_id; league.timezone; P50's formatInLeagueTz (apps/web/src/waivers/waiversLogic.ts).
Part 1 — Lineup matchday order (priority). The lineup page's matchday/period selector is mis-ordered. Sort it by the canonical tournament order: MD1, MD2, MD3, R32, R16, QF, SF, Final — group matchdays (by MD number), then knockout rounds in bracket order. Not by opens_at and not alphabetical (alpha gives Final/QF/R16/R32/SF — the bug). If a shared period-order constant exists (the provision seed order is this sequence), reuse it; else add a small canonical PERIOD_ORDER in @app/shared as the single source.
Part 2 — Country filter on waivers (collapsible), mirroring the draft. Add the same collapsible country filter the draft pool uses (Prompt 31) to the waivers FA pool — reuse the component/logic, don't reinvent. Collapsed by default; filters the displayed claimable-FA list by nation(s).
Part 3 — Flags on waivers players. Render each waivers player's country flag via the existing resolver (Prompts 35/36), from player.country. Match the flag-chip placement/sizing used elsewhere; don't set background-size:cover on kit chips (the project-wide kit-render gotcha).
Part 4 — Match date + time per lineup player. Show each player's fixture date + time (all 15, starters and bench), prominently. Resolve per player for the period being viewed: player.team_id → the fifa_match in that period where the team plays (home or away) → kickoff_at, formatted in league.timezone (reuse formatInLeagueTz; lift it to a shared util if cleaner than duplicating). It doubles as the player's lock/sub deadline, so keep it legible. Resolve-miss (knockout TBD) → "TBD"/"—", never crash.
Tests: Part 1 — canonical order (knockout not alpha); Part 4 — per-player kickoff resolves + formats in a non-UTC tz; Parts 2/3 — assert wiring of the reused components. Existing suites green; resolve.ts/purity untouched.
Brain files: PROJECT.md status; ARCHITECTURE.md only if you add PERIOD_ORDER / lift formatInLeagueTz. DECISIONS.md/SCORING.md untouched.
Gate: pnpm -w typecheck && pnpm lint && pnpm format:check && pnpm test.
STOP after committing (4 commits, in order; no force-push). Report each part's diff, the rendered canonical order, and a sample per-player time string (league-tz). No merge, no push — Chat clears.